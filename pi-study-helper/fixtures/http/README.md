# W1 HTTP 安全响应 fixture 目录

第一周不实现 HTTP 服务。本目录仅预留给后续 HTTP 安全 DTO 的 fixture；文件名采用 `<use-case>-safe-response.json`。

任何 fixture 只能使用允许字段白名单生成，且不得出现正确答案、隐藏测试、私有数据、参考实现、完整 Rubric、密钥或宿主绝对路径。真实 HTTP 实现后，E 应将响应序列化结果与本目录 fixture 对照并执行泄漏扫描。
