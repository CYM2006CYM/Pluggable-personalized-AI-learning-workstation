# 质量报告（draft）

当前候选包不得启用。D2/D3 审计整改已将私有变体 02 从源码静态检查改为真实 `clean_df` 执行测试，并由候选复算脚本连续运行三次、记录列序、dtype、行序和摘要指纹；该证据仍只表示 B 候选结果。

来源许可证与摘要哈希、最终 CSV 和唯一 `clean_df`、具体 dtype、环境上限及资产哈希仍待负责人/C 原型确认；CIDPP 五维仍待人工复核；私有资产隔离待 E 审计。当前不得宣称 V2-3 通过或将 Profile 转为 `active`。
