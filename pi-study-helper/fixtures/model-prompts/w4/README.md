# W4-D2 recorded model boundary

These prompts and recordings are deterministic D-owned fixtures for `W4-C2/W4-R1`.
They do not contain a live key, HTTP header, private Profile answer asset, hidden test,
Rubric, reference solution, host path, or learner source text. Runtime CIDPP remains off.

Result labels are `RECORDED_PASS`, `MOCK_FALLBACK_USED`, and `LIVE_NOT_RUN`.
