# W5-D2 E 岗位交付报告

交付状态：`READY_FOR_OWNER_REVIEW / NOT_COMMITTED / NOT_PUSHED / uploadLock=NOT_REQUESTED`

基线：`HEAD == origin/main == 127a71cce4a8423327fb5ce75d31294252b92a0b`

合同：`W5-C1 / W5-R1`

## 1. 本次附件

1. `W5-D2-E-delivery.zip`
   - 大小：`215943 bytes`
   - SHA-256：`40ea7e52cef617fe1dc60771ae9d243e03db30e20165bc94e7956ff86f5f04ea`
   - 条目：`39`
   - 与 `d2-proposed-files.txt` 比较：`39/39`，缺失 `0`，额外 `0`
   - Manifest 复算：`38` 个文件，SHA-256 不匹配 `0`
2. `W5-D2-E-交付报告.md`
   - 本报告为 ZIP 外的转发说明，不计入候选拟提交清单。

## 2. 上游绑定

- A-D1：`0fd1f45386682a3859d8d9f6b37904b47ae98c33`
- C-D1 / 负责人 D2 起点：`677f54c609ef3bfbe78ff6d37f6b432e9c68ff4d`
- A-D2 正式主干：`127a71cce4a8423327fb5ce75d31294252b92a0b`

A-D2 交接正文仍保留候选阶段状态，但本候选以 Git 正式提交事实为准，没有修改 A 的交接文件。

## 3. 完成内容

- 直接复用正式 `PublicExecutionBundle`、`BrowserCodeRunner` 和 `PreparedActivityOutput`，没有建立第二套 Facade、DTO 或提交协议。
- 完成专用 Worker、每次运行独立实例、`runId` 迟到消息拒绝、取消、墙钟超时、UTF-8 输出字节上限、错误销毁和下一次重建。
- 公开预览固定执行“保存草稿 -> 请求公开包 -> Worker 本地运行”，预览结果不回传、不正式提交、不创建 Evidence 或路径变化。
- 新增 `/study?sessionId=...&nodeId=...&activityId=...` 安全深链恢复；未知、重复、非法或错配参数均进入开始页恢复，不调用 `startSession`。
- 开始页支持安全 Profile 选择，并显示对应 revision/modalities；背景问卷和章节入口保持可用。
- 路径页显示诊断安全画像，`mastery=null` 明确显示 `unverified`，不伪造为 `0`。
- 草稿按 `sessionId/activityId/attemptId/profileRevision/draftVersion` 绑定到 `sessionStorage`，仅在 Bootstrap 确认同一 Attempt 和版本后恢复；正式提交成功后清理。
- Pyodide 明确登记为 `PYODIDE_CANDIDATE_UNAVAILABLE`，未使用 CDN、未访问网络、未新增依赖；正式 Node/Python 提交保持独立可用。

## 4. 验证结论

- `npm.cmd run typecheck`：PASS
- Web 测试：`14 files / 97 passed`
- A/C/共享会话受影响回归：PASS
- 全量测试：`93 files / 809 passed / 1 skipped`
- `npm.cmd run verify`：`93 files / 809 passed / 1 skipped`
- docs、Web build、demo build、extension smoke、release：PASS
- `git diff --check`：自然退出码 `0`；仅存在交付前已有的 W4 行尾警告
- 结构化命令：`11/11 PASS`
- DOM、网络、Worker、公开 bundle、生产 bundle、日志与 sessionStorage 安全扫描：PASS，敏感命中 `0`
- Microsoft Edge 桌面/移动证据：`4/4 PASS`，非空、无横向溢出、未命名控件 `0`
- 精确拟提交清单：`39` 个文件，E 所有权范围外文件 `0`

首次验证使用 WindowsApps Python 占位程序导致既有 Python 测试失败；第二次使用 Node 24 导致正式环境锁不匹配。两次失败均保留在结构化证据的 `priorRuns`。最终复验使用 Node `22.23.1`、npm `10.9.8`、Python `3.13.7`、Pandas `3.0.5`，全部通过。

## 5. 边界与未声明事项

- `LIVE_MODEL=LIVE_NOT_RUN`
- `PYODIDE=PYODIDE_CANDIDATE_UNAVAILABLE`
- `D4_CROSS_CLIENT_CLOSURE=NOT_RUN`
- 不声明 D4 Web/TUI 正反向跨端闭环，不声明三个正式案例闭环，不声明 W5 PASS。
- 未修改 A/B/C/D 实现、Profile、gold、SDK、依赖、锁文件或负责人合同。
- 未移动或改写现有 W4 证据。
- ZIP 不包含 `.zcf/`、`node_modules/`、`dist-web/`、临时数据、原始日志或无关工作树文件。

## 6. 请负责人处理

1. 校验 ZIP SHA-256 与本报告一致。
2. 按 ZIP 内 `handoff-w5-e-d2.md`、`d2-command-results.json`、`d2-security-scan.json` 和 `d2-sha256-manifest.json` 进行复核。
3. 若候选通过，请明确授予 E 岗位上传锁和提交范围。
4. 在获得明确上传授权前，候选继续保持 `NOT_COMMITTED / NOT_PUSHED`。
