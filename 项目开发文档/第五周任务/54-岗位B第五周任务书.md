# 54 岗位B第五周任务书

> 所有权：revision 3环境锁/seal、三个展示案例和PPT大纲
>
> 正常上传：D3，必须消费负责人Pyodide裁决和C正式测量提交

## 0. 开工输入

D1—D2可准备案例Schema和PPT结构，但不得填写环境数值或生成最终seal。D3必须拉取C正式提交，核对实测命令、版本、10组对照、限制和负责人`PYODIDE_ENABLED`或`PYODIDE_DISABLED_WITH_NODE_FALLBACK`裁决。

## 1. 必做交付

1. 逐字段更新revision 3 `environment-lock.json`；Node/Python/Pandas保持批准值，Pyodide及能力标志只采用C证据。C只提供实测和证据，B承担Profile文件写入和seal重算。
2. 运行Schema、引用、旧revision保护后重算排除自身的revision seal；不得手改哈希。
3. 制作高基础、非计算机初学者、实践导向三个可重放输入；同一目标、revision和确定性规则。B只交付输入、差异假设和矩阵，不手写最终路径或Evidence。
4. 生成三案例预期差异矩阵；每对至少3项可观察差异，不预写实际系统输出。
5. 完成覆盖问题、方案、确定性主链、Agent边界、三案例、评测设计、限制和演示顺序的PPT大纲。

## 2. 边界与验证

- C证据为输入，B不得改C报告或将`false/unknown`写成`true`。
- Pyodide关闭时不得写`measured_dual_backend`；页面提示由E实现。
- revision 2逐字节不变；60例gold、隐藏测试和参考实现不进入展示案例。
- 三案例必须由A使用正式PathEngine运行，E独立复验；B不得手工编辑路径、难度、Evidence或mastery。

交付环境锁差异、seal、C证据映射表、三案例输入/矩阵、PPT大纲、结构化命令结果和`handoff-w5-b-d3.md`。案例交接必须注明“待A正式PathEngine运行、待E独立复验”，不得把预期差异矩阵写成实际系统结果。运行B资产、A Schema、三案例重放、revision 2不变、泄漏、全量、verify及差异检查。

若C证据缺字段、负责人裁决未签或seal复算失败，停止对应部分并提交精确阻塞；不得猜值。
