# W3-D3 岗位 E 验证工具基线交付报告

生成时间：`2026-08-09T13:17:50+08:00`

```text
contractVersion = W3-C4/W3-R2
deliveryStage = W3-D3
verificationTools = FROZEN_CANDIDATE
inputManifest = STRUCTURE_COMPLETE_INPUTS_PENDING
fullV3Status = NOT_RUN_D3
V3_1_TO_V3_8 = NOT_EXECUTED
gitStatus = NOT_COMMITTED / NOT_PUSHED
```

## 1. 交付结论

本交付已经建立 V3-1 至 V3-8 的验证工具、冻结配置、测试用例合同、证据模板和输入清单。D3只执行脚本解析、结构检查、已知输入哈希复算和Plan模式自测，没有执行任何D4仓库验证命令，没有生成V3 PASS/BLOCKED正文。

当前仓库快照：

```text
HEAD = a0730d667a9429e395e92f687dbf6b2e0e0db179
origin/main = a0730d667a9429e395e92f687dbf6b2e0e0db179
W3_START_COMMIT = f190326a4a906b46e4001484ffa30a7839b82ed2
```

## 2. 工具设计

- 一个通用执行器消费冻结门禁配置和解析输入清单，避免八套重复执行逻辑。
- D3默认且唯一允许模式为 `Plan`，只生成 `NOT_RUN_D3` 计划证据。
- Execute模式必须同时提供显式 `W3-D4` 令牌、实际仓库路径，并且该门禁所有必需输入均为 `FROZEN`。
- Execute只记录原始命令、退出码和stdout/stderr哈希；最终结论仍须按门禁测试合同核对机械指标，不能只看命令退出码。
- V3-4、V3-7、V3-8附带只读专项扫描器，分别覆盖C正式事实边界、gold冻结顺序和Web安全边界。

## 3. 八项门禁基线

| 门禁 | 冻结能力 | 预期机械项数 | 失败所有者 |
| --- | --- | ---: | --- |
| V3-1 | 20例、180分钟投影、每例10次 | 200 | A |
| V3-2 | 20例原始预算及2个边界fixture，各10次 | 220 | A |
| V3-3 | 2任务×8分类×3次 | 48 | C；业务资产问题点名B |
| V3-4 | C源码/端口/正式事实所有权4项 | 4 | C；A公开事务缺口点名A |
| V3-5 | 1项幂等+5个事务故障阶段 | 6 | A |
| V3-6 | 2任务×4类评测器故障 | 8 | C；事务映射问题点名A |
| V3-7 | 60 gold + 40终裁 + 4资格/顺序项 | 104 | 负责人或对应原始材料所有者 |
| V3-8 | D46整改后的Web作者测试 | 81 | E |

上述“预期机械项数”是D4证据应覆盖的合同项数，不表示D3已经执行或通过。

## 4. 输入清单

清单共22项：19项 `FROZEN`，3项 `PENDING`。已冻结文件、Git祖先、两个 `assetBundleHash`、环境锁和 `environmentHash` 已机械复算一致。

当前待输入：

1. A-D3正式提交；
2. D-D3正式提交；
3. 负责人只读gold候选目录及其哈希。

负责人提供三项输入后，应复制D3清单形成D4解析清单并填写真实值；不得回写或覆盖本D3冻结原件。任一既有冻结输入变化时停止执行，交负责人重新裁决。

## 5. D3自检

| 检查 | 结果 |
| --- | --- |
| PowerShell脚本解析 | 7个脚本，0错误 |
| 基线结构检查 | 8门禁、22输入、3待定，退出码0 |
| 输入绑定复算 | 22/22检查通过；19冻结、3待定 |
| 八门禁Plan模式 | 8/8退出码0 |
| Plan状态 | 8/8为 `NOT_RUN_D3 / NOT_PRODUCED_D3` |
| 无令牌Execute反例 | 退出码1，未生成命令日志 |
| 输入待定Execute反例 | 退出码1，未生成命令日志 |

## 6. 权限边界

- 未执行V3-1至V3-8、全量门禁、正式60例系统输出、gold验证或Go/No-Go。
- 未读取B原始标注或负责人机械差异清单。
- 未修改A/B/C/D实现、合同、SDK、依赖、Profile状态、环境锁或gold。
- 未对仓库进行暂存、提交、推送或上传锁申请。
- 所有新增材料均在仓库外 `D:/.A_C_code/PPALW/W3-D3`。

## 7. 负责人审核顺序

1. 校验 `W3-D3-E-package-hashes-r2.sha256`。
2. 审核输入清单包中19项冻结值和3项PENDING边界。
3. 逐包审核V3-1至V3-8的命令、项数、机械条件和失败所有者。
4. 核对八份Plan证据均为 `NOT_RUN_D3`。
5. 审核基线冻结包中的逐文件SHA-256和自检记录。

本报告只申请负责人审核D3工具与输入冻结，不申请D4执行授权。
