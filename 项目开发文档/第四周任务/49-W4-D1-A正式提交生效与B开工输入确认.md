# 49 W4-D1-A正式提交生效与B开工输入确认

> 签发日期：2026-08-13
> 适用周次：第四周 D1 结束、D1→B交接
> 合同：`W4-C2 / W4-R1`
> A正式代码上游：`4c52eb7c78fa80007aa9a8ab4e00768d71d3f3f5`

## 1. 文档性质

本文是负责人对岗位 A W4-D1 正式提交的生效确认，以及岗位 B 开始 revision 3 正式封存工作的输入确认。

本文不改写 A 的原始交接单、验证报告或历史审计记录。A 交接单中出现的 `NOT_COMMITTED / NOT_PUSHED / uploadLock=NOT_GRANTED` 是候选封包时的真实状态；本文件记录的是后续负责人审核、授权上传并完成正式提交后的生效事实。

本文不授予 B 上传锁，不批准 Profile activation，不宣布 W4 GO，也不替代 41—48 号文档的合同和权限边界。

## 2. A提交生效事实

负责人已审核 A 的完整候选并批准其上传。岗位 A 已将 55 个批准文件提交到 `main`，当前正式上游为：

```text
W4_START_COMMIT   = ac6e307e17cf84450845dfc5ffa467063dd3ae4c
A正式代码上游     = 4c52eb7c78fa80007aa9a8ab4e00768d71d3f3f5
B开工文档上游     = 包含本文的最新 origin/main
合同              = W4-C2 / W4-R1
```

A 的正式提交包含公共 contracts、17-method Facade、只读 Bootstrap、revision 3 Schema/seal 激活入口、PathEngine/题组/Attempt/事务实现、A/D 端口和对应测试。A 未创建或编辑 B 的 revision 3 正式内容、私有答案或 seal。

A 的 smoke 历史超时仍按复验报告记录为 `FLAKY_BASELINE_REPRODUCIBLE`；该历史事实不影响本提交作为 A 正式上游生效，也不等于 W4 全部门禁或产品闭环已经 GO。

## 3. B正式开工输入

B 自本文生效后可以把 A 正式提交作为 revision 3 封存的正式上游。B 开工前必须在自己的工作区执行：

```text
git fetch origin main
git pull --ff-only origin main
git rev-parse HEAD
git rev-parse origin/main
git merge-base --is-ancestor 4c52eb7c78fa80007aa9a8ab4e00768d71d3f3f5 HEAD
git status --short
```

实际 `HEAD` 必须等于拉取后的最新 `origin/main`，且 `git merge-base --is-ancestor` 必须以退出码 0 证明该开工提交包含 A 正式代码上游 `4c52eb7c78fa80007aa9a8ab4e00768d71d3f3f5`。`git status --short` 必须为空，工作区不得混入其他岗位改动。若拉取失败、HEAD 与 `origin/main` 不一致、祖先校验失败、出现冲突或 A 的公共 Schema 与 42 号不一致，B 必须停止正式资产落盘并提交问题，不得自行改 A 代码、复制另一套 DTO 或添加未冻结字段。

B 必须重新阅读并以以下顺序消费：

1. 20号负责人决策冻结清单 D48—D60；
2. 42号 W4-C2 公共合同第2—4节及 revision seal 算法；
3. 41号 W4-R1 总调度和权限边界；
4. 44号岗位 B 第四周任务书；
5. 02号可插拔资料包规范和 07号 Pandas 示范资料包设计；
6. 本文、48号补充裁决和 A 的 `handoff-w4-a-d1.md`。

## 4. B本轮只允许交付的内容

- 新建 `pandas-cleaning-revision-3-draft`，不得修改 revision 2；
- 六个核心知识点的 `activityPolicy=all_in_order` 和正整数 `contentEstimatedMinutes`；
- 每个核心知识点恰好一张固定 `LearningCardSafeView`；
- 每个核心知识点一个稳定 mcq 活动；
- 每个核心知识点一个 4—6 题完整 fixed 题组；
- 每个核心知识点另备 1—2 道与 fixed 不重合的 supplemental 题；
- 私有答案、解释、来源锚点、source map、质量报告、revision 3 seal 和逐文件哈希；
- revision 2 不变证明、公开/私有隔离证明、六点覆盖矩阵和 B 交接单。

B 不实现判分、事务、PathEngine、Agent、HTTP、Bootstrap、页面或画像任务；不修改 SDK、依赖、锁文件、W3 环境锁、TaskBundle、Rubric、正式 gold、hidden tests、reference solutions 或 A 代码。

## 5. 对B的关键语义提醒

- 核心六点按目标 `targetKnowledgePointIds` 的确定性并集识别；辅助先修点不自动变成核心内容资产；
- 卡片 ID 不得写入 `activityIds`，卡片不属于计分 Activity；
- `goal.requiredActivityIds` 只保留最终综合实操；
- supplemental 题只用于动态候选补位，不能冒充完整重试组；
- 固定题组必须是 4—6 题，公开资产不得含答案，私有答案必须与题面和来源一一对应；
- seal 必须在所有内容稳定后生成，任何字节变化都必须重新生成并复验；
- 不得把 `step_by_step`、`example_first` 或 `concise` 描述成已经改变卡片、路径或 Agent 行为；W4 当前只要求偏好可采集、校验、持久化和恢复。

## 6. B上传前置和停止条件

B 只能先形成未提交候选，保持：

```text
NOT_COMMITTED / NOT_PUSHED / uploadLock=NOT_REQUESTED
```

申请上传锁前必须完成 44 号任务书规定的资产定向、受影响回归、全量测试、`typecheck`、`check:docs`、`verify` 和 `git diff --check`，并提交完整目录、Manifest、seal、哈希、证据和交接单。B 不得把本文件解释为上传锁授权。

若发现 A 正式 Schema 与 42 号不一致、revision 2 字节变化、六点覆盖不足、题组答案泄漏、seal 复算不一致或需要修改 A/C/D/E 文件，立即停止并请求负责人裁决。
