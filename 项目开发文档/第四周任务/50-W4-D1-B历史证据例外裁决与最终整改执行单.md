# 50 W4-D1-B历史证据例外裁决与最终整改执行单

> 签发日期：2026-08-13  
> 适用岗位：B  
> 适用日次：W4-D1  
> 适用交付：`W4-D1-B-3`及其下一版完整整改候选  
> 合同基线：`W4-C2 / W4-R1`  
> 仓库基线：`00037c1aa995a0ec2aec70b097fc680e193ed08a`  
> A正式上游：`4c52eb7c78fa80007aa9a8ab4e00768d71d3f3f5`  
> 普通传输包：`W4-D1-B-2-transport-v5.zip`  
> 普通传输包SHA-256：`ba971f0d7240007ba27539d3ff4e0bc834e673edbc6c264d7f585a94d4bc9a7a`  
> 负责人审计包：`W4-D1-B-2-owner-audit-v5.zip`  
> 负责人审计包SHA-256：`c37430cf5bc942918dc80994306fe714e4cf7f2ef30ddaa55e53c5e6d1573b0a`

## 1. 文档性质与当前结论

本文同时承担两项职责：

1. 对B首次Python 3.14.4运行原始证据灭失问题作出负责人限定裁决；
2. 将`W4-D1-B-3`独立审计中仍未闭合的问题转换为一次性最终整改执行单。

本文不修改41号、42号或44号合同，不放宽合同运行环境，不构成`commit`、`push`、上传锁、Profile激活、D开工授权或W4 GO。

当前状态固定为：

```text
B-R2-01=OWNER_EVIDENCE_EXCEPTION_APPROVED
B-R2-02=CONFIRMED_CLOSED
B-R3-01=CONFIRMED_BLOCKER
B-R3-02=CONFIRMED_REQUIRED
AUDIT_RESULT=EVIDENCE_REMEDIATION_REQUIRED
UPLOAD_LOCK=NOT_GRANTED
NOT_COMMITTED / NOT_PUSHED
```

B必须在现有候选上做最小证据整改并重新交付完整W4-D1-B候选，不得把本文件理解为上传授权。

## 2. 已确认通过且必须保持不变

以下事实已由负责人独立复验，除非本执行单明确要求，不得重新设计或改写：

1. revision 3候选仍包含79个文件、78个seal entries。
2. 六个核心知识点、六张固定卡片和六个group-MCQ活动闭合。
3. 公开题共30道；每点一个4题fixed组和一个1题supplemental组。
4. 公开题面与私有答案顺序一致，`questionId`不重复，来源锚点闭合，公开资产未泄漏答案、解释、Rubric、hidden tests、reference solutions或私有CSV标识。
5. `goal.requiredActivityIds`只含最终综合实操；六个核心点使用`all_in_order`；卡片没有写入`activityIds`。
6. revision 2共71个文件，树哈希保持为：

   ```text
   2a4538272cc47a3451b434999d620f429e5deaa0eb0f2c3f95fa76e53d80786d
   ```

7. B-R2-02核心整改已通过：
   - `w4-b-asset-inventory.json`排除`quality/revision-seal.json`和inventory自身；
   - inventory当前77项逐文件SHA-256和`byteLength`全部匹配；
   - 六点coverage matrix已登记私有答案映射、来源和资产字段；
   - B资产验证退出码为0；
   - 正式A Schema与seal入口独立复验通过。
8. 当前候选seal为：

   ```text
   entries=78
   assetTreeSha256=e118fd65c4583821f686cba4faab5990a81d2149a8f73cf89af2c376ba15b352
   ```

9. 上述当前seal只在Profile目录字节不变时继续有效。本轮若只修改`pi-study-helper/scripts/w4-b-validation/`、第四周handoff和仓库外传输材料，不得无故重写Profile或seal。

## 3. 负责人裁决：B-R2-01首次历史证据例外

### 3.1 已确认历史事实

B首次使用错误环境执行聚合测试时，保留下列摘要：

- Python：`3.14.4`
- Pandas：`3.0.5`
- 全量：`670 passed / 20 failed / 1 skipped`
- `npm run verify`：退出码1
- 失败归因：批准运行环境不匹配

下列首次原始材料已经客观灭失：

```text
原始首次stdout/stderr=NOT_AVAILABLE
原始首次开始/结束时间=NOT_AVAILABLE
原始首次日志SHA-256=NOT_AVAILABLE
缺失原因=首次证据留存不足
```

这些字段不得倒填、推算或使用后续复现记录冒充。

### 3.2 正式裁决

负责人批准：

```text
OWNER_EVIDENCE_EXCEPTION_APPROVED
```

允许以下组合关闭B-R2-01：

1. 明确保留的首次运行摘要和缺失字段；
2. 明确标记为`REPRODUCTION_ONLY_NOT_HISTORICAL_ORIGINAL`的Python 3.14.4/Pandas 3.0.5后续复现实测；
3. B本人机器上Node v22.23.1、Python 3.13.7、Pandas 3.0.5合同环境的最终复验。

后续不再要求补交已经灭失的首次时间戳、原始日志和SHA-256。该缺口不属于延期债务，不作为D/C/E输入，也不允许被解释为环境合同放宽。

### 3.3 B必须同步修改

1. `scripts/w4-b-validation/command-results.json`中的`unresolvedEvidenceBlocker`必须改为已裁决状态，并引用本文档及裁决值。
2. 首次阶段继续保留`null/NOT_AVAILABLE`，不得填入后续复现的时间或哈希。
3. 后续复现阶段继续明确标记为非首次原件。
4. `handoff-w4-b-d1.md`必须说明D/C/E只消费最终合同环境结果、正式提交哈希和最终seal，不消费Python 3.14.4历史记录。
5. `b-r2-01-stop-report.md`作为历史阻塞报告保留，不得改写成“从未阻塞”；由本文档关闭其待裁决状态。

## 4. 必须一次性整改的问题

### 4.1 B-R3-01：V2-6通过声明没有对应运行记录

【级别】`CONFIRMED_BLOCKER`

【合同依据】

41号第7.1节要求结构化证据记录实际命令、工作目录、开始/结束时间、退出码、项数、摘要和stdout/stderr SHA-256；44号要求申请上传锁前运行适用的全量和受影响回归。既有负责人裁决要求V2-6外层wrapper超时时保留非零退出码，并另跑direct与isolated完成归因。

【证据位置】

`W4-D1-B-3`负责人包中的：

```text
b-validation/command-results.json
b-validation/build-formal-command-results.mjs
handoff-w4-b-d1.md
```

【实际结果】

1. `command-results.json`中的`directAndIsolatedRecords`为空数组；
2. 同一对象的`conclusion`却声称revision 2 direct 20+60和isolated均退出0；
3. handoff重复了相同的无记录PASS声明；
4. `build-formal-command-results.mjs`将V2-6记录读取失败静默视为可选，却仍无条件输出PASS结论。

【根因】

证据生成器没有把“记录存在且字段完整”作为输出PASS结论的前置条件，导致缺少原始记录时仍生成肯定结论。

【预期结果】

任何V2-6 direct/isolated PASS文字必须由同一正式结构化结果中的非空真实记录支撑；记录缺失或命令非零时不得生成PASS。

【必须修改】

1. 在B本人机器、合同环境和最终候选上实际运行revision 2 V2-6直接复算：

   ```powershell
   node scripts/w2-verification/v2-6-preconditions.mjs `
     --development ../evaluation/personas/development-20.jsonl `
     --final ../evaluation/personas/final-60.jsonl `
     --profile fixtures/profiles/pandas-cleaning-v2-draft
   ```

2. 实际运行V2-6独立测试：

   ```powershell
   npm test -- --maxWorkers=1 scripts/w2-verification/v2-6-preconditions.test.mjs
   ```

3. 两条记录必须写入正式`command-results.json`，至少包含：
   - 精确命令；
   - 绝对`cwd`；
   - `startedAt`和`endedAt`；
   - 退出码；
   - direct的development 20例、final 60例及输入SHA-256；
   - isolated的测试文件数、通过/失败/跳过数；
   - stdout/stderr SHA-256和安全摘要；
   - 明确的revision 2 Profile路径。
4. `build-formal-command-results.mjs`不得再把这些记录作为“缺失也继续”的可选数据。缺少任一记录、字段不完整、Profile不是revision 2或退出码非0时：
   - 不生成direct/isolated PASS结论；
   - 最终证据状态必须为非通过；
   - 脚本自身以非0退出或明确`BLOCKED`结束。
5. 删除无真实记录支撑的`INPUT_REVISION_MISMATCH`说明，除非包内确实保留了对应实际运行记录；不得只保留文字结论。
6. handoff必须从最终结构化记录投影实际结论，不得硬编码PASS。

【强制反例】

至少机械验证以下情况：

1. 删除direct记录后重新构建正式结果，不得出现“direct exits 0”文字，构建结果必须非通过；
2. 删除isolated记录后同样不得出现PASS；
3. 把direct的Profile参数改为revision 3时，必须被识别为输入错误，不得登记为revision 2 PASS；
4. 任一记录退出码改为非0时，handoff和最终结论不得仍写PASS；
5. 合法revision 2 direct记录必须证明20+60案例，isolated记录必须证明对应测试实际通过。

【必须保持】

聚合`npm test -- --maxWorkers=1`若出现冻结V2-6外层30秒wrapper超时，必须保留该轮真实退出码1和`689 passed / 1 failed / 1 skipped`，不得被另一轮`verify`的690通过覆盖或改写为聚合PASS。

### 4.2 B-R3-02：交付清单声明双sidecar但实际未交付

【级别】`CONFIRMED_REQUIRED`

【合同依据】

41号第7节和团队整改提示词规范第9节要求包外SHA-256、sidecar、Manifest和交付清单同步一致。

【证据位置】

`W4-D1-B-2-delivery-manifest-v5.json`声明：

```text
W4-D1-B-2-transport-v5-SHA256.txt
W4-D1-B-2-owner-audit-v5-SHA256.txt
```

【实际结果】

负责人收到的`W4-D1-B-3`顶层只有两个ZIP、delivery manifest和stop report；两个外置sidecar均不存在，ZIP内部也不存在这两个文件。

【预期结果】

delivery manifest中声明的每个sidecar必须作为真实外置文件与对应ZIP同目录交付，并与负责人独立实算哈希一致。

【必须修改】

1. 最终重新打包完成后，分别计算普通传输ZIP和负责人完整审计ZIP的SHA-256。
2. 为两个ZIP分别生成外置sidecar，文件名与delivery manifest完全一致。
3. sidecar每个只绑定对应ZIP的最终文件名和最终SHA-256。
4. delivery manifest同步记录每个ZIP的最终`byteLength`、SHA-256和sidecar文件名。
5. 若B-R3-01整改后重新生成ZIP，v5旧哈希立即失效，不得沿用本文件头部或旧manifest中的哈希。
6. ZIP不得包含自己的sidecar，也不得把sidecar列入正式Git拟提交清单；sidecar是仓库外传输证据。

【强制反例】

1. sidecar缺失时，交付自检必须失败；
2. sidecar中的文件名与实际ZIP不一致时必须失败；
3. 修改ZIP任一字节后旧sidecar校验必须失败；
4. delivery manifest、sidecar和`Get-FileHash`三者不一致时不得形成最终交付包。

## 5. 横向闭合要求

B完成两项整改后，必须同时保证：

1. `command-results.json`、handoff、问题映射表、scope audit和delivery manifest对环境、测试结果、阻塞状态和文件范围的表述一致。
2. B-R2-01状态统一为`OWNER_EVIDENCE_EXCEPTION_APPROVED`，不得继续写`OWNER_DECISION_OR_ORIGINAL_LOG_ARCHIVE_REQUIRED`。
3. 最终合同环境阶段不能仅因V2-6已知wrapper超时笼统写`FAILED`而不归因，也不能写成全量PASS；应同时保留：
   - 聚合命令的真实非零结果；
   - direct的真实结果；
   - isolated的真实结果；
   - 另一轮`verify`的真实结果。
4. 拟提交逐文件Manifest必须继续覆盖：
   - `pi-study-helper/fixtures/profiles/pandas-cleaning-revision-3-draft/`；
   - `pi-study-helper/scripts/w4-b-validation/`；
   - `新版设计文档-重写版/第四周任务/handoff-w4-b-d1.md`。
5. 拟提交Manifest中的中文handoff路径必须以UTF-8 JSON保存，并由B在自己的仓库根按该相对路径实际解析、复算哈希；不得依据乱码控制台显示修改真实中文路径。
6. 正式Git拟提交清单不得包含ZIP、sidecar、原始完整stdout/stderr、B虚拟环境、A编译运行时、`node_modules`、`.demo-data`或历史审计目录。
7. 如果本轮没有修改Profile目录内任何字节：
   - 保持当前79文件及seal `e118fd65...b352`不变；
   - 重新验证seal，但不得重新生成内容制造无关哈希漂移。
8. 如果确实修改了任何Profile字节：
   - 必须说明修改原因和授权条款；
   - 重新生成inventory、coverage、完整文件Manifest和seal；
   - 使用正式A入口重新验证；
   - 所有交付哈希随之更新。

## 6. 最终合同环境封板复验

B必须在本人电脑建立或复用仓库外独立环境，不得引用负责人电脑路径。封板环境必须实际输出：

```text
Node v22.23.1
Python 3.13.7
Pandas 3.0.5
```

完成B-R3-01和B-R3-02后，针对最终待打包字节至少运行并如实记录：

1. `node.exe --version`；
2. `python.exe -c "import sys,pandas; print(sys.executable); print(sys.version.split()[0]); print(pandas.__version__)"`；
3. B完整资产验证脚本；
4. A正式Schema/seal入口验证；
5. 五个公开Python测试文件；
6. `tests/profile-revision-3-activation.test.ts`；
7. `tests/w4-contracts.test.ts`；
8. `tests/python-process-evaluation.test.ts`；
9. `tests/python-process-evaluation-r2.test.ts`；
10. `npm run typecheck`；
11. `npm run check:docs`；
12. `npm run smoke:extension`；
13. `npm test -- --maxWorkers=1`；
14. V2-6 revision 2 direct 20+60复算；
15. V2-6 isolated测试；
16. `npm run verify`；
17. `git diff --check`。

每条命令必须记录实际命令、`cwd`、开始/结束时间、退出码、适用测试统计、stdout/stderr SHA-256和安全摘要。不得预填测试数、退出码或新哈希。

若出现以下情况，应按真实事实处理：

- 全量只因V2-6外层30秒wrapper超时非零：保留非零，继续执行direct/isolated，不改W2预算；
- Windows瞬时文件占用：隔离重跑并保存前后两次证据，不把首次失败删除；
- 任何B资产、A Schema/seal、Python评测器、direct或isolated逻辑失败：立即停止，不得申请上传锁；
- 环境版本不匹配：修正B本人独立环境后重新运行，不修改环境锁。

## 7. 最终完整交付包

B必须重新交付一个完整、可独立复验的W4-D1-B候选目录，不得只发送增量说明。顶层至少包含：

1. 最终普通传输ZIP；
2. 普通传输ZIP外置SHA-256 sidecar；
3. 最终负责人完整审计ZIP；
4. 负责人完整审计ZIP外置SHA-256 sidecar；
5. 最终delivery manifest；
6. B-R3-01、B-R3-02闭合报告。

负责人完整审计ZIP至少包含：

1. 完整79文件revision 3候选，包括负责人可读私有正文；
2. revision 2只读基线或足以逐文件复核的不变证据；
3. 最终B验证脚本；
4. 唯一正式`command-results.json`；
5. 非空且字段完整的V2-6 direct/isolated记录；
6. B-R2-01历史摘要、后续复现和本文裁决引用；
7. B-R2/B-R3问题到文件和测试的映射表；
8. 最终handoff；
9. 拟提交94项或按最终实际文件数生成的逐文件Manifest；
10. scope audit；
11. A Schema/seal验证结果；
12. 负责人独立运行所需的相对路径说明。

普通传输ZIP继续排除私有答案正文、hidden tests、Rubric、reference solutions和私有CSV正文，但必须携带能与负责人完整包对应的完整哈希清单和安全审计入口说明。

最终交付消息必须报告：

```text
合同版本
实际HEAD / origin/main
A正式上游及祖先检查
候选Profile文件数
revision 2树哈希
最终seal entries与assetTreeSha256
普通ZIP文件名、byteLength、SHA-256及sidecar
负责人ZIP文件名、byteLength、SHA-256及sidecar
最终环境实际版本
各门禁实际退出码和统计
拟提交文件数和精确路径范围
NOT_COMMITTED / NOT_PUSHED / uploadLock=NOT_REQUESTED
```

## 8. 禁止事项

1. 不修改41号、42号、44号、48号、49号或本文裁决。
2. 不修改revision 2、W2输入、V2-6判据或30秒wrapper预算。
3. 不修改六点卡片、题目、答案、来源和冻结代码评测资产来制造证据PASS。
4. 不修改`src/`、A/C/D/E文件、SDK、依赖、`package.json`或锁文件。
5. 不删除首次Python 3.14.4失败摘要，不倒填灭失证据，不把后续复现冒充首次原件。
6. 不用另一轮`verify` PASS覆盖聚合wrapper超时事实。
7. 不把ZIP、sidecar、原始日志、虚拟环境、A运行时或审计目录列入正式Git提交。
8. 不清理、回退、覆盖或暂存负责人及其他成员的本地材料。
9. 未获明确上传锁前不得`commit`或`push`。

## 9. 停止条件

出现以下任一情况，B必须停止对应工作并提交新的精确阻塞报告：

1. revision 2 direct 20+60或isolated测试在正确输入与合同环境下非0；
2. A正式Schema或seal入口无法接受最终候选；
3. revision 2或冻结代码评测资产出现字节变化；
4. 必须修改A/C/D/E文件、公共合同、SDK、依赖或锁文件才能继续；
5. 最终seal无法由正式A入口复算一致；
6. 传输包、sidecar和delivery manifest无法形成三方一致哈希；
7. 发现合同没有答案且不同方案会改变跨岗位语义。

阻塞报告必须列出问题编号、冲突条款、实际文件、实际命令和退出码、影响范围及最小候选方案。不得把可以按本文直接修复的证据缺失再次上报为负责人裁决问题。

完成本执行单只代表形成新的审计候选。负责人独立复验通过并另行授予上传锁后，B才可按批准清单提交和推送。
