# 27 第二周总任务布置与权限边界

状态：`[负责人正式签发]`。执行周期：2026-07-27至2026-08-02，共7天。

现行合同身份统一为`W2-C2/W2-R5`，现行负责人裁决覆盖D01—D33；D33补充裁决标识为`W2-V2-3-ENV-1`，D7收口补充标识为`W2-D7-CLOSEOUT-1`，均不升级`W2-R5`。本文D1行中的`W2-C2/W2-R4`和D22—D31只记录D1当时已经签发的历史版本；D32签发后的D2—D7执行统一以`W2-C2/W2-R5`、D33审计补充和本批D7收口补充为准。

决策生效边界：D01—D14是第一周岗位任务的验收依据；D15—D33均从本第二周任务包或追加裁决签发后生效。D15—D33不得追溯用于判定A、B、C、D、E的第一周交付存在缺陷，也不得要求成员重写第一周交接单。第一周产物与新决策之间的差异统一登记为第二周迁移、约束或后续周责任。

第二周不可变周起点固定为`W2_START_COMMIT = f343a6c1c630f362f4686e6f6b0f50c6577d5562`。D1身份检查已经在该提交完成；它不等于D2-D3持续开发的工作HEAD，也不得被`50834a7`或其他后续提交替换。负责人本次合同勘误进入`main`后，A必须在开始或继续D2-D3实现前拉取最新`origin/main`，并保证实际工作HEAD以固定周起点为祖先。

## 1. 第二周目标

本周把第一周的公共骨架和Pandas候选资产推进为可加载、可校验、可复算的确定性事实底座：完成Profile revision 2迁移、固定诊断、Evidence与KnowledgeState、会话持久化事务骨架、唯一清洗结果、案例输入和首批20个gold双标。

本周不实现PathEngine，不调用`buildPath`，不统计路径合法率，不接真实模型主链，不开发React页面，也不提前启动第三周任务。

## 2. 权威文档与阅读顺序

所有成员按以下顺序阅读：

1. [20-第一周开发前负责人决策冻结清单](../第一周任务/20-第一周开发前负责人决策冻结清单.md)：D01-D33及`W2-D7-CLOSEOUT-1`负责人决策；
2. [21-第一周公共合同总册](../第一周任务/21-第一周公共合同总册.md)：跨周公共类型、Facade、仓储和错误码；
3. [28-第二周公共合同总册](./28-第二周公共合同总册.md)：本周新增Schema、事务、迁移、案例和gold合同；
4. [06-六周MVP范围与验收](../06-六周MVP范围与验收.md)：W2范围、V2-1至V2-8和Go/No-Go；
5. 本文与本人岗位任务书；
6. 05、07、11号专项文档仅用于对应岗位的专业细节。


## 3. 统一红线

- 成员不得修改20、21、27、28号或其他总规范；合同缺口必须提交问题单，由负责人书面裁决并更新权威文档。
- 不得自行增加公共字段、错误码、Facade方法、Profile核心字段、评分规则、Evidence写入口或依赖。
- A以外岗位不得修改A的公共类型、诊断/学情逻辑和事务仓储；B以外岗位不得修改Pandas资产正文；C不得替B修数据；D不得接真实模型；E不得替其他岗位修实现。
- 诊断答案、私有CSV路径、参考实现、隐藏测试、密钥、完整学习者代码不得进入安全DTO、前端、普通日志、模型上下文或gold文件。
- `ActivityRepository`、Agent和评测器不得公开正式Evidence；本周只有`LearningSessionRepository.commit`可以原子公开诊断Evidence和KnowledgeState。
- 不运行`npm audit fix`，不修改`allowScripts`、依赖版本、SDK哈希、`package.json`或`package-lock.json`。
- B和E在首批20例gold独立标注完成前，不得查看系统路径或正式系统输出；两份原始标注不得互相覆盖。
- 本周任何提前完成时间只用于补测试、审计和文档，不得转做W3功能。
- 第一周实现与D15—D33不一致时，记录为W2迁移项或后续周待办，不写成W1缺陷；本周未授权实现的功能继续按06号排期执行。

## 4. 文件所有权

| 岗位 | 本周可修改范围 | 对其他范围的权限 |
|---|---|---|
| 负责人 | 20、21、27、28号及必要的权威文档同步；`evaluation/golden/gold-input-freeze.json`、`evaluation/golden/difficulty-gold.jsonl`、`evaluation/golden/path-constraints.jsonl`、`evaluation/golden/adjudication-log.jsonl`；周门禁最终签署 | 裁决、复核；不代替成员实现 |
| A | `src/domain/`、`src/application/`、`src/repositories/learning-session-repository.ts`内本周诊断/学情/事务实现，A作者测试和非Pandas smoke fixture | 只读审计B资产和D提示词；不得改B/C/D/E文件 |
| B | `fixtures/profiles/pandas-cleaning-v2-draft/`、其中`quality/run-candidate-evidence.py`维护/复算脚本、`evaluation/personas/`、`evaluation/golden/annotations/b-first-20.jsonl`及B作者测试/资产报告 | 只读消费公共Schema；不得改A代码和C报告 |
| C | `pi-study-helper/scripts/w2-data-validation/`内的数据反向验证脚本、C作者测试和C验证报告 | 只读运行B资产；不得直接修B资产或修改评测公共协议 |
| D | `pi-study-helper/fixtures/model-prompts/w2/`提示词草稿、`pi-study-helper/fixtures/model-responses/w2/`脱敏录制响应和D配置说明 | 只读消费合同；不得修改模型运行端口或接真实模型 |
| E | `pi-study-helper/scripts/w2-verification/`、`pi-study-helper/scripts/evaluation-metrics.mjs`、`evaluation/claims/claim-split-template.md`、`evaluation/golden/annotations/e-first-20.jsonl`和`周门禁记录/W2-验证记录.md`的验证正文 | 只读复验全仓；不得修其他岗位产物；D6上传后将验证记录最终签署段的编辑权移交负责人 |

新增文件的确切路径必须在交接单列明。两个岗位需要修改同一文件时先停工，由负责人重新指定唯一所有者。

## 5. 单仓库上传规则

继续使用一个仓库、默认分支、无个人分支和一把上传锁。正常上传分布固定为D2无上传、D3 A、D4 D、D5 B→C；D6只有双封存和盲标资格检查通过时才按B→E上传，失败时原始标注不得入Git。B为保证B/E原始gold互不可见，在条件通过时分成D5资产上传和D6原始标注上传两次，这是唯一预先批准的组员例外。负责人固定在D1上传任务包、D7上传冻结记录、正式gold与门禁签署。所有上传严格串行。

```text
申请上传锁
→ 负责人确认当前持锁人为“无”
→ git status确认只含本人范围
→ 【拉取】默认分支最新提交
→ 检查同路径冲突
→ 重跑本人最小测试和受影响回归
→ 只暂存本人交付
→ commit并【上传】
→ 报告提交号、测试和文件清单
→ 负责人确认后释放上传锁
```

拉取到新提交后必须重新测试。发现同文件冲突、公共合同冲突或远端变化时停止上传，不得强推、覆盖或删除他人文件。

## 6. 七天依赖顺序

总依赖固定为：

```text
负责人签发任务包
→ E复验统一基线
→ D2五岗并行准备，D3 A完成诊断、学情和事务仓储并上传
→ D4并行推进：B候选→A/C/E审计→C完整V2-3→负责人冻结gold输入；D材料→A/E审计→D上传
→ D5 B上传冻结资产，C用内容哈希绑定Git提交并上传报告
→ D6 B/E基于同一冻结输入封存首标并接受盲标资格检查，E完成V2-1至V2-7；V2-8按检查结果进入待终裁或阻塞
→ 负责人终裁分歧、冻结哈希并Go/No-Go
```

| 天 | 严格先后顺序 | 主上传 | 负责人 | A | B | C | D | E | 当日门禁 |
|---|---|---|---|---|---|---|---|---|---|
| D1 启动与基线 | ①负责人`【上传】`最终任务包并登记固定`W2_START_COMMIT = f343a6c1c630f362f4686e6f6b0f50c6577d5562` → ②全员`【拉取】` → ③E确认当时HEAD及祖先关系并复验 → ④负责人书面关闭W1遗留门禁并确认开工 | 负责人 | 当时签发D22-D31、`W2-C2/W2-R4`和任务书（D1历史版本；后续执行已由D32及`W2-R5`覆盖）；登记起始提交；确认上传锁、文件所有权；逐项关闭W1遗留门禁 | 阅读合同、列类型迁移、持久化和测试清单，不上传 | 清点revision 1迁移范围，不上传 | 准备数据反向验证矩阵，不上传 | 准备离线交付目录，不上传 | 在固定周起点执行V2-1；开始准备V2脚本、claim模板、指标骨架和V2-7扫描 | 当时HEAD等于固定周起点、`8785c6e`是其祖先、V2-1通过且W1门禁书面关闭；否则W2业务实现NO-GO |
| D2 五岗并行生产 | A—E分别在授权目录实现或准备；相互提供只读候选，不上传 | 无 | 处理正式问题单；确认D4冻结manifest字段、审计人和截止点 | 开发完整类型、KnowledgeState、诊断和事务仓储主体 | 完成Profile结构、正式ID、来源、讲义和诊断主体 | 完成V2-3工具、三次运行和错误实现检查框架 | 完成D15配置和四角色提示词主体 | 完成V2脚本主体、V2-7 canary、claim模板和指标骨架 | 五岗候选可在D3收口；不产生正式PASS |
| D3 候选收口与A上传 | ①E滚动预检A的V2-4/V2-5 → ②A修复本人阻塞 → ③A全量自检 → ④D3晚间A`【拉取】`并`【上传】` | A | 预审来源、许可证、候选清单和gold冻结范围 | 完成诊断、Evidence、KnowledgeState、事务仓储和smoke；吸收E意见后上传 | 完成数据、参考结果、错误实现、维护脚本及20+60案例输入；不上传 | 在B滚动候选上预跑并修正自己的工具；只记预检 | 完成脱敏响应和fallback候选，接受A/E审计；不上传 | 完成A预检；预跑V2-2/V2-6/V2-7；冻结工具版本而非正式结论 | A正式提交进入`main`；其他候选具备D4冻结条件 |
| D4 正式候选、V2-3与gold冻结 | ①全员`【拉取】`A → ②并行执行gold链“B生成冻结候选→A/C/E审计→C完整V2-3→负责人冻结→B/E开始标注”和D材料链“D完成候选→A/E审计→D`【上传】`” | D | gold链核验A提交号、规范化内容哈希和审计结论并生成`gold-input-freeze.json`；D材料链只核验自身审计 | 审计Profile/诊断Schema和D权威事实边界；候选变化后复验受影响项 | 生成冻结候选；只修本人资产；每次修改均重建manifest和资产树哈希 | 完整执行V2-3；候选变化后重新确认 | 拉取A提交，通过A/E审计后上传；不等待C或gold冻结 | 对gold链执行V2-2/V2-6及候选V2-7预检，并独立审计D材料；冻结前不得宣布gold链正式PASS | 18:00前gold链审计、完整V2-3和冻结完成才可启动标注；gold链失败不阻塞已通过A/E审计的D上传 |
| D5 正式资产与报告绑定 | ①B确认内容与冻结候选一致并`【上传】` → ②C`【拉取】`B → ③C按`W2-V2-3-ENV-1`预检环境，再核对路径、逐文件/资产树/`final-60`哈希并最小复现 → ④C`【上传】`正式报告 → ⑤负责人确认Git提交绑定冻结候选 | B后C | 确认Git内容哈希和C环境预检；不将候选批准写成active | 只读核对Schema和C报告 | 上传与冻结候选逐文件一致的资产、案例、维护脚本和作者测试；继续独立首标 | `pandas==3.0.5`且哈希一致时绑定D4完整V2-3；环境不符时记`environment_mismatch/BLOCKED`，内容不一致时阻止旧报告并要求重新冻结 | 只读响应离线材料复现问题 | 补检D材料的V2-7；保持工具冻结；继续独立二标 | Git资产与冻结候选一致，V2-3在临时审计基线下绑定正式提交；否则停止对应验证并按错误类别处理 |
| D6 原始标注封存与首次完整复验 | ①中午前B/E分别封存并登记SHA-256 → ②负责人检查20例覆盖、Schema、冻结输入绑定、哈希和独立性 → ③仅检查通过时B`【上传】`B原始标注 → ④E确认自身哈希未变后`【拉取】`B并首次完整运行V2 → ⑤E`【上传】`E原始标注、脚本/模板和验证正文；检查失败则禁止原始标注入Git，E只执行不依赖双标的V2-1至V2-7并记录阻塞 | 条件通过时B后E；失败时E仅可上传不含原始标注的阻塞证据 | 核对双封存和盲标资格；失败时将V2-8记为`BLOCKED`且不签GO | 只读复核可执行结果 | 仅资格检查通过后上传B首标，不查看E标注 | 只读响应V2-3复现问题 | 只读响应离线材料复现问题 | 检查通过后才拉取B并运行最终完整V2；失败时不得查看B，只保留V2-1至V2-7结果 | V2-1至V2-7=`PASS/BLOCKED`；V2-8按条件=`PENDING_OWNER_ADJUDICATION/BLOCKED` |
| D7 阻塞修复与负责人签署 | ①阻塞文件所有者按负责人指定顺序条件`【拉取】/【上传】`修复 → ②E`【拉取】`后只复验受影响项，必要时条件`【上传】`更新验证正文 → ③负责人`【拉取】`最终提交 → ④负责人终裁gold并生成三份正式gold文件 → ⑤负责人补写门禁最终签署段 → ⑥负责人取得最终上传锁并`【上传】` | 负责人；阻塞时文件所有者和E条件上传 | 终裁gold分歧，登记原始/正式哈希，批准来源/数据/诊断/draft状态，生成正式gold，签署并上传Go/No-Go | 仅被点名时修A范围 | 仅被点名时修B范围 | 仅被点名时修C范围 | 仅被点名时修D范围 | 不重复第一次全量验收；只复验受影响项并移交验证记录最终签署段 | 修复后受影响项全绿且负责人最终签署已入库；否则NO-GO，不提前做W3 |

同一天存在多个主上传时段时，表中箭头就是锁顺序；后一人必须拉取前一人的提交。D4的gold链和D材料链是并行依赖，不构成上传锁先后关系；当天只有D正常上传。E只有在自身原始标注已封存、双方盲标资格检查通过且负责人书面放行后，才能在D6拉取含B原始标注的提交。检查失败时B/E原始标注都不得入Git，E可以上传不含原始标注的工具和`BLOCKED`验证记录。条件修复永远插在E受影响项复验之前。E完成D6验证记录上传后不得再编辑最终签署段；负责人D7只追加终裁、正式哈希和Go/No-Go，不改写E的命令与原始结果。

E在D3登记验证工具基线文件清单和SHA-256。D4以后只有可复现的工具缺陷允许受控修改，必须登记缺陷证据、修复前后哈希、受影响V2项和复验命令，不得降低断言或改写预期结果换取PASS；D6才发现的工具缺陷将对应项记为`BLOCKED`并转D7条件修复。

> **2026-08-03执行勘误（`W2-GOLD-AUTH-CORR-1`）**：D4行的18:00门禁继续作为默认规则。本批次A/C/E审计、完整V2-3和负责人冻结在D4晚间全部完成后，负责人以`2026-08-01T19:37:30.5739587+08:00`为准确生效时间书面批准B/E立即开始独立标注；原批准文本中的“D5生效”是时间误写。该一次性例外只允许开始标注，不允许原始标注在D6资格检查前入Git，也不改变B→E上传锁顺序或V2-8状态条件。

D4冻结候选固定使用以下名称，均为仓库外审计载体；负责人维护的冻结记录在D7与正式gold一并入库：

```text
B-W2-D4-freeze-candidate-<W2_START_SHORT>.zip
B-W2-D4-freeze-candidate-<W2_START_SHORT>.zip.sha256
B-W2-D4-freeze-candidate-<W2_START_SHORT>.manifest.json
evaluation/golden/gold-input-freeze.json
```

正式一致性按28号`W2-R5`规范化算法计算，以规范化相对路径、逐文件SHA-256、资产树SHA-256和`final-60.jsonl` SHA-256为准，不只比较可能受压缩时间戳影响的ZIP哈希。冻结记录还必须绑定A正式提交号、Profile/scoring版本、D4候选V2-3报告SHA-256及诊断/KnowledgeState前置摘要SHA-256；冻结记录自身SHA-256由D7门禁记录旁路登记。冻结清单内任一文件变化时，立即停止标注，重建manifest和资产树哈希，由A/C/E复验受影响项、C重新确认V2-3、负责人重新冻结；受影响标注作废重做。

D33只补齐C验证工具的可复现环境和错误归因：第二周V2-3先确认`pandas==3.0.5`，不匹配时在资产判断前返回`environment_mismatch/BLOCKED`，不得将环境差异写成B资产缺陷。C修正本人目录内工具、测试、报告和交接清单不改变B冻结67项、资产树、`final-60`或gold输入；只有B冻结内容实际变化才触发重新冻结。该临时审计值不写入Profile最终`environment-lock.json`。

`W2-D7-CLOSEOUT-1`只处理本批D7：B现有独立性声明由负责人结合`W2-GOLD-AUTH-CORR-1`登记后沿用；E补交原V2-7机器原件，并在仓库外构造普通日志和安全导出测试视图，连同safe DTO、D录制响应及C/D正式报告完成五类扫描；负责人替代C核对E新工具未降低V2-3标准；E精确开始时间不作为本批资格阻塞；资格通过后E只生成机械差异清单，负责人跳过协商直接终裁。以上均不改变原始标注、冻结输入或第三周规则。

E的D7修复不再从旧ZIP或聊天摘要自行拼接规则，唯一操作入口为 [E岗位W2-D7-CLOSEOUT-1一次性整改执行单](./E岗位W2-D7-CLOSEOUT-1一次性整改执行单.md)。E必须先拉取最新`origin/main`并确认`e7136b320f2ce28edc0185dcf52c9d0e67288f7c`为实际HEAD祖先；safe DTO、D录制响应、C报告和D报告均从正式Git读取，ordinary log和safe export由E按28号固定结构构造，不存在等待其他岗位提供的前置依赖。最终申请上传锁前必须完成六输入V2-7、负责人签署后的20例机械差异、七件工具逐文件哈希和最新HEAD的`verify`。

## 7. 审计关系

| 产物 | 作者 | 必须审计 | 通过标准 |
|---|---|---|---|
| 诊断、学情、事务仓储 | A | E执行，A复核 | V2-4、V2-5；幂等、恢复、原子性和越权边界通过 |
| Profile revision 2 | B | E执行，A复核 | V2-2；严格Schema、引用闭合、revision和坏包拒绝通过 |
| 数据和唯一`clean_df` | B | C执行，E复核记录 | V2-3；每份3次一致，错误实现全部被识别 |
| 案例前置数据 | A+B | E执行，A复核 | V2-6；不得调用PathEngine |
| 私有资产隔离 | A+B+D原产物；E按`W2-D7-CLOSEOUT-1`构造仓库外测试视图 | E执行，负责人复核 | V2-7；本批固定六个输入覆盖安全DTO、普通日志、录制响应、安全导出和C/D验证报告，逐输入六类敏感canary零命中；E验证记录不递归扫描自身 |
| 首批20个gold | B、E独立原始标注；负责人生成正式文件 | 负责人终裁 | V2-8；原始双标保留，分歧写入`adjudication-log.jsonl`，正式`difficulty-gold.jsonl`与`path-constraints.jsonl`有冻结哈希 |
| D离线准备 | D | A权限审计、E脱敏审计 | 参数与D15一致，不接真实模型、不写权威事实 |
| claim拆分与指标脚本框架 | E | 负责人复核 | 只定义字段、分子分母和排除结构，不读取正式输出、不生成正式效果结论 |

上述审计只判断第二周新增产物是否符合D15—D33。A—E第一周交付继续按当时19—26号任务书验收，不因本表新增要求被追溯判为阻塞。

## 8. 负责人批准事项

负责人本周必须书面批准：W1遗留门禁关闭、来源的候选资格、公开/私有数据和唯一`clean_df`、诊断题与答案、revision 2继续保持`draft`、gold输入资产树和60案例输入哈希、首批20个gold分歧与原始/正式哈希、W2 Go/No-Go。D4/D5批准只表示候选可继续验证，不构成`draft→active`批准；本周Profile不得设为`active`。

口头或群消息决定必须当日补问题单和书面结论，否则不生效。组员不得代替负责人批准，也不得把“审计PASS”写成“已active”。

## 9. 统一交付格式

每次申请上传锁必须包含：固定`W2_START_COMMIT = f343a6c1c630f362f4686e6f6b0f50c6577d5562`、拉取最新`origin/main`后的实际工作HEAD、合同版本`W2-C2/W2-R5`、适用的补充裁决标识、文件清单、作者测试、全量回归、已知限制、只读审计结论、未提交/未推送声明。C本次还必须报告`W2-V2-3-ENV-1`及实际Python/Pandas/平台；E的D7申请还必须报告`W2-D7-CLOSEOUT-1`、V2-7仓库外证据哈希和负责人不降标确认。实际HEAD必须以固定周起点为祖先；上传后必须补充正式提交号和锁释放时间。

过程截图、聊天记录、重复回执和过期整改稿不进入正式仓库。每岗只保留一份最终交接清单；V2结论统一归档到`新版设计文档-重写版/周门禁记录/W2-验证记录.md`。

## 10. 第二周完成定义

- V2-1至V2-8全部通过并记录真实命令、项数、执行人和复核人；
- Profile revision 2以`draft`候选从干净目录加载，revision 1只在Git历史与迁移记录中保留；
- 固定诊断可保存、提交、跳过、恢复和原子完成，失败不产生半写入；
- 数据产生唯一结果，私有资产不泄漏；
- 20个开发案例和60个正式案例输入可计算，首批20个gold双标完成；
- `difficulty-gold.jsonl`、`path-constraints.jsonl`和`adjudication-log.jsonl`由负责人生成并登记哈希；
- B的数据维护/复算脚本、E的claim拆分模板和指标脚本骨架均已入库；
- 本周不以PathEngine、真实模型、CIDPP、React或路径合法率作为完成项；
- 第二周达到完成条件时，D6必须已按“`W2_START_COMMIT`为最新HEAD祖先”的复验语义完成V2-1，并完成V2-2至V2-7首次完整执行；双封存和盲标资格检查通过时V2-8登记为`PENDING_OWNER_ADJUDICATION`，否则登记为`BLOCKED`。D7只处理阻塞、受影响项复验、V2-8终裁和负责人最终Go/No-Go上传；V2-8未转为`PASS`不得签署W2 GO。
