# 岗位 E｜第二周 D1 V2-1 基线报告

执行日期：2026-07-29  
执行人：岗位 E  
合同版本：`W2-C2 / W2-R4`  
仓库：`Pluggable-personalized-AI-learning-workstation`

## 结论

**V2-1：PASS。**

负责人已正式登记 `W2_START_COMMIT` 为 `f343a6c1c630f362f4686e6f6b0f50c6577d5562`，并确认第一周门禁已关闭。E 已在该提交上拉取、核对提交关系，并执行 `node scripts/w2-verification/v2-1-baseline.mjs --run`；提交关系和全部基线命令均通过。

## 提交关系事实

| 项目 | 实测结果 |
|---|---|
| 当前分支 | `main` |
| 当前 HEAD | `f343a6c1c630f362f4686e6f6b0f50c6577d5562` |
| 当前 HEAD 描述 | `<负责人>第一周任务完全完成`（2026-07-28 18:07:07 +0800） |
| 负责人登记的 `W2_START_COMMIT` | `f343a6c1c630f362f4686e6f6b0f50c6577d5562` |
| 统一基线 | `8785c6e40eb0cd95cee989d9b88cb8b341a4d627` |
| `8785c6e` 是否为当前 HEAD 祖先 | 是（`git merge-base --is-ancestor 8785c6e f343a6c` 退出码 0） |
| `HEAD == W2_START_COMMIT` | 是；E 拉取后 HEAD 与负责人登记值完全一致 |
| W1 遗留门禁书面关闭 | 已关闭；依据 `新版设计文档-重写版/第一周任务/E岗位第六天Go-No-Go清单.md` 中负责人最终裁决 `GO`（2026-07-28），明确批准进入第二周既定任务 |

## 已执行命令与真实结果

执行目录：`pi-study-helper/`

| 命令 | 结果 | 真实项数/输出 |
|---|---|---|
| `npm.cmd ci` | PASS | npm 调试日志记录退出码 0；未修改 `allowScripts`、依赖版本或锁文件。 |
| `npm.cmd run typecheck` | PASS | `tsc --noEmit && tsc -p tsconfig.test.json`。 |
| `npm.cmd test -- --maxWorkers=1` | PASS | 32 个测试文件通过，356/356 测试通过。 |
| `npm.cmd run smoke:extension` | PASS | Pi 成功解析并初始化 Pi Study Helper extension。 |
| `npm.cmd run verify` | PASS | 类型检查、32 文件/356 测试、30 个 Markdown 链接、扩展冒烟、发布检查全部通过。 |

`npm ci` 的首次 PowerShell 调用 `npm` 被本机执行策略拦截（`npm.ps1` 禁止执行）；改用等价的 `npm.cmd ci` 后成功。这是命令调用环境差异，不是仓库代码或依赖版本问题，也未通过 `npm approve-scripts`、修改 `allowScripts` 或运行 `npm audit fix` 绕过。

发布检查输出：190 个受跟踪文件，未发现私有数据或密钥。

## 负责人登记后的脚本复验

执行命令：

```powershell
Set-Location pi-study-helper
$env:W2_START_COMMIT = 'f343a6c1c630f362f4686e6f6b0f50c6577d5562'
node scripts/w2-verification/v2-1-baseline.mjs --run
```

结果：`V2-1 PASS: 提交关系与 D1 命令组均已通过。`

- 脚本确认 `headMatchesW2StartCommit: true`；
- 脚本确认 `baselineIsAncestor: true`；
- 脚本依次通过类型检查、32 文件/356 测试、扩展冒烟及完整 `verify`；
- 为消除 Windows 对 `.cmd` shim 的调用兼容问题，E 仅修正了本岗位的 `v2-1-baseline.mjs`，改为显式调用 `cmd.exe`；复验无警告、退出码 0。

## D1 已完成的 E 范围准备

- 新增 `pi-study-helper/scripts/w2-verification/v2-1-baseline.mjs`：必须显式提供 `W2_START_COMMIT`，拒绝猜测起始提交；可在门禁通过后运行 D1 命令组。
- 新增 `pi-study-helper/scripts/w2-verification/v2-7-asset-isolation.mjs`：只消费显式清单中的安全 DTO、普通日志、录制响应、安全导出和验证报告；不扫描整个源码仓库，且报告不回显 canary 正文。
- 新增 V2 验证目录说明、claim 拆分模板和指标脚本空输入骨架。它们只冻结结构，不读取正式系统输出、不产生正式指标值。

上述文件均属于岗位 E 在第二周允许修改的范围；尚未申请上传锁、提交或上传。V2-1 已通过，现将本报告回报负责人，由负责人宣布第二周业务实现 `GO`。
