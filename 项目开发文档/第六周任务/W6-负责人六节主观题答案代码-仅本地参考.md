# 六节主观题答案代码

> **文件性质：负责人本地参考资料**
>
> 本文件包含当前 revision 3 六个代码活动对应的参考答案。不得提交到公开仓库、不得上传到 Demo 页面、不得发送给学习者，也不得放入 Web/TUI 构建产物。
>
> 题目判定仍以服务端 Node/Python 正式评测、公开测试、hidden tests 和 Rubric 为准；复制代码只是参考，不代表绕过评测。

## 先看清“填空”与“完整代码”的关系

这类代码补全题有两层概念：

1. **学习者操作层**：页面先给出一份带函数、参数、固定返回语句和 `TODO_BEGIN/TODO_END` 标记的 starter code。学习者只应在标记之间补代码，其他骨架保持不变。
2. **评测提交层**：提交接口收到的是编辑器中的完整 Python 文本，因为评测器必须把完整程序写入临时文件后运行。因此，提交时看起来是“完整代码”，但学习者实际完成的是 TODO 区域。

例如 `act-load-csv` 页面原始骨架是：

```python
import pandas as pd

def load_orders(csv_path):
    # TODO_BEGIN
    pass
    # TODO_END
```

你真正需要填写的只有：

```python
return pd.read_csv(csv_path, dtype="string")
```

填写后，评测器看到的完整程序才是：

```python
import pandas as pd

def load_orders(csv_path):
    # TODO_BEGIN
    return pd.read_csv(csv_path, dtype="string")
    # TODO_END
```

所以：**不要把整份 reference solution 当成“必须从头重写的程序”；先看 starter code，再只替换 TODO 区域。**

### 六节的挖空范围

| 小节 | 页面已有的固定部分 | 学习者主要填写内容 |
|---|---|---|
| 01 读取 CSV | `import pandas as pd`、函数、参数和标记 | 一行 `pd.read_csv` 返回语句 |
| 02 检查结构 | 函数、`clean_df = df.copy()`、返回语句 | 列清单和固定列选择 |
| 03 缺失值 | 函数、复制输入、返回语句 | 主键过滤和备注缺失处理 |
| 04 重复订单 | 函数、复制输入、返回语句 | 日期解析、稳定排序和去重 |
| 05 类型格式 | 函数、复制输入、返回语句 | 文本、金额、城市、日期和状态规范化 |
| 06 综合实操 | 函数和 `raise NotImplementedError` | 完整函数体；这是独立实操，不是小段代码补全 |

第 06 节的 `coding_practical` 可编辑区域覆盖整个函数体，所以它确实要求学习者在给定函数框架内完成一整套流程；但函数名、参数和题目边界仍由系统固定。

## 当前实现需要注意的合同风险

Profile 中已经声明了 `editableRegions`，但当前提交 DTO 的 `userText` 是完整文本，`CodeActivityFacadeAdapter.submitActivityWithContext` 主要校验活动、Session、版本和幂等字段。负责人后续应单独复验：服务端是否拒绝修改 TODO 区域之外的固定代码。

如果固定骨架可以被任意改写，题目就会从“代码补全”退化成“开放式写完整程序”，这应作为代码活动合同整改项，而不是让学习者自行承担。

## 01 读取 CSV：`act-load-csv`

题目要求：实现 `load_orders(csv_path)`，使用传入路径读取 CSV，并让读取阶段的列保持字符串类型，不提前清洗金额、日期或状态。

```python
import pandas as pd


def load_orders(csv_path):
    return pd.read_csv(csv_path, dtype="string")
```

### 实际填空内容

只粘贴到 `TODO_BEGIN` 和 `TODO_END` 之间：

```python
return pd.read_csv(csv_path, dtype="string")
```

核心点：

- 使用 `csv_path`，不能写死文件名；
- 使用 `dtype="string"`，得到 Pandas `StringDtype`；
- 只负责读取，不负责清洗。

## 02 检查 DataFrame 结构：`act-inspect-dataframe`

题目要求：返回只包含七个合同列、顺序固定的新 DataFrame，不能修改传入的原始对象。

```python
"""Private draft reference; build validation and evaluator only."""

COLUMNS = ["order_id", "customer_id", "amount", "city", "order_date", "status", "note"]


def inspect_orders(df):
    return df.loc[:, COLUMNS].copy()
```

### 实际填空内容

页面已经提供 `clean_df = df.copy()` 和函数返回语句，TODO 区域内填写：

```python
columns = ["order_id", "customer_id", "amount", "city", "order_date", "status", "note"]
clean_df = clean_df.loc[:, columns]
```

核心点：

- 用固定列清单保证列名和列顺序；
- `.loc[:, COLUMNS]` 选择目标列；
- `.copy()` 防止修改原始 DataFrame。

## 03 处理缺失值：`act-missing`

题目要求：按照合同处理主键和备注列的缺失值。空订单号不能保留，备注缺失转为空字符串。

```python
"""Private draft reference; build validation and evaluator only."""


def clean_missing(df):
    result = df.copy()
    result["order_id"] = result["order_id"].astype("string").str.strip()
    result = result[result["order_id"].notna() & result["order_id"].ne("")].copy()
    result["note"] = result["note"].astype("string").fillna("").str.strip()
    return result
```

### 实际填空内容

页面已经复制输入并在 TODO 之后返回 `clean_df`，TODO 区域内填写：

```python
clean_df["order_id"] = clean_df["order_id"].astype("string").str.strip()
clean_df = clean_df[clean_df["order_id"].notna() & clean_df["order_id"].ne("")].copy()
clean_df["note"] = clean_df["note"].astype("string").fillna("").str.strip()
```

核心点：

- 先复制，避免直接改输入；
- `order_id` 先转字符串并去掉两端空格；
- `order_id` 缺失或空字符串的行要删除；
- `note` 缺失填充为空字符串。

## 04 处理重复订单：`act-duplicates`

题目要求：同一个 `order_id` 重复时，优先保留日期可解析的记录，再保留日期较新的记录；日期仍相同则保留原始位置靠前的记录，并恢复被选记录的原始相对顺序。

```python
"""Private draft reference; build validation and evaluator only."""

import pandas as pd


def deduplicate_orders(df):
    result = df.copy()
    result["_position"] = range(len(result))
    result["_parsed_date"] = pd.to_datetime(result["order_date"], format="%Y-%m-%d", errors="coerce")
    result["_parseable"] = result["_parsed_date"].notna()
    chosen = result.sort_values(
        ["order_id", "_parseable", "_parsed_date", "_position"],
        ascending=[True, False, False, True],
        kind="stable",
        na_position="last",
    ).drop_duplicates("order_id", keep="first")
    return chosen.sort_values("_position", kind="stable").drop(columns=["_position", "_parsed_date", "_parseable"])
```

### 实际填空内容

页面已经复制输入并在 TODO 之后返回 `clean_df`。由于 starter code 没有把 `pandas` 名称暴露在函数外，TODO 内需要包含局部导入：

```python
    import pandas as pd
    clean_df["_position"] = range(len(clean_df))
    clean_df["_parsed_date"] = pd.to_datetime(clean_df["order_date"], format="%Y-%m-%d", errors="coerce")
    clean_df["_parseable"] = clean_df["_parsed_date"].notna()
    clean_df = clean_df.sort_values(
        ["order_id", "_parseable", "_parsed_date", "_position"],
        ascending=[True, False, False, True],
        kind="stable",
        na_position="last",
    ).drop_duplicates("order_id", keep="first")
    clean_df = clean_df.sort_values("_position", kind="stable").drop(
        columns=["_position", "_parsed_date", "_parseable"]
    )
```

注意：这里每一行开头的 4 个空格必须保留，因为这些语句位于
`deduplicate_orders` 函数内部。若不想处理缩进，直接复制本节上方的“完整参考实现”
（包含函数定义）替换编辑器全文即可。

核心点：

- `_position` 保存原始行位置；
- `errors="coerce"` 让非法日期变成不可解析状态；
- 排序后按 `order_id` 稳定去重；
- 最后按 `_position` 排回原始相对顺序。

## 05 规范类型与格式：`act-types`

题目要求：清理金额、城市、日期、状态和文本列，并按照合同输出对应类型和业务取值。

```python
"""Private draft reference; build validation and evaluator only."""

import pandas as pd

CITY_MAP = {"上海市": "上海", "沪": "上海", "北京市": "北京", "京": "北京", "广州市": "广州", "穗": "广州"}
VALID_STATUS = {"completed", "pending", "cancelled"}


def normalize_types(df):
    result = df.copy()
    for column in ["order_id", "customer_id", "city", "status", "note"]:
        result[column] = result[column].astype("string").str.strip()
    amount_text = result["amount"].astype("string").str.replace(r"[¥￥,\s]", "", regex=True)
    result["amount"] = pd.to_numeric(amount_text, errors="coerce").replace([float("inf"), float("-inf")], pd.NA)
    result["city"] = result["city"].replace(CITY_MAP)
    result["order_date"] = pd.to_datetime(result["order_date"], format="%Y-%m-%d", errors="coerce")
    result["status"] = result["status"].str.lower().where(result["status"].str.lower().isin(VALID_STATUS), pd.NA)
    result["note"] = result["note"].fillna("")
    return result
```

### 实际填空内容

页面已经复制输入并在 TODO 之后返回 `clean_df`。TODO 内填写以下处理逻辑：

```python
import pandas as pd
city_map = {"上海市": "上海", "沪": "上海", "北京市": "北京", "京": "北京", "广州市": "广州", "穗": "广州"}
valid_status = {"completed", "pending", "cancelled"}
for column in ["order_id", "customer_id", "city", "status", "note"]:
    clean_df[column] = clean_df[column].astype("string").str.strip()
amount_text = clean_df["amount"].astype("string").str.replace(r"[¥￥,\s]", "", regex=True)
clean_df["amount"] = pd.to_numeric(amount_text, errors="coerce").replace([float("inf"), float("-inf")], pd.NA)
clean_df["city"] = clean_df["city"].replace(city_map)
clean_df["order_date"] = pd.to_datetime(clean_df["order_date"], format="%Y-%m-%d", errors="coerce")
clean_df["status"] = clean_df["status"].str.lower().where(clean_df["status"].str.lower().isin(valid_status), pd.NA)
clean_df["note"] = clean_df["note"].fillna("")
```

核心点：

- 文本列先转 `string` 并去掉两端空格；
- 金额去掉货币符号、逗号和空白后再转数值；
- 城市使用固定映射表；
- 日期使用指定格式解析，非法值转缺失；
- 状态统一小写，非允许值转缺失。

## 06 综合独立实操：`act-practical`

题目要求：实现 `clean_orders(df)`，把前五节的结构检查、缺失处理、类型规范化、稳定去重和结果验证组织成完整流程。

```python
"""Private draft reference; exact dtype names remain pending the C environment lock."""

import pandas as pd

COLUMNS = ["order_id", "customer_id", "amount", "city", "order_date", "status", "note"]
CITY_MAP = {"上海市": "上海", "沪": "上海", "北京市": "北京", "京": "北京", "广州市": "广州", "穗": "广州"}
VALID_STATUS = {"completed", "pending", "cancelled"}


def clean_orders(df):
    result = df.loc[:, COLUMNS].copy()
    result["_position"] = range(len(result))
    for column in ["order_id", "customer_id", "city", "status", "note"]:
        result[column] = result[column].astype("string").str.strip()
    result = result[result["order_id"].notna() & result["order_id"].ne("")].copy()
    amount_text = result["amount"].astype("string").str.replace(r"[¥￥,\s]", "", regex=True)
    result["amount"] = pd.to_numeric(amount_text, errors="coerce").replace([float("inf"), float("-inf")], pd.NA)
    result["city"] = result["city"].replace(CITY_MAP)
    result["order_date"] = pd.to_datetime(result["order_date"], format="%Y-%m-%d", errors="coerce")
    result["status"] = result["status"].str.lower().where(result["status"].str.lower().isin(VALID_STATUS), pd.NA)
    result["note"] = result["note"].fillna("")
    result["_parseable"] = result["order_date"].notna()
    chosen = result.sort_values(
        ["order_id", "_parseable", "order_date", "_position"],
        ascending=[True, False, False, True],
        kind="stable",
        na_position="last",
    ).drop_duplicates("order_id", keep="first")
    chosen = chosen.sort_values("_position", kind="stable").drop(columns=["_position", "_parseable"])
    for column in ["order_id", "customer_id", "city", "status", "note"]:
        chosen[column] = chosen[column].astype("string")
    return chosen.reset_index(drop=True)
```

### 实际填写范围

这一节的 starter code 是：

```python
def clean_orders(df):
    # TODO_BEGIN
    raise NotImplementedError
    # TODO_END
```

这里 TODO 覆盖整个函数体，所以需要把完整清洗流程写在函数内部。前面展示的 `solution-practical.py` 是完整参考实现，不是让你在代码补全题中照抄到每个小节的内容。

## 使用提醒

1. 代码补全题只把对应函数内容填入 `TODO_BEGIN` 和 `TODO_END`，不要删除函数名、参数、导入和边界标记。
2. 不要把上述代码直接写入题库、公开题面、Web bundle 或交付报告。
3. 不要改动 `environment-lock.json`、公开测试、hidden tests、Rubric 或 reference solution 来迁就答案。
4. 真实提交时仍应使用题面提供的 `csv_path` 或 `df`，不能硬编码示例数据。
5. 当前正式判分由 Node/Python 评测器完成；浏览器预览（如果存在）不产生正式 Attempt、Evidence 或 mastery。
