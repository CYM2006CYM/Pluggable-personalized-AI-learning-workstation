# 六周 Pandas 个性化学习产品开发计划、验收与提交缓冲

> **第四周D0已签发**：2026-08-11以W3 GO提交`ac6e307e17cf84450845dfc5ffa467063dd3ae4c`为`W4_START_COMMIT`，D48—D60形成`W4-C2/W4-R1`。第四周以41—47号为执行入口，新增V4-8；运行时CIDPP关闭，真实Key不属于门禁。

> **本轮冻结覆盖**：D01—D47均已生效；D48—D60已签发并在负责人W4 D0提交进入`origin/main`后对成员生效。D15固定OpenAI兼容模型配置，D16关闭五项六周候选并保留Monaco条件门禁，D17固定CIDPP自动裁决，D18冻结前端依赖白名单，D19/D20冻结人工评审与批准机制，D22—D31冻结第二周诊断、类型、事务和首轮执行合同，D32冻结D2—D6负载、候选哈希和V2-8状态，D33以`W2-V2-3-ENV-1`冻结第二周V2-3审计临时环境基线和错误归因；`W2-D7-CLOSEOUT-1`只处理本批D7证据与终裁收口。D34—D43形成第三周`W3-C1`，已于2026-08-06以`W3_START_COMMIT=f190326a4a906b46e4001484ffa30a7839b82ed2`完成D0签发，现行调度为`W3-R2`；D44—D46依次冻结gold、V3和前端口径；D47冻结三层测试、Windows兼容和无真实Key的固定轨迹Demo，现行第三周合同为`W3-C5/W3-R2`。D48—D60形成第四周`W4-C2/W4-R1`。负责人权限、不得越权规则和岗位入口见 [第一周总任务书](./第一周任务/19-第一周总任务布置与权限边界.md)，跨周公共合同见 [第一周公共合同总册](./第一周任务/21-第一周公共合同总册.md)，第二周执行合同为 [第二周公共合同总册](./第二周任务/28-第二周公共合同总册.md) 的`W2-R5`。

> 文档状态：六周MVP开发执行基线；第7周仅作提交缓冲，不计入功能开发周期。
> 适用团队：4至5名大一学生，中等投入，可一人兼任两条工作流。  
> 开发节奏：第2周完成资产和确定性学情底座；第3周完成确定性闭环并搭建React骨架；第4周接入模型和页面真实数据；第5周联调并彩排评测；第6周冻结、正式评测和交付；第7周只做提交缓冲。
> 核心纪律：功能开发时同步完成最小契约测试和单元测试，不能把所有测试留到第6周；第3周结束后原则上不再改变核心数据结构、评分规则和公共接口。

## 1. 六周交付目标

全项目当前架构关系见[架构图PNG预览](./project-graph/pi-study-helper-full-architecture.drawio.png)，可编辑源文件见[Draw.io架构图](./project-graph/pi-study-helper-full-architecture.drawio)。该图只作开发导航和交接参考，不替代本文件的验收标准；交互式HTML运行图和开发DAG尚未进入当前快照，不得按“已有”验收。

六周内交付一条能够在本地运行、由发榜公司接入其兼容模型接口后复测的 Pandas 个性化学习闭环：

```text
Profile v2资料包
→ 背景问卷和固定诊断
→ 知识状态与合法路径
→ 学习卡片和活动
→ 动态单选/判断或固定兜底
→ 代码补全和独立实操
→ 确定性判分与学习证据
→ 知识状态及未完成路径更新
→ 中断恢复和下一步学习
```

最终产品为 React + Vite 本地网页、本地 Node.js 服务和 Pi 终端兼容入口。产品不正式上线，不建设登录、多租户、云数据库或公网部署。

## 2. 范围与优先级

### 2.1 P0：必须完成

1. Profile v2读取、校验、修订兼容和Profile v1回归；
2. 一个完整的 `pandas-cleaning` Profile v2和一个极小非Pandas smoke fixture；
3. 来源锚点、固定诊断、`KnowledgeState`和确定性 `PathEngine`；
4. 章节学习与系统推荐路径共用同一活动、证据和路径引擎；
5. AI动态生成单选/判断，程序比较 `correct_answer`；revision 3首次生成失败时使用完整4—6题fixed组；
6. Pandas固定代码补全、固定最终实操、确定性Rubric和Node.js正式评测；
7. `Attempt`、`Evidence`、版本比较、幂等提交和会话恢复；
8. 内容质量CIDPP、风险驱动的串行智能体审核和固定降级资源；
9. React主流程、本地HTTP接口和TUI/Web共享会话；
10. 60个离线案例、3个完整展示案例、20组盲评、6至10名试用者和指标报告。

### 2.2 可降级

按下列顺序降级，前一项不足以挽回进度时才执行后一项：

1. 取消雷达图不是默认降级项；保留五维“分数+置信度+证据卡”，只有负责人批准的进度降级才隐藏可视化，不能删除画像数据；
2. 关闭运行时CIDPP，只保留构建期内容质量摘要；
3. 减少动态客观题数量；
4. 减少动态题变体，但不得删除revision 3每点的完整4—6题fixed组；1—2道supplemental题只用于动态候选补位，无法形成不重复重试组时登记`insufficient`；
5. Pyodide不可用时显示“公开预览暂不可用”，保留Node.js正式提交；
6. 不接外部评测器，始终使用Node.js/Python本地评测器；
7. 代码活动缩减为一个代码补全和一个最终实操；
8. 路径图改为纵向步骤列表；
9. 取消动画和高级编辑器能力，使用普通文本编辑区。

不得降级：Profile校验、固定诊断保存与恢复、路径合法性、客观题确定性判分、Node.js正式代码判分、隐藏资产隔离、Attempt/Evidence、版本冲突保护、重复提交保护、Profile v1回归和正式评测原始记录。

### 2.3 延期

六周不实现：登录、云数据库、公网部署、任意CSV上传、移动应用、教师后台、并行智能体网络、第二个完整领域、动态简答、完整AI代码题生成、模型训练或微调、复杂分析仪表盘、生产级恶意代码沙箱、外部代码评测器接入、GoA/OAEO、SSE推送、批量离线生成和任意在线安装依赖。D16关闭的五项能力只保留赛后设计或调研记录，六周内不得申请接入。

## 3. 团队工作流与负责人

“负责人”表示对该工作流的完整性和交付负责，不表示可以独自修改所有相关文件。一个功能通常跨越多个工作流：资产事实由内容组提供，领域规则由架构组实现，执行器产生确定性结果，网页只展示安全视图，测试组独立验证。任何工作流都不能把自己的中间结果直接升级为正式业务事实。

### 3.1 工作流权责总表

| 工作流 | 拥有的决策权 | 主要交付 | 必须征求其他工作流确认 | 不拥有的决策权 |
|---|---|---|---|---|
| 领域与架构 | 公共类型、确定性规则、应用用例、仓储事务、版本与错误语义 | Profile v2加载、诊断/证据/路径、Facade、会话仓储和恢复 | Profile资产字段需内容组确认；活动提交需执行器组确认；安全DTO需网页/测试确认 | 专业内容事实、题目答案、隐藏测试内容、模型提示和页面视觉 |
| 资料包与内容 | 学什么、来源依据、讲义、诊断蓝图、固定任务业务契约和Rubric教学维度 | Pandas Profile、来源锚点、数据、诊断题、固定任务、参考实现和内容质量记录 | schema由架构组确认；测试可执行性由执行器组确认；正式人工标准由独立评审确认 | 掌握度算法、路径排序、提交事务、执行器错误归因和正式评测结果 |
| 执行器与后端 | 如何安全准备和执行代码、如何从确定性测试生成 `ActivityResult` | Node.js/Python适配器、Pyodide公开运行、执行协议、环境锁和故障证据 | TaskContract/Rubric由内容与架构确认；HTTP安全响应由网页/测试确认 | 修改题面事实、Rubric权重、路径、Evidence权重、学习者画像和Agent结论 |
| Agent与模型 | 模型输入裁剪、提示、角色图、串行审核、缓存和固定降级调用 | 动态题、CIDPP、Generator/Hunter/Defender/Judge轨迹和模型适配器 | 输入/输出schema由架构组确认；来源范围由内容组确认；泄漏检查由测试组确认 | 客观题最终对错、代码分数、隐藏测试、知识状态、路径合法性和正式证据写入 |
| 网页与测试 | 网页交互、安全展示、自动化验证、正式评测执行和证据报告 | React页面、HTTP客户端、端到端测试、评测脚本、报告和证据索引 | 页面动作调用架构组Facade；代码状态遵循执行器结果；人工内容判断由独立评审确认 | 在前端复制业务规则、为了通过测试改标准答案、选择性删除失败记录或暴露受限资产 |

### 3.2 领域与架构工作流边界

**允许负责和修改：**

- `src/domain/`中的公共类型、状态机、纯规则、结构校验和错误分类；
- `src/application/`中的 `LearningRuntimeFacade`、诊断、路径、活动提交和恢复用例；
- 仓储端口、本地文件实现、比较写版本、幂等和提交标记；
- Profile v2解析、v1兼容适配、路径安全和交叉引用校验；
- `mastery`、`confidence`、`PathEngine`和Evidence映射等确定性算法；
- 向网页、TUI、Agent和执行器提供稳定接口，不向它们泄漏真实文件路径。

**输入边界：** 内容组提供已登记的Profile资产和业务规则；执行器组提供 `ActivityResult`；Agent组提供经过schema校验的解释或审核建议；网页/TUI只提交公共请求字段。领域与架构组必须把这些输入视为待校验输入，不能因为来自组内成员就跳过校验。

**输出与验收责任：** 输出公共DTO、Facade结果、不可变Evidence、路径快照和提交版本。该组对“相同输入得到相同确定性结果、非法路径不能写入、失败不留下半提交、v1不回归”负责。

**不得越权：**

1. 不得自行编写或裁决Pandas专业事实、来源许可证、诊断答案和参考实现；
2. 不得为某个展示案例硬编码路径、难度、知识点状态或分数；
3. 不得在Controller、HTTP路由、React或TUI中复制第二套领域规则；
4. 不得直接读取模型自由文本后写入Evidence或路径，必须先经过结构校验和确定性规则；
5. 不得把仓储“写入成功”等同于整个跨仓储事务提交成功；只有唯一提交标记公开后结果才可读；
6. 不得为了修复界面问题改变业务错误含义，也不得为了指标达标修改正式人工标准；
7. 不得以架构重构为由引入FastAPI、图数据库、消息队列或第二套运行时。

### 3.3 资料包与内容工作流边界

**允许负责和修改：**

- `pandas-cleaning`资料包中的讲义、知识点、来源锚点、诊断题面和答案；
- 公开/私有合成数据、日期格式、城市映射和唯一 `clean_df` 业务契约；
- 固定代码补全、最终实操、起始代码、可编辑区域、参考实现和已知错误实现；
- Rubric的教学维度、权重、阻断原则和安全反馈含义；
- 内容质量CIDPP的评价对象和人工确认记录；
- Profile revision对应的资产摘要和许可证记录。

**输入边界：** 只能使用已登记来源、维护者确认的业务规则和02号Profile路径；不得把模型生成内容、网络搜索结果或个人记忆直接作为正式事实。执行器返回的运行结果用于验证资产，不自动授权修改测试标准。

**输出与验收责任：** 输出可由Profile v2校验器加载的完整资产。该组对“来源可查、题面与答案一致、数据只有一个规范结果、参考实现通过所有活动测试、固定兜底在无模型时可用”负责。

**不得越权：**

1. 不得修改 `mastery/confidence`公式、证据权重、路径排序、状态阈值和会话版本；
2. 不得直接修改active资料包；所有正式修改必须创建draft、通过校验并产生新revision；
3. 不得把诊断题、学习兜底题和正式效果评测题互相复用；
4. 不得把参考实现、诊断答案、私有测试或完整Rubric放入学习者可见目录、Agent上下文或普通日志；
5. 不得在看过正式评测结果后降低难度、删除失败数据、改核心标记或修改人工标准；
6. 不得自行认定执行器异常是学习者错误，也不得用人工印象覆盖确定性代码测试；
7. 不得增加资料包外依赖、任意CSV上传或未经环境锁验证的API；
8. 内容作者不得成为自己生成内容的唯一事实裁决者，核心事实和正式人工标准必须有第二人复核。

### 3.4 执行器与后端工作流边界

**允许负责和修改：**

- 12号规定的 `CodeEvaluationPort`、`BrowserCodeRunner`和父子进程协议；
- Node.js/Python正式提交、Pyodide公开运行、临时目录和进程生命周期；
- 公开/隐藏测试装载、Rubric确定性汇总、输出裁剪和错误阶段归因；
- `EnvironmentLock`、版本探测、资源限制、能力标志和原型证据；
- 评测器故障记录、幂等执行配合、临时目录清理和Windows进程树验证；
- Node.js/Python本地评测器适配和故障恢复；外部评测器只保留赛后调研，不进入六周实现。

**输入边界：** 只接受服务端根据Profile和会话重建的任务、环境和测试引用；不信任前端提交的测试、Rubric、环境版本、允许库、宿主路径或任务来源覆盖值。

**输出与验收责任：** 只输出符合 `ActivityResult` 合法组合的确定性结果和安全反馈。该组对“同一锁定输入结果可重复、学习者错误与评测器错误区分、隐藏资产不泄漏、Node正式结果权威”负责。

**不得越权：**

1. 不得在适配器中修改题面、参考答案、Rubric权重、通过阈值或阻断测试定义；
2. 不得计算或写入 `mastery`、路径和通用Evidence；只能生成ActivityResult并交应用层提交；
3. 不得信任客户端传来的隐藏测试、文件路径、依赖列表和环境覆盖值；
4. 不得把评测器启动失败、依赖缺失、测试损坏或无法归因的崩溃记成学习者错误；
5. 不得把普通 `child.kill()`、Web Worker或导入白名单宣传为生产级沙箱；
6. 未经故障测试证明，不得声明可靠内存限制、网络隔离或完整进程树终止；
7. 不得把隐藏断言、精确预期值、参考实现、绝对路径和接口密钥写入反馈或日志；
8. 六周正式提交只使用Node.js/Python本地评测器；外部评测器不得接入。赛后若重新立项，也不得改变HTTP、Attempt、Evidence、Rubric和页面契约。

### 3.5 Agent与模型工作流边界

**允许负责和修改：**

- 模型执行适配器、结构化输入输出、提示版本和录制响应；
- 动态单选/判断候选题、教学卡片、解释、反馈和CIDPP内容评价；
- `Generator → Hunter → 条件Defender → Judge`严格串行编排；
- 审核轨迹、超时、有限重试、缓存和固定fallback调用；
- 来源范围、泄漏阶段和学情安全摘要的提示注入方式。

**输入边界：** Agent只接收经过应用层裁剪的安全画像、允许知识点、来源摘录、公开错误类别和版本摘要。私有答案、隐藏测试、参考实现、接口密钥、绝对路径和其他用户原始数据不得进入上下文。

**输出与验收责任：** 输出结构化候选内容、问题清单、裁决和面向学习者的解释。该组对“输出可校验、角色职责不重叠、失败可降级、轨迹可审计、不泄漏答案”负责。

**不得越权：**

1. 不得直接写Profile active目录、会话仓储、Attempt、Evidence、KnowledgeState或LearningPath；
2. 不得决定客观题最终对错、代码分数、Rubric维度分、通过状态和路径合法性；
3. 不得新增资料包外知识点、来源、允许API或运行依赖；
4. Judge不得覆盖程序硬门禁，Defender不得无证据替Generator补事实；
5. 不得让多个角色并行修改同一产物，也不得跳过Hunter直接伪造“已审核”；
6. 不得无限自动修订；达到预算、超时或结构无效时必须使用固定fallback；
7. CIDPP只评价教学内容，不能成为学习者掌握度或路径分数；
8. 不得把模型输出、思维过程或完整提示直接展示为正式证据。

### 3.6 网页与测试工作流边界

该工作流由同一成员兼任时，仍须区分“网页开发”和“测试评测”两种身份。网页开发者可以修复展示和客户端状态；正式评测执行者必须按冻结版本运行，不能为了页面通过而改变服务端事实或评测标准。

**网页允许负责和修改：**

- React路由、页面、组件、客户端状态、可访问性和安全错误展示；
- 调用Facade/HTTP、保存本地未提交草稿、显示版本冲突和恢复入口；
- `textarea` P0编辑器和Pyodide Worker客户端生命周期；Monaco只允许通过W5提前门禁后进行纯前端替换；
- 只根据安全DTO展示路径、证据、运行结果和Agent步骤。

**测试允许负责和修改：**

- 契约、单元、集成、端到端、属性、安全和故障注入测试；
- `evaluation/`清单、运行脚本、随机盲评表、指标计算、报告和证据索引；
- 正式运行编号、环境/资料包摘要、原始结果和失败记录；
- 发布门禁检查，但内容事实由独立内容评审判定。

**输出与验收责任：** 网页侧对“用户始终知道当前状态和下一步、刷新不丢草稿、前端不复制业务规则”负责；测试侧对“分子分母可复算、失败不被隐藏、正式资产未污染、发布结论有原始证据”负责。

**不得越权：**

1. React不得直接读取/写入Profile、仓储、隐藏测试、参考实现或宿主文件路径；
2. 前端不得自行计算掌握度、解锁节点、判定答案、汇总Rubric或把Run结果写成Evidence；
3. 不得仅隐藏按钮来代替服务端权限、版本和先修校验；
4. 不得把API Key、隐藏测试、完整Rubric、参考实现、Agent系统提示或绝对路径放进bundle、DOM、localStorage或网络响应；
5. 测试负责人不得单独裁决专业事实，也不得修改来源或答案使测试通过；
6. 不得看过正式结果后修改 `final-60`、人工标准、指标分母、排除规则或盲评配对；
7. 不得选择性删除失败、超时、负向盲评或中途退出记录；
8. 不得把页面可用误写成学习效果有效，也不得把6至10人试测夸大为因果结论。

### 3.7 跨工作流交接和变更规则

本节给出权责摘要；交接五步法、问题模板、三级变更、Git协作和兼任复核的完整执行规范见 [18-跨工作流协作交接与变更治理指南](./18-跨工作流协作交接与变更治理指南.md)。

每个跨组交付使用“生产方—使用方—验收方”三方关系：

| 交付物 | 生产方 | 使用方 | 独立验收方 | 验收后变更 |
|---|---|---|---|---|
| Profile schema和安全DTO | 领域与架构 | 内容、Agent、网页、执行器 | 测试 | 不兼容变更升级schema/接口版本并补迁移测试 |
| Pandas资料和固定任务 | 资料包与内容 | 路径、Agent、执行器、网页 | 第二内容评审+测试 | 正式资产变化创建新Profile revision |
| ActivityResult和环境锁 | 执行器与后端 | 应用层、网页、测试 | 领域与架构+测试 | 结果协议变化升级评测协议版本 |
| Agent候选和审核轨迹 | Agent与模型 | 应用层、网页、盲评 | 内容+测试 | 提示变化记录prompt版本并使相关缓存失效 |
| 正式指标和报告 | 网页与测试中的测试身份 | 项目组、评委、公司 | 对应领域负责人交叉复核 | 不覆盖旧运行，创建新evaluationId |

跨组发现问题时，发现者提交复现输入、期望合同、实际结果和影响范围；由拥有该规则决策权的工作流修改。使用方不得在自己的层临时打补丁绕过生产方合同。例如：网页发现路径错误，应提交给领域与架构修复，不能在React中重新排序；执行器发现Rubric歧义，应退回内容/架构确认，不能自行改权重。

### 3.8 兼任、复核与升级规则

团队不足5人时允许兼任，但遵守以下规则：

1. 同一人兼任两个工作流时，代码提交中仍需标明当前以哪个职责修改、影响哪个合同；
2. 同一人不能独立批准自己制作的核心内容、隐藏测试、正式人工标准或发布门禁；至少由另一名成员复核；
3. 网页与测试由同一人负责时，正式评测必须使用冻结脚本和只读资产，不能边测边修改页面或标准；
4. 内容与执行器由同一人负责时，参考实现和测试至少由另一人运行已知错误样例进行反向验证；
5. 架构与Agent由同一人负责时，Agent输出仍必须通过同样的schema和权限门禁，不能调用内部方法跳过校验；
6. 出现跨组争议时，以02号Profile规范、11号学情规则、12号执行器规范、05号评测口径和本06号排期的职责边界为裁决顺序；仍无法解决则记录为阻塞项，由全组在当日门禁会上决定并形成书面记录。

每天短会只同步四项：已完成的可验证产物、当前阻塞、接口或资产变化、当天门禁。每周末保存一次可运行版本、测试结果、资料包revision、环境摘要和未决问题记录。

## 4. 全局开发顺序

必须遵守以下依赖顺序：

```text
Profile v2契约
→ 正式来源和Pandas资料资产
→ 固定数据及clean_df契约
→ 诊断/Evidence/KnowledgeState
→ PathEngine和会话仓储
→ 固定代码任务和Rubric
→ Node.js正式执行器
→ React mock骨架和安全DTO
→ 动态客观题、五维画像和串行审核
→ 本地HTTP和React真实数据页面
→ Pyodide公开预览
→ 端到端、故障和安全测试
→ 发布冻结、正式评测与交付
```

禁止倒序开发：Profile契约未冻结前不做完整页面；`clean_df`契约未确定前不冻结隐藏测试；Node正式评测未稳定前不接最终实操页面；React mock骨架不得提前实现业务判断；路径和证据未稳定前不允许Agent决定下一步；完整AI代码题按D16在六周内关闭，不得以固定任务已经通过为由提前投入；CIDPP只有主链全绿并经负责人批准后才实施；不得看过正式集结果后修改人工标准。

## 5. 每周验证性测试统一规则

自第2周起，每周末执行该周的验证性测试清单。验证与该周Go/No-Go门禁具有同等效力，并统一遵守以下规则：

1. 生产方可以先自测，但计入周验收的结果必须由清单指定的执行人运行；内容事实、隐藏资产、正式人工标准等必须由未单独制作该产物的第二人复核；
2. 每周验证结果只写入 `新版设计文档-重写版/周门禁记录/W{n}-验证记录.md`，记录日期、基线提交、执行人、命令或步骤、关键输出、证据路径和逐项结论；不得在仓库根目录另建周门禁目录；
3. 任一硬性标准未达到，该周门禁即未通过；下周只能先修复阻塞项，除非负责人按既定降级阶梯作出书面裁决；
4. 自动化验证必须能在干净临时数据目录重复运行；模型集成测试统一使用负责人审计冻结的录制响应或固定适配器，不使用真实Key；要求重复运行的测试必须在同日、同一环境完成并登记哈希或差异；
5. 当周验证标准随周计划一并冻结，不得为了让结果通过而临时降低标准；确需修订时，必须由负责人书面批准并在验证记录中保留变更前后口径；
6. 验证记录必须区分“生产方自测”“独立复验”和“负责人门禁结论”，组员的PASS只能作为证据，最终Go/No-Go由负责人决定。

## 6. 第1周（7/20–7/26）：冻结契约，保住旧系统

项目负责人向组员分配第1周任务时，使用 [19-第一周总任务布置与权限边界](./第一周任务/19-第一周总任务布置与权限边界.md)；各成员再进入对应岗位任务书查看文档索引、交付物和验收边界。

### 6.1 本周目标

冻结后续代码共同遵守的数据、目录、接口、状态和错误规则；建立可持续回归基线。第1周不制作完整网页，只允许定义HTTP契约和安全数据视图。

### 6.2 任务顺序

1. 运行并记录当前 `npm run typecheck`、`npm test`、`npm run smoke:extension`和`npm run verify`基线；
2. 清点Profile v1、`StudySession`、`Attempt`、`SessionEvidence`、五个Pi命令、三个仓储和当前智能体执行器；
3. 冻结Profile v2唯一清单、目录、条件资产、revision和v1兼容规则；
4. 冻结活动、诊断、Evidence、KnowledgeState、路径、提交结果和安全视图类型；
5. 冻结状态枚举、错误码、版本字段、幂等字段和可重试语义；
6. 定义 `LearningRuntimeFacade`、`CodeEvaluationPort`和HTTP接口的最小合同；
7. 建立模型录制响应/固定适配器，使自动测试不依赖实时模型；
8. 建立来源登记模板、许可证字段和“官方文档优先”的裁决规则；
9. 扩展Profile v2解析骨架、路径安全检查和坏包拒绝测试。

### 6.3 第1周结束前必须冻结

| 标准 | 冻结内容 | 变更规则 |
|---|---|---|
| Profile契约 | `profile.json`字段、英文路径、条件目录、资产引用和revision语义 | 第2周后不兼容修改必须升级schema并同步迁移测试 |
| v1兼容 | `demo-review`继续按v1加载、学习、修订、启用和恢复 | 新功能不得要求v1补齐v2资产 |
| 公共类型 | `LearningActivity`、`ActivityAttempt`、`Evidence`、`KnowledgeState`、`LearningPath`、`ActivityResult` | 第3周后只允许向后兼容的可选字段 |
| 版本字段 | `profileRevision`、`evidenceVersion`、`pathVersion`、`sessionVersion` | 所有写请求必须校验相关版本 |
| 错误合同 | 错误码、触发条件、是否已写入、是否可重试和界面动作 | 前后端不得各自发明错误语义 |
| 执行职责 | Pyodide只做公开预览；Node.js/Python正式提交为六周唯一权威结果 | 外部评测器六周关闭；赛后重新立项也只能替换适配器 |
| Agent权限 | Agent不能修改答案、路径合法性、掌握度、代码分数和正式证据 | 越权输出必须被应用层拒绝 |
| SDK协作 | 工程基线固定为负责人批准的SDK版本和提交；Profile、会话及业务协议不保存该哈希 | 只有负责人可批准升级，且须同步源码目录、依赖、锁文件、文档和回归 |

### 6.4 本周交付物

- Profile v2 schema/解析骨架和v1兼容适配；
- 活动、学情、路径、评测结果和公共错误类型；
- `LearningRuntimeFacade`与 `CodeEvaluationPort`最小接口；
- 来源登记模板和人工标准模板；
- 模型录制响应测试适配器；
- 非法Profile fixture和测试命名规范；
- 第1周契约冻结记录。

### 6.5 最小测试

- 现有测试全部通过；
- v1资料包仍可读取、学习、恢复和修订；
- 路径穿越、缺失文件、悬空引用、重复清单和错误revision能被拒绝；
- 录制响应可以替代真实模型；
- 领域规则不依赖React或实时模型。

### 6.6 Go/No-Go门禁

只有旧回归全绿、v2命名无高影响未决项、核心错误码已登记、至少一个非法v2包能被拒绝、确定性领域逻辑不依赖网页或模型时，才能进入第2周。否则停止新增功能，优先修复契约和回归。

## 7. 第2周（7/27–8/2）：Pandas资产与确定性学情底座

> **D27/D32执行节奏**：本周完整使用7天，不设置“D5全绿即提前启动W3”。D2五岗并行且不上传；D3由A上传；D4并行推进“B候选→A/C/E审计→完整V2-3→gold输入冻结”和“D材料→A/E审计→D上传”，gold分支只控制B/E能否开始标注，不阻塞已通过审计的D上传；D5按B→C上传；D6形成V2-1至V2-7结论，只有双标完成、双封存和盲标资格检查全部通过时才按B→E上传并让V2-8进入待终裁，否则记为`BLOCKED`；D7只做阻塞修复、V2-8终裁、负责人批准与Go/No-Go。W3最早于8/3启动。

> **2026-08-03执行勘误（`W2-GOLD-AUTH-CORR-1`）**：D4默认18:00截止规则继续有效，但本批次A/C/E审计、完整V2-3及负责人冻结在D4晚间全部完成后，负责人以`gold-input-freeze.json.frozenAt = 2026-08-01T19:37:30.5739587+08:00`为生效锚点，书面批准B/E立即开始首批20例独立标注。原“D5生效”文字勘误为上述准确时间；该例外不改变冻结输入、不提前授权原始标注入Git，也不代替D6双封存和盲标资格检查。

> **D33审计临时环境基线**：第二周V2-3统一使用`W2_V2_3_AUDIT_PANDAS_VERSION = 3.0.5`。Python和平台只登记实际证据，不在本裁决中冻结最终值；Pandas版本不匹配时必须在读取或判断B资产前返回`environment_mismatch/BLOCKED`，不得归因为`b_asset_defect`。该值只保证W2审计可复现，不等于最终产品的Python/Pandas/Pyodide环境锁；最终`environment-lock.json`仍按第3至5周原型证据冻结。

> **D7一次性收口（`W2-D7-CLOSEOUT-1`）**：负责人沿用B既有独立性声明并登记授权JCS勘误；以独立差异核对和真实六输入作者测试代替C对E新工具的本次“不降标”确认；授权E仅在仓库外构造普通日志与安全导出测试视图并完成五类V2-7扫描；E精确开始时间不可恢复但不作为本批资格阻塞；本批首20例跳过B/E协商，由负责人直接终裁。该补充签发时不改变第三周规则；2026-08-06新增D44后，第三周剩余40例也改为双封存后由负责人直接终裁。原始标注、冻结输入、哈希和Schema始终不变。

> **E整改唯一入口**：E必须先拉取包含`e7136b320f2ce28edc0185dcf52c9d0e67288f7c`及后续整改执行单的最新`origin/main`，再按 [E岗位W2-D7-CLOSEOUT-1一次性整改执行单](./第二周任务/E岗位W2-D7-CLOSEOUT-1一次性整改执行单.md) 执行。旧记录中的“等待C确认”“等待ordinary log/safe export/C-D报告”均已失效；最终记录必须基于最新HEAD，把V2-3记为负责人确认的PASS、把完成六输入扫描的V2-7记为PASS，并在负责人双封存签署后把V2-8记为`PENDING_OWNER_ADJUDICATION`。

> **W2身份基线**：不可变周起点固定为`W2_START_COMMIT = f343a6c1c630f362f4686e6f6b0f50c6577d5562`。D1要求当时`HEAD == W2_START_COMMIT`；负责人合同勘误进入`main`后，A在D2-D3开始或继续实现前必须拉取最新`origin/main`，实际工作HEAD只需以固定周起点为祖先。后续工作提交不能替换该常量。

### 7.1 本周目标

把Pandas资料包变成可加载、可校验、可复算的事实底座，完成固定诊断、Evidence、KnowledgeState和会话持久化事务骨架。PathEngine尚未完成，本周只证明案例输入、诊断结果和路径前置数据可计算，不运行路径引擎，也不验收路径合法率。

### 7.2 任务顺序

每日出口和正常上传顺序固定如下；同日箭头表示上传锁顺序，后一岗位必须先拉取前一岗位的提交：

| 天 | 主要工作 | 正常上传 | 当日出口 |
|---|---|---|---|
| D2 | A开发诊断/学情/事务；B完成Profile主体；C/D/E完成工具和离线材料主体 | 无 | 五岗候选可以在D3收口，不产生正式PASS |
| D3 | E滚动预检V2-4/V2-5，A修复后上传；B/C/D/E形成冻结候选 | A | A正式提交进入`main` |
| D4 | 基于A提交并行执行两条链：B候选→A/C/E审计→C完整V2-3→负责人冻结gold输入；D材料→A/E审计→D上传 | D | gold分支通过后B/E可以开始独立标注；D分支可独立完成上传 |
| D5 | B上传冻结资产；C核对内容哈希、最小复现并上传正式报告 | B→C | Git资产与冻结候选一致，V2-3绑定正式提交 |
| D6 | B/E分别封存原始标注并接受盲标资格检查；全部通过后B上传、E拉取并首次完整运行V2，否则按阻塞分支处理 | 条件满足时B→E；否则E只可上传不含原始标注的阻塞证据 | V2-1至V2-7有结论；V2-8按条件记为`PENDING_OWNER_ADJUDICATION`或`BLOCKED` |
| D7 | 阻塞文件所有者条件修复；E复验受影响项；负责人终裁和签署 | 条件修复者→条件E→负责人 | V2-8最终结论和W2 Go/No-Go入库 |

1. 【负责人】登记第二周最终任务包提交为固定`W2_START_COMMIT = f343a6c1c630f362f4686e6f6b0f50c6577d5562`；确认其继承新版统一基线`8785c6e`，全员拉取后关闭第一周遗留门禁，再批准本周来源、数据、诊断和60案例输入范围；
2. 【B】选定并登记官方Pandas文档和必要教程，冻结三章六节、六个核心知识点、`basic-python`辅助点及先修关系；
3. 【B】完成六节讲义骨架、来源锚点、示例、反例、误区和最终任务关系；
4. 【B+C复核】制作一份公开CSV和2至3份私有变体，生成每份数据唯一的 `clean_df`；C使用已知错误样例反向验证；
5. 【B】完成8至12道固定诊断题、私有答案、权重和知识点映射；
6. 【A】实现诊断草稿、逐题提交、跳过、完成、不可变结果快照，以及Evidence聚合、`mastery/confidence`和状态映射；
7. 【A】提前实现 `LearningSessionRepository`最小持久化与事务骨架，固定 `create/getSnapshot/commit/recover`边界；本周不接完整路径提交；
8. 【B+A】冻结 `development-20.jsonl`和 `final-60.jsonl`的案例输入，验证其必填字段、诊断输入和KnowledgeState前置数据可计算；
9. 【B+E】在不查看系统路径或正式输出的条件下，B完成首标、E作为第二标注人完成首批20个正式案例的独立gold标注；通常分歧先协商一次，仍不一致由负责人按预冻结口径终裁。本批首20例按`W2-D7-CLOSEOUT-1`由E生成机械差异清单、负责人直接终裁，不覆盖两份原始标注；其余40个在第3周按D44/`W3-C2/W3-R2`完成独立双标和双封存后，也取消B/E协商，由负责人生成机械差异清单并直接终裁；
10. 【A】创建极小非Pandas v2 smoke fixture，证明schema没有写死Pandas；
11. 【E】跟进契约与单元测试，起草claim拆分表和指标脚本框架；【D】只准备动态题、审核链提示词草稿和录制响应，不接实时模型。

### 7.3 第2周结束前必须冻结

| 标准 | 冻结内容 |
|---|---|
| 知识结构 | 六个核心点、辅助点、先修图、资料包声明顺序和核心标记 |
| 来源 | 正式来源、许可证、锚点、技术冲突裁决和内容摘要 |
| 数据 | 七列顺序、CSV内容、日期格式、城市映射、空值、重复规则和唯一 `clean_df` |
| 诊断与学情 | 题面、答案、权重、知识点覆盖、跳过语义、Evidence权重、算法和状态阈值 |
| 会话事务骨架 | `create/getSnapshot/commit/recover`职责、会话版本和正式写入唯一入口 |
| 案例输入 | `development-20.jsonl`和 `final-60.jsonl`输入及哈希 |
| gold首批 | 20个正式案例的双人标注、分歧处理和哈希；与系统输出隔离 |

### 7.4 本周交付物

- 可加载的 `pandas-cleaning` Profile v2、六节讲义、知识点图和来源锚点；
- 公开CSV、私有变体、参考实现、维护脚本和唯一标准结果；
- 固定诊断题、Evidence聚合器和KnowledgeState计算；
- `LearningSessionRepository`最小持久化与事务骨架；
- 20个开发案例和60个正式案例输入，以及首批20个正式案例gold；
- 非Pandas smoke fixture、测试与第2周验证记录。

### 7.5 验证性测试清单

| # | 测试项 | 执行与复核 | 硬性通过标准 |
|---|---|---|---|
| V2-1 | 新版基线复验 | E执行，负责人复核 | 固定`W2_START_COMMIT = f343a6c1c630f362f4686e6f6b0f50c6577d5562`。D1身份检查要求当时HEAD等于该周起点且`8785c6e`是其祖先，并保存完整基线记录；D6最终回归要求该周起点是当时最新HEAD的祖先、D1记录仍完整有效，并在D6最新HEAD重跑类型检查、全量测试、扩展冒烟和`verify`，全部通过并记录真实项数 |
| V2-2 | Profile与坏包拒绝 | E执行，A复核 | 正常包从干净目录加载；路径穿越、缺失、悬空引用、重复清单和错误revision全部拒绝 |
| V2-3 | 数据唯一性 | C执行，非B成员复核 | 按`W2-V2-3-ENV-1`确认`pandas==3.0.5`后，公开CSV和每份私有变体连续运行3次，`clean_df`逐单元格、dtype、列序和行序一致；已知错误实现全部被识别；环境不匹配只记`environment_mismatch/BLOCKED`。本批E工具变更按`W2-D7-CLOSEOUT-1`由负责人核对旧新差异、六输入约束及26/26作者测试后出具`OWNER_NO_LOWERING_CONFIRMATION`，不要求C重复确认或重跑完整V2-3 |
| V2-4 | 诊断与学情 | E执行，A复核 | 全对、全错、跳过、退出恢复和重复提交均符合合同；无证据为 `unverified`；KnowledgeState输入可稳定复算 |
| V2-5 | 仓储事务骨架 | E执行，A复核 | 四类接口存在；只有 `commit`可公开正式状态；版本冲突和失败不留下半写入 |
| V2-6 | 案例前置数据 | E执行，A复核 | 20个开发案例和60个正式案例的字段、诊断结果与KnowledgeState前置数据均可计算；本项不得调用 `buildPath`或统计路径合法率 |
| V2-7 | 资产隔离 | E执行，负责人复核 | 本批固定扫描safe DTO、由该DTO确定性构造的普通日志、D录制响应、由该DTO确定性构造的安全导出、C的`v2-3-final-report.md`和D的`d4-validation-report.md`；五类表面逐输入六类canary零命中。E验证记录是结果载体，不递归扫描自身；canary、manifest和机器结果仓库外封存，Git只登记哈希、命令、计数和结论 |
| V2-8 | 输入与首批gold冻结 | B和E执行，负责人复核/终裁 | D6前60个案例输入及gold输入资产树已登记哈希；B/E各20例、Schema、冻结绑定、不可变SHA-256和独立性均通过后记为`PENDING_OWNER_ADJUDICATION`。本批E精确开始时间按`W2-D7-CLOSEOUT-1`不作为资格阻塞，但必须保留授权、最早可审计内容和完成时间链；E生成机械差异清单，负责人直接终裁并生成三份正式gold、登记哈希后才可记为`PASS` |

### 7.6 Go/No-Go门禁

Profile必须以`draft`候选从干净目录加载，固定数据必须产生唯一 `clean_df`，诊断必须可保存、退出、恢复和完成，Evidence与KnowledgeState可复算，仓储事务骨架通过测试，60案例输入和首批20个gold已冻结，私有资产不进入安全视图。D4/D5的负责人批准只表示候选可继续验证，不等于Profile已经`active`。D6必须完成V2-1至V2-7，并按双封存和盲标资格检查的真实结果将V2-8登记为`PENDING_OWNER_ADJUDICATION`或`BLOCKED`；V2-8为`BLOCKED`时负责人不得签署W2 GO。D7终裁或修复后V2-8仍未通过则不得进入第3周。本周不以PathEngine执行或路径合法率作为门禁。

## 8. 第3周：确定性闭环主干与前端骨架

> 第三周采用`W3-C5/W3-R2`。负责人已于2026-08-06完成D0签发，唯一`W3_START_COMMIT`为`f190326a4a906b46e4001484ffa30a7839b82ed2`；D45—D47只升级业务合同，不改变调度。D0不占开发日；D1—D5按完整工作日执行，不设置小时级截止点，每个工作日最多两次Git上传。能够单日独立闭环的任务不得人为拆日，正常主链在D4收口，D5只保留给真实阻塞。

> **2026-08-09 D36执行记录**：负责人以[W3-D36-PROFILE-ACTIVATION-1](./第三周任务/W3-D3-负责人Profile-v2激活批准.md)核对B正式TaskBundle、C正式环境锁、固定哈希和提交祖先关系后，书面批准A在拉取批准提交后通过仓储事务激活Pandas Profile v2。该记录不新增D编号、不升级`W3-C4/W3-R2`，不代替A的事务重验、失败回滚、D3交付、上传锁或后续验收。

> **2026-08-09 D47测试裁决**：负责人以[W3-D47-TEST-LAYERS-1](./第三周任务/W3-D3-负责人测试分层与环境兼容裁决.md)冻结确定性、评测器集成和固定轨迹Demo三层验证。Windows构建号只记录；Node/Python/Pandas继续按批准值验证。第三周不运行真实Key，假LLM经同一端口和Schema回放审计冻结的成功/失败轨迹，结果只证明离线模型边界。

> **2026-08-10 D47运行兼容执行**：E的D4复验确认旧适配器仍把Windows完整release作为运行硬门槛。负责人以[W3-D47-RUNTIME-COMPAT-1](./第三周任务/W3-D4-负责人D47运行兼容执行记录.md)落实既有裁决：运行时只校验`win32`、`x64`和进程树终止能力，Node/Python/Pandas继续精确锁定；D40历史锁和`environmentHash`不变。该执行不新增D编号、不改变`W3-C5/W3-R2`或V3标准。

> **2026-08-11 W3最终收口**：E正式提交`330949b51b4a73532b70345131b73a0ab08e0927`进入`main`后，负责人按[W3最终验证记录](./周门禁记录/W3-验证记录.md)复核V3-1至V3-8全部PASS，将D3只读候选经CRLF到LF机械规范化后晋级为三份60例正式gold并更新完整SHA-256；逐案JSON不变，W2前20例Git字节保持不变。批准环境下Web 81/81、全量636通过/1跳过及全部聚合门禁通过，负责人签署`W3_GATE=GO`。该收口不新增D编号、不升级`W3-C5/W3-R2`，不修改B/E原始标注、SDK、依赖或D40历史锁。

### 8.1 本周目标

在没有实时模型、无需网页参与业务判断的条件下跑通“诊断→路径→活动→正式提交→Evidence→下一步”，并同步搭好业务结果只消费冻结DTO、页面缺失字段只使用`src/web`私有fixture的React骨架。第3周是A与C的共同峰值周，但正式事实写入权限必须严格分开。

### 8.2 五日执行与上传顺序

| 工作日 | 完整工作日任务 | 当日Git上传 |
|---|---|---|
| D1 | A一次完成路径、Facade、仓储及V3-1/V3-2并上传；B/E各自完成`final-021—060`全部40例并封存，负责人完成双封存资格检查且PASS后，B闭合两个TaskBundle并上传；C完成环境和故障原型；D完成全部离线Agent材料并封存 | `A→B` |
| D2 | 负责人根据C证据冻结Node环境并生成gold机械差异清单；C拉取B后完成适配器、Rubric和故障矩阵；A按C滚动候选准备正式事务；B/E不查看对方标注、不参加协商；E一次完成React六页、Facade DTO mocks、页面fixtures、本地恢复和前端测试/typecheck/build | `负责人→C` |
| D3 | 负责人核对B/C正式资产后批准Profile激活；A通过仓储事务激活v2并完成正式提交事务、Evidence、KnowledgeState及路径后缀后上传；D上传D1封存材料；负责人终裁gold并形成只读60例候选；E只准备V3工具、输入清单和测试用例，不提前形成完整V3结论 | `A→D` |
| D4 | E完成V3-1至V3-8、全量门禁和验证正文并上传；负责人复核后提交完整60例gold、哈希、验证记录和W3 Go/No-Go | `E→负责人` |
| D5 | 只处理D4遗留的真实阻塞；无阻塞时不开发、不上传，W3已于D4结束 | 条件上传，最多2次 |

D4正常完成`E→负责人`后第三周即结束。若D4存在阻塞，仅需E修正记录时D5按`E→负责人`收口；需要上游修复时D5改为“一个阻塞文件所有者→E”，负责人最终签署转入条件D6。条件D6不开发新功能，只允许负责人提交E提供且哈希不变的复验记录和最终签署；出现多个独立修复上传需求，或D6结束仍未全绿，W3判定NO-GO。

主链不得颠倒：

```text
A完成路径第一阶段；B冻结TaskBundle
→ 负责人冻结环境，C实现执行器和Rubric并返回ActivityResult
→ 负责人批准Profile激活，A激活v2并接入正式提交和Evidence事务；D上传离线材料
→ E独立复验
→ 负责人提交gold并签署
```

并行的gold链为`D1 B/E各自完成40例独立标注→双封存资格检查→D2负责人生成机械差异清单→D3负责人直接终裁并形成只读候选→D4正式60例gold`。B/E不得查看对方原始标注或机械差异清单，不参加协商，不得修改封存原件；标注者不得在正式gold冻结前查看系统对60例的路径或正式输出。

### 8.3 第3周结束前必须冻结

- 路径排序、状态转换、原因码、不可行响应、`skipEligible`和知识状态阈值；
- Evidence权重、提示折扣、题组相关性，以及正式写入只经A负责事务仓储的边界；
- Rubric六维权重、总分0.80通过线、阻断测试和 `ActivityResult`合同；
- `requestId + attemptId`幂等语义，Attempt、Evidence、知识状态、路径和会话版本的提交顺序；
- 学习者错误与评测器错误分类、Node执行器实测开发期限制；
- HTTP安全DTO、核心公共类型和接口，此后只允许向后兼容修正；
- 60个正式案例gold全部双标完成并登记哈希；
- Node正式提交环境状态可使用`measured_node_submit`；Node、Python、Pandas和限制值必须来自C原型证据并由负责人在D2批准。2026-08-08负责人已以[W3-D40-ENV-1](./第三周任务/W3-D2-负责人Node环境裁决.md)批准C的D1实测Node正式提交值；Pyodide、可靠内存限制、网络隔离和`measured_dual_backend`均未获批准。
- D47三层测试口径：确定性层不启动Python；V3-3必须在评测器集成层和批准的Node/Python/Pandas环境中执行；固定轨迹Demo由假LLM回放，不使用真实Key。Windows只硬校验`win32`、`x64`和批准能力，构建号仅记录。
- Pandas revision 2只能由负责人批准后通过仓储事务从`draft`激活，禁止手工移动或改写active目录；新会话只消费实际active revision。

### 8.4 本周交付物

- PathEngine、完整会话仓储和可用Facade；
- 固定代码补全、最终实操、TaskBundle和Rubric计算器；
- 只返回 `ActivityResult`的Node.js/Python正式评测适配器；
- 由事务仓储提交的Attempt、Evidence、KnowledgeState、路径和恢复机制；
- 无需网页即可运行的端到端闭环脚本、React骨架、冻结安全DTO、仅限`src/web`的页面展示fixtures和60案例gold；
- `b-final-021-060.jsonl`、`e-final-021-060.jsonl`两份不可覆盖的原始标注，以及按稳定顺序追加并登记完整哈希的三份正式gold。

### 8.5 验证性测试清单

D45固定使用同一批20例执行两套相互隔离且均可复算的输入快照：V3-1保留原始诊断、KnowledgeState、目标和模式，仅把验证投影预算固定为180分钟；V3-2恢复案例原始30/60/90/120/150分钟预算，并增加A自有“全部掌握”“缺失先修”边界fixture。原文件、生成脚本和第二周冻结哈希不得修改。

| # | 测试项 | 执行与复核 | 硬性通过标准 |
|---|---|---|---|
| V3-1 | 路径确定性与合法率 | E执行，A复核 | 20个开发案例按`w3-v3-feasible-180-v1`投影，各连续运行10次；candidate必须为20/20，同一投影输入快照逐字段一致，先修、必做/最终活动、难度/脚手架和预算均合法，实际路径合法率100% |
| V3-2 | 路径边界 | E执行，A复核 | 20例原始预算逐例稳定返回结构正确的`path_infeasible`，结构正确率100%；“全部掌握”“缺失先修”两个A自有fixture均产生确定结果；非法路径为0 |
| V3-3 | 执行器判分 | E执行，C复核 | 正确、部分正确、语法错、运行错、测试失败、超时、超输出、禁用导入分类正确；`act-inspect-dataframe`和`act-practical`各连续3次逐字段一致 |
| V3-4 | 权限分离 | E执行，A和C交叉复核 | C的调用输出止于 `ActivityResult`；C源码无正式Attempt/Evidence写入入口；正式状态只由仓储 `commit`公开 |
| V3-5 | 幂等与崩溃恢复 | E执行，A复核 | 同一 `requestId+attemptId`不重复计分；各提交阶段注入中断后无半写入、无重复Attempt/Evidence |
| V3-6 | 评测器故障 | E执行，C复核 | 依赖、环境、测试资产或协议故障只归为评测器故障，不生成负Evidence |
| V3-7 | gold冻结 | B和E独立双标，负责人复核/直接终裁 | 60/60双标、负责人直接终裁和完整哈希均在任何正式60案例系统运行前冻结；B/E不协商，前20例不变 |
| V3-8 | DTO与前端骨架 | A和E执行，负责人复核 | 六页面可达；Facade DTO mocks逐字段绑定21号类型，页面fixtures仅位于`src/web`且双组泄漏扫描为零；覆盖空、错、冲突和恢复状态，草稿不丢失；前端无业务判定 |

### 8.6 Go/No-Go门禁

应用层无需网页即可完成确定性闭环；20例180分钟可行性投影candidate为20/20且实际路径合法率100%，20例原始预算不可行结果结构正确率100%；两个冻结任务各连续三次结果一致；C源码和输出止于公共`ActivityResult`；正式事实只由A事务仓储公开；评测器异常不产生负Evidence；DTO、六页mock和60案例gold均冻结。最终必须通过typecheck、全量测试、文档检查、extension smoke、release check、`verify`和`git diff --check`。任何V3项为BLOCKED、周起点未登记、Profile未通过事务激活或正式gold未冻结，负责人不得签署GO。

## 9. 第4周（8/10–8/16）：模型接入与页面接真数据

### 9.1 本周目标

在确定性主干之上接入动态客观题、串行Agent、Pandas五维画像、本地HTTP和真实数据页面。模型、Agent或页面失败不得破坏固定学习闭环；CIDPP仅在主链全绿后实施，不进入本周门禁。

### 9.2 任务顺序

1. 【D】实现AI动态生成单选/判断候选，由程序校验题型、选项、知识点、来源和 `correct_answer`；
2. 【D】按D15接入OpenAI兼容模型配置：`OPENAI_MODEL`默认`deepseek-chat`，Base URL和API Key由宿主环境提供；应用冻结角色温度、60秒超时、2次结构重试和10万token会话预算；
3. 【D+A+B】完成4至6题题组、75%向上取整、缓存、每点完整fixed组和1至2道supplemental补位题；
4. 【D】实现Generator→Hunter→条件Defender→Judge串行编排、检查点、有限重试、fallback和可复查轨迹；
5. 【D+A】接入确定性知识点底座之上的Pandas五维画像快照；失败保留旧快照，不修改mastery、路径、代码分数或Rubric；
6. 【A】按第3周冻结DTO实现本地HTTP，每个资源化路由只调用一个Facade短用例；
7. 【E】把开始、诊断、路径、学习、活动和总结六个页面从mock切换到真实API，统一加载、空、错误、冲突和恢复状态；
8. 【E+D】使用负责人审计冻结的录制响应，通过同一模型端口和Schema运行成功及固定失败集成测试；不使用真实Key，不把mock结果写成在线模型能力；
9. 【E】用 `development-20`少量产物试跑claim拆分、来源对照和指标统计流水线；
10. 【D，可选】只有上述主链全绿后，才实现单一CIDPP内容质量Agent、最多一次优化和超时降级；
11. 【自动裁决】W4周日18:00检查V4-1至V4-7；任一未通过或CIDPP尚未完成时自动关闭运行时CIDPP，只保留构建期摘要，无需再次开会。

### 9.3 第4周结束前必须冻结

- 动态题结构、缓存键、失效条件、完整fixed组、supplemental补位和`insufficient`语义；
- Hunter/Judge触发条件、角色权限、Agent超时、无效结构、拒绝和恢复语义；
- Web API、安全DTO、版本冲突、重复请求和页面状态语义；
- 五维画像的证据忠实、版本幂等、失败保留旧快照和越权拦截规则；
- CIDPP的实际状态只能是“V4全绿且已接入后保留”或“按D17自动关闭”，不得继续作为未决阻塞项。

### 9.4 本周交付物

- 动态题与题组服务、缓存、完整fixed组和supplemental补位；
- 串行审核编排器、检查点和轨迹；
- 五维画像快照更新服务；
- 本地Node HTTP接口和接入真实数据的六个主页面；
- 指标复核流水线原型和CIDPP裁决记录；若未启用CIDPP，不要求运行时CIDPP服务。

### 9.5 验证性测试清单

| # | 测试项 | 执行与复核 | 硬性通过标准 |
|---|---|---|---|
| V4-1 | 动态题结构与判分权 | E执行，A复核 | 不合格结构100%被校验器拒绝；正确与错误答案均由程序比较 `correct_answer`，模型无判分权 |
| V4-2 | 模型故障降级 | E执行，D复核 | 超时、无效结构、答案矛盾、来源缺失均触发有限重试或完整fixed组，学习不中断；完整fixed组已经使用且无法形成不重复重试组时明确返回`insufficient` |
| V4-3 | 串行链与越权 | E执行，A和D复核 | 审核轨迹完整且角色严格串行；Agent尝试修改答案、路径、分数或Evidence时全部被拒绝 |
| V4-4 | 五维画像安全 | E执行，A复核 | 成功快照引用真实Evidence并推进版本；无证据维度为 `unverified`；失败保留旧快照且不影响确定性事实 |
| V4-5 | 页面真数据闭环 | E执行，负责人复核 | 干净目录完成开始、问卷、诊断、路径确认、进入并提交一个活动；诊断草稿、题组Attempt和提交后刷新均从服务端恢复 |
| V4-6 | 泄漏扫描 | E执行，负责人复核 | 前端bundle、DOM、HTTP响应、普通日志和Agent上下文中，隐藏测试、参考实现、诊断答案、密钥和绝对路径零命中 |
| V4-7 | 双轨集成与模型配置 | E执行，D复核 | 录制响应轨全绿；真实模型轨使用三个`OPENAI_*`变量和冻结参数，无崩溃、卡死、超预算追加或未降级的流程级失败 |

### 9.6 Go/No-Go门禁

全部模型调用失败时固定主链仍可运行；动态题由程序判分；串行轨迹可复查且Agent不能越权；网页能够通过真实API完成开始、诊断、路径确认和进入活动；泄漏扫描通过。运行时CIDPP按D48固定关闭，不再等待周日自动裁决。

### 9.7 W4-C2 D0执行覆盖（2026-08-11）

第四周从W3 GO提交`ac6e307e17cf84450845dfc5ffa467063dd3ae4c`开始，现行合同为`W4-C2/W4-R1`。D48—D60已经在开发前冻结9.3所列内容及题组提交、刷新恢复、A/D端口和revision 3激活，因此9.2的实现顺序改由41—47号任务书执行；本节与42号公共合同优先于本章先前的W4概述，但不修改W1—W3历史验收。

本周正常主链为：A完成公共类型、题组确定性服务、活动进度、A/D端口、激活仓储和事务；B完成revision 3六知识点卡片、题组、私有fallback与seal；D实现两个端口、录制响应、动态内容审核、私有缓存和异步画像；C在上述正式输入上完成Bootstrap、Node HTTP、端口组装和统一Demo启动；E接入六页真实API并独立执行V4。运行时CIDPP从D0起固定关闭，只保留`development-20`构建期评价原型，不再等待周日自动裁决。

问卷、入口、题组和会话规则以D49—D53为准：两种入口均填写三项背景字段；章节模式使用`background_only`完成诊断并仍完成全目标；revision 3核心点按卡片、mcq、代码补全顺序完成；有效题组4—6题、75%向上取整、一次不重题重试；`insufficient`推进但不生成Evidence；正式提交的Evidence、KnowledgeState、路径、游标与checkpoint必须原子公开。

Web主链必须从干净`.demo-data`真实调用本地API，完成启动、问卷、推荐诊断或章节直达、建路、确认、打开并提交题组、刷新恢复和下一活动推进。`npm run demo`只使用录制响应；真实Key只允许`npm run demo:live`可选smoke，不属于GO门禁。Vite根必须收窄，且fixtures、private、hidden、Rubric和参考实现不能通过开发服务器直链。

| # | 新增或覆盖的测试项 | 执行与复核 | 硬性通过标准 |
|---|---|---|---|
| V4-7 | 录制响应与可选在线smoke | E执行，D复核 | 录制响应覆盖成功、非法Schema、超时、provider错误、高风险审核和越权拒绝且全绿；未配置真实Key登记`LIVE_NOT_RUN`，不得判失败或伪称在线通过 |
| V4-8 | 六知识点题组真实可实现性 | E执行，B和A复核 | 六个核心点均有固定卡片和mcq；每个有效题组4—6题；`score=correctCount/totalCount`且阈值可复算；一次重试使用新Attempt且题目不重合；`insufficient`不造Evidence但推进；打开阶段私有答案在HTTP、DOM、bundle、日志和Agent上下文零泄漏，提交后复盘只含白名单字段 |

W4 GO要求V4-1至V4-8全部PASS；业务功能、录制响应和安全扫描必须分别有命令与证据。W4网页主链使用客观题，不因本机Python环境阻断；Python评测器继续按独立集成层验证。Windows构建号只记录，Node/Python/Pandas能力仍按既有分层合同判定。

## 10. 第5周（8/17–8/23）：产品闭环、双后端与评测彩排

### 10.1 本周目标

把已验证能力串成无需人工修改后台文件的本地产品，完成双后端取舍、环境冻结、故障验证和整套评测彩排。第一次运行评测全流程不能留到第6周。

### 10.2 任务顺序

1. 【E】补全资料包选择、背景问卷、章节、反馈、画像更新和恢复页面；
2. 【C+E】接入Pyodide Worker公开预览，只使用公开数据和公开测试，支持取消、超时和输出裁剪；公开运行不得创建正式Attempt或Evidence；
3. 【负责人，第3天】主持Pyodide Go/No-Go：若仍不稳定，立即关闭公开预览并显示明确提示，保留Node正式提交；此降级不得拖延后续故障测试和彩排；
4. 【A+E】完成TUI深链、pending活动和Web/TUI共享会话；
5. 【C】验证临时目录清理、Windows进程树终止、版本冲突、重复提交和服务重启恢复；比较Pyodide与Node同任务结果；
6. 【C】用实测结果冻结 `environment-lock.json`和环境能力标志；不得凭开发机现值或猜测填写；
7. 【B+A】完成高基础、非计算机初学者、实践导向三个展示案例；
8. 【E】运行失败注入、安全扫描、v1回归和干净目录端到端测试；
9. 【全体】使用 `development-20`完整彩排“自动测试→案例评测→幻觉复核→盲评→指标计算→报告生成”，生成彩排版 `metrics.json`并记录单案例人工复核耗时；
10. 【负责人+E+B+D】负责人组织组内盲评轮换，E完成至少6名试用者预约和匿名化方案，B完成PPT大纲，D完成视频分镜，E完善前后测、迁移题和演示脚本；
11. 【Monaco条件项】W5周五18:00前预跑V5-1至V5-8；全部通过且E书面确认有余力时，周六可进行不改变后端、DTO或评测链的纯前端替换；周日完整复验失败即恢复`textarea`并关闭该项。

### 10.3 第5周结束前必须冻结

| 项目 | 冻结内容 |
|---|---|
| 运行环境 | Node.js、Python、Pandas、Pyodide版本和允许库 |
| 资源限制 | 代码、数据、输出、超时上限及可靠内存限制标志 |
| 浏览器能力 | Pyodide启用或关闭的负责人裁决、允许/禁止能力、取消和迟到消息规则 |
| Windows执行 | 进程树终止方式、临时目录和残留检测 |
| 双后端差异 | Node权威、Pyodide差异提示和不可用降级 |
| 页面文案 | 加载、空、冲突、模型失败、评测器失败和恢复提示 |
| 发布边界 | 已知限制、降级功能、候选状态和候选Profile revision |
| 评测准备 | development-20彩排结果、组内交叉回避的试用者与盲评排期、B的PPT大纲和D的视频分镜 |
| 编辑器 | `textarea`为P0；Monaco只能按D16周五预门禁和周日复验结果登记启用或关闭 |

### 10.4 本周交付物

- 完整React主流程、Node权威提交，以及经裁决启用的Pyodide预览或明确关闭状态；
- TUI/Web共享会话和 `environment-lock.json`；
- 三个展示案例、故障注入、安全扫描和端到端结果；
- `development-20`全流程彩排记录、彩排版 `metrics.json`和问题责任表；
- 试用者与盲评安排、前后测/迁移题、PPT大纲、演示脚本和视频分镜。

### 10.5 验证性测试清单

| # | 测试项 | 执行与复核 | 硬性通过标准 |
|---|---|---|---|
| V5-1 | 干净目录闭环 | E执行，负责人复核 | 纯Web和TUI深链各完整运行一次，无需改后台文件；刷新后可恢复 |
| V5-2 | 三案例差异 | E执行，负责人复核 | 三条路径合法率100%，每对案例至少有3项可观察的路径、难度、脚手架或表达差异 |
| V5-3 | 双后端与降级 | E执行，A复核 | Node同提交连续3次一致；Pyodide启用时差异可解释且公开运行零正式Evidence；关闭时页面无死入口且Node正式提交可用 |
| V5-4 | 故障矩阵 | E执行，A和C复核 | 刷新、重启、模型失败、Pyodide失败、磁盘写失败和进程残留均可恢复或明确降级，无半写入 |
| V5-5 | 环境锁依据 | C执行，负责人复核 | 每个版本和阈值均能指向原型、压力测试或故障记录；不可靠能力明确标为不支持 |
| V5-6 | 评测彩排 | E主持，全体参与，负责人复核 | `development-20`六个环节全部走通，指标分子分母可人工复算，人工耗时有记录，流程缺陷均有责任人 |
| V5-7 | 安全与回归 | E执行，负责人复核 | 全量测试、v1回归和泄漏扫描通过，敏感资产零命中 |
| V5-8 | 人员与材料 | E/B/D执行，负责人复核 | E确认至少6名试用者；负责人完成组内盲评回避安排；B的PPT大纲和D的视频分镜覆盖完整闭环 |

### 10.6 Go/No-Go门禁

干净目录可完成Pandas闭环；三案例路径不同且合法；刷新、重启和模型失败可恢复；隐藏资产扫描通过；`development-20`彩排全流程走通且指标可计算；第6周人员与材料安排明确。Pyodide可按第3天裁决关闭，不单独阻塞进入第6周。进入第6周后禁止新增功能。

## 11. 第6周（8/24–8/30）：冻结、正式评测、试测与交付物

### 11.1 本周目标与按天执行表

第6周先冻结再评测，试用、盲评、自动测试、报告和演示材料并行推进。只允许阻塞性修复，不得新增功能。

| 天 | 任务顺序 |
|---|---|
| D1 | 负责人冻结Profile revision、来源、诊断、任务、测试、Rubric、环境锁、提示版本、HTTP/错误合同、正式评测集、人工标准及全部哈希；开展第一批试用 |
| D2 | E在冻结候选版运行完整自动测试；开展第二批试用；任何红项先登记，不得边测边改标准 |
| D3 | 启动60案例正式评测和20组匿名配对盲评；完成试用并回收前测、后测和迁移题数据 |
| D4-D5 | 完成案例复核、盲评回收、三案例定稿及幻觉率、难度适配、覆盖率、路径合法率计算；D并行录制视频初版，B回填PPT |
| D6 | 仅按固定优先级处理阻塞性修复；受影响结果必须使用新revision或新 `evaluationId`重跑；D补录必要视频镜头 |
| D7 | 定稿 `metrics.json`、指标报告、限制报告、证据索引、视频、源码与安装说明 |

### 11.2 冻结与修复纪律

冻结后只允许修复导致发布门禁无法验证或主链无法运行的数据/来源错误、安全问题、路径非法、判分错误、恢复失败、执行器故障和页面主流程阻塞。修复顺序固定为：

```text
数据/来源错误
→ 路径非法
→ 判分错误
→ 隐藏资产泄漏
→ 恢复失败
→ 执行器崩溃或错误归因
→ 页面主流程阻塞
```

非关键样式不在第6周修复范围。任何修复都必须创建新revision、版本或 `evaluationId`；不得覆盖旧结果、修改人工标准、删除失败或不利样本、改变指标分母，或根据正式结果反向调整gold。仍未达标时如实写入限制报告。

### 11.3 正式评测范围

- 60个冻结正式案例和20组多智能体配对盲评；
- 三个完整差异化案例；
- 核心知识点覆盖率、专业知识幻觉率、难度适配准确率和路径合法率；
- 代码评测正确性、可复算性和错误归因；
- 6至10名真实学习者的前测、系统学习、后测和迁移任务；
- 类型、单元、契约、执行器、双后端、恢复、冲突、幂等、泄漏、v1回归和扩展冒烟。

### 11.4 本周交付物

- 可运行源码、安装说明、Pandas Profile v2和非Pandas smoke fixture；
- 自动测试结果、60案例、3个展示案例和20组盲评记录；
- 匿名试用及探索性学习效果报告；
- `metrics.json`、中文指标报告、限制报告和证据索引；
- 设计文档、第三方许可证和模型接口替换说明；
- 8至10分钟演示视频、PPT、演示脚本和故障兜底脚本。

### 11.5 验证性测试清单

| # | 测试项 | 执行与复核 | 硬性通过标准 |
|---|---|---|---|
| V6-1 | 候选冻结 | 负责人执行，A复核 | 冻结清单无空项且全部登记版本/哈希；修复后旧结果零覆盖 |
| V6-2 | 正式自动测试 | E执行，负责人复核 | 类型、单元、契约、执行器、双后端、恢复、泄漏、v1回归和冒烟全绿 |
| V6-3 | 正式指标 | E执行，各领域负责人交叉复核 | 幻觉率小于5%、难度适配不低于85%、覆盖率不低于90%、路径合法率100%，分子分母可独立复算 |
| V6-4 | 盲评完整性 | E组织，负责人复核 | 20组全部回收后才解盲；样本不中途替换，负向结果不删除 |
| V6-5 | 真人试测 | E执行，负责人复核 | 至少6人的前测、学习、后测和迁移题记录齐全；退出记录保留；只报告可用性和趋势，不声称因果 |
| V6-6 | 交付一致性 | 负责人和未参与撰写的成员 | 报告、PPT、视频与 `metrics.json`数字零出入；状态表述真实；视频不超过10分钟 |

### 11.6 Go/No-Go门禁

以本文件“正式发布门禁”为唯一指标口径。未达标项只能按修复纪律处理或写入限制报告；不得通过修改标准、样本或分母制造通过结论。第6周结束只形成可提交候选，不把第7周算作功能开发时间。

## 12. 第7周（8/31–9/4）：提交缓冲周（不计入六周MVP开发）

### 12.1 允许范围与执行顺序

第7周只允许部署复现、打包、核对、上传和送达留证，禁止新增功能、改变公共合同、调整评分规则或借提交名义继续开发。

1. 在干净环境仅按部署说明复现安装和演示主链，补正文档缺失步骤后重新验证；
2. 打包设计实现方案、作品介绍、PPT、不超过10分钟的视频、源码与权限、部署说明、单元测试用例、知识库切片及至少2组差异化学情完整输入输出；
3. 按要求命名压缩包，核对报名表与系统申报信息，实际验证仓库评审权限；
4. 文件过大时上传云盘并从另一设备验证链接与提取码，保留含时间截图；
5. 在9月5日前发送指定邮箱并确认送达；留档最终tag、评测运行编号、包哈希和全部凭证。

### 12.2 本周交付物与门禁

交付物为最终提交包、权限核对记录、报名信息核对表、部署复现记录、上传或发送凭证。本周门禁是提交材料完整、干净环境可复现、信息零差异、链接可访问并在截止前送达；任何门禁失败只能修复交付材料或部署说明，不得回到功能开发。

### 12.3 提交前验证清单

| # | 核查项 | 执行与复核 | 硬性通过标准 |
|---|---|---|---|
| V7-1 | 干净环境部署 | 最少参与开发的成员执行，负责人复核 | 不看源码、不求助开发者，仅按说明可完成安装和主链演示 |
| V7-2 | 提交包完整性 | 负责人和E | 赛题要求逐项齐全，仓库权限已实际登录验证，包名和哈希正确 |
| V7-3 | 报名信息 | 负责人执行，第二人复核 | 报名表与系统申报逐字段零差异 |
| V7-4 | 送达 | 负责人 | 9月5日前有回执或发送记录；云盘链接经另一设备验证可下载 |

## 13. 岗位负载与峰值错开

| 岗位 | W2 | W3 | W4 | W5 | W6 | W7 |
|---|---|---|---|---|---|---|
| 负责人 | 批准来源、数据、诊断和案例输入 | 冻结路径、DTO、gold和执行器原型边界 | 审批模型配置，裁决CIDPP启用或关闭 | 主持联调门禁、评测彩排和发布候选选择 | 冻结资产、监督评测诚信、审批阻塞修复 | 核对提交包、权限、报名信息和送达凭证 |
| A 领域与架构 | 诊断、学情及仓储事务骨架 | **路径、Facade、仓储和正式提交** | HTTP、题组和画像边界 | 共享会话和展示案例 | 修复复核 | 部署与合同核对 |
| B 资料包与内容 | **讲义、数据、诊断题和首批gold** | TaskBundle和剩余gold | 内容事实响应 | 展示案例和来源终检 | 内容评审和盲评 | 资料资产核对 |
| C 执行器与后端 | 参考实现反向验证 | **执行器、Rubric和ActivityResult** | 执行器加固 | Pyodide、环境锁和故障验证 | 修复复测 | 部署环境复核 |
| D Agent与模型 | 提示词和录制样本 | 离线联调 | **动态题、审核链和画像** | 缓存与降级打磨 | 盲评资产和轨迹 | 模型配置核对 |
| E 网页与测试 | 测试和评测工具起草 | React骨架和DTO | 页面接真数据和复核流水线 | **产品收尾和评测彩排** | 正式评测执行 | 打包、复现与送达核对 |

项目峰值依次为：`B（W2）→ A/C（W3）→ D（W4）→ E（W5）→ 全体评测（W6）`。负责人按该顺序保护峰值岗位不被无关跨组事务打断；W7没有功能开发峰值。

## 14. 每周统一完成定义

一项功能只有同时满足以下条件才可标记“完成”：

1. 真实类型或schema已经定义；
2. 统一应用入口已经接入；
3. 正常、空、错误、重试和恢复行为明确；
4. 至少有契约测试或单元测试；
5. 使用真实fixture或受控测试资产；
6. 敏感数据边界经过检查；
7. 不破坏Profile v1和现有Pi命令；
8. 对应文档状态与代码事实一致；
9. 在干净临时数据目录可以重复运行；
10. 失败时有确定性降级或明确阻断，而不是留下半写入状态。

只有页面或演示截图、只有接口空壳、只有AI输出、只有测试但没有实际入口，均不能算完成功能。

## 15. 正式发布门禁

发布候选必须同时满足：

- 专业知识幻觉率小于5%；
- 难度适配准确率不低于85%；
- 核心知识点覆盖率不低于90%；
- 60个正式案例路径合法率100%；
- Profile v1回归和Profile v2契约测试通过；
- 客观题、代码任务和Rubric判分可复算；
- 隐藏测试、参考实现、诊断答案和密钥不进入前端、普通日志或Agent上下文；
- 评测器异常不产生负学习证据；
- 重复提交不重复计分；
- 干净数据目录无需人工改文件可走完闭环；
- 所有未达标、降级和本地安全边界如实写入限制报告。

## 16. 最终演示顺序

在8至10分钟内展示：Profile v1/v2兼容→选择Pandas目标→固定诊断→三类画像和合法路径差异→学习卡片及随时提问→动态客观题及固定降级说明→串行审核轨迹→代码公开运行和正式隐藏测试→错误解释与重试→Evidence更新和路径后缀变化→恢复→指标报告。

第二个非Pandas fixture只证明Profile v2没有写死Pandas，不宣称完成第二个垂直领域。演示中不得宣称生产级安全沙箱、正式教育测量认证或大样本因果学习效果。

## 17. 默认假设

- 团队为4至5人，中等投入；
- 模型接口保留可替换配置，但自动测试和现场演示使用负责人审计冻结的录制响应或固定适配器，不使用真实Key；
- Pandas正式来源在第2周前半选定并登记；
- 前端第3周搭建骨架，第4周接入真实数据，第5周完成主流程；
- 第6周可以安排人工评审和6至10名试用者；
- Pandas代码补全和独立实操固定使用 `profile_fixed`；
- 完整AI代码题生成不属于六周交付；
- 除D33仅用于第二周V2-3复现的`pandas==3.0.5`临时审计基线外，最终精确环境版本和资源阈值只能通过第3至5周原型冻结；临时审计基线不得直接写成成品运行环境锁。
