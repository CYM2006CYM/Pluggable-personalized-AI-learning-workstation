# 27 第二周总任务布置与权限边界

状态：`[负责人正式签发]`。执行周期：2026-07-27至2026-08-02，共7天。

决策生效边界：D01—D14是第一周岗位任务的验收依据；D15—D27均从本第二周任务包签发后生效。D15—D27不得追溯用于判定A、B、C、D、E的第一周交付存在缺陷，也不得要求成员重写第一周交接单。第一周产物与新决策之间的差异统一登记为第二周迁移、约束或后续周责任。

## 1. 第二周目标

本周把第一周的公共骨架和Pandas候选资产推进为可加载、可校验、可复算的确定性事实底座：完成Profile revision 2迁移、固定诊断、Evidence与KnowledgeState、会话持久化事务骨架、唯一清洗结果、案例输入和首批20个gold双标。

本周不实现PathEngine，不调用`buildPath`，不统计路径合法率，不接真实模型主链，不开发React页面，也不提前启动第三周任务。

## 2. 权威文档与阅读顺序

所有成员按以下顺序阅读：

1. [20-第一周开发前负责人决策冻结清单](../第一周任务/20-第一周开发前负责人决策冻结清单.md)：D01-D27负责人决策；
2. [21-第一周公共合同总册](../第一周任务/21-第一周公共合同总册.md)：跨周公共类型、Facade、仓储和错误码；
3. [28-第二周公共合同总册](./28-第二周公共合同总册.md)：本周新增Schema、事务、迁移、案例和gold合同；
4. [06-六周MVP范围与验收](../06-六周MVP范围与验收.md)：W2范围、V2-1至V2-8和Go/No-Go；
5. 本文与本人岗位任务书；
6. 05、07、11号专项文档仅用于对应岗位的专业细节。

桌面`六周任务计划/第二周任务/`只保留为来源草稿，不是执行依据。历史交付报告只证明当时事实，不能覆盖当前合同；反过来，当前合同也不能倒推改判第一周已经完成的交付。

## 3. 统一红线

- 成员不得修改20、21、27、28号或其他总规范；合同缺口必须提交问题单，由负责人书面裁决并更新权威文档。
- 不得自行增加公共字段、错误码、Facade方法、Profile核心字段、评分规则、Evidence写入口或依赖。
- A以外岗位不得修改A的公共类型、诊断/学情逻辑和事务仓储；B以外岗位不得修改Pandas资产正文；C不得替B修数据；D不得接真实模型；E不得替其他岗位修实现。
- 诊断答案、私有CSV路径、参考实现、隐藏测试、密钥、完整学习者代码不得进入安全DTO、前端、普通日志、模型上下文或gold文件。
- `ActivityRepository`、Agent和评测器不得公开正式Evidence；本周只有`LearningSessionRepository.commit`可以原子公开诊断Evidence和KnowledgeState。
- 不运行`npm audit fix`，不修改`allowScripts`、依赖版本、SDK哈希、`package.json`或`package-lock.json`。
- B和E在首批20例gold独立标注完成前，不得查看系统路径或正式系统输出；两份原始标注不得互相覆盖。
- 本周任何提前完成时间只用于补测试、审计和文档，不得转做W3功能。
- 第一周实现与D15—D27不一致时，记录为W2迁移项或后续周待办，不写成W1缺陷；本周未授权实现的功能继续按06号排期执行。

## 4. 文件所有权

| 岗位 | 本周可修改范围 | 对其他范围的权限 |
|---|---|---|
| 负责人 | 20、21、27、28号及必要的权威文档同步；周门禁签署 | 裁决、复核；不代替成员实现 |
| A | `src/domain/`、`src/application/`、`src/repositories/learning-session-repository.ts`内本周诊断/学情/事务实现，A作者测试和非Pandas smoke fixture | 只读审计B资产和D提示词；不得改B/C/D/E文件 |
| B | `fixtures/profiles/pandas-cleaning-v2-draft/`、`evaluation/personas/`、`evaluation/golden/annotations/b-first-20.jsonl`及B作者测试/资产报告 | 只读消费公共Schema；不得改A代码和C报告 |
| C | 数据反向验证脚本、C作者测试和C验证报告 | 只读运行B资产；不得直接修B资产或修改评测公共协议 |
| D | 本周离线提示词草稿、脱敏录制响应和配置说明 | 只读消费合同；不得修改模型运行端口或接真实模型 |
| E | E验证脚本、`evaluation/golden/annotations/e-first-20.jsonl`和`周门禁记录/W2-验证记录.md` | 只读复验全仓；不得修其他岗位产物 |

新增文件的确切路径必须在交接单列明。两个岗位需要修改同一文件时先停工，由负责人重新指定唯一所有者。

## 5. 单仓库上传规则

继续使用一个仓库、默认分支、无个人分支和一把上传锁。正常流程中每个岗位只安排一次正式上传；B为保证B/E原始gold互不可见，固定分成D5资产上传和D6原始标注上传两次，这是唯一预先批准的例外。所有上传严格串行。

```text
申请上传锁
→ 负责人确认当前持锁人为“无”
→ git status确认只含本人范围
→ 【拉取】默认分支最新提交
→ 检查同路径冲突
→ 重跑本人最小测试和受影响回归
→ 只暂存本人交付
→ commit并【上传】
→ 报告提交号、测试和文件清单
→ 负责人确认后释放上传锁
```

拉取到新提交后必须重新测试。发现同文件冲突、公共合同冲突或远端变化时停止上传，不得强推、覆盖或删除他人文件。

## 6. 七天依赖顺序

总依赖固定为：

```text
负责人签发任务包
→ E复验统一基线
→ A完成诊断、学情和事务仓储
→ B完成revision 2资料包、案例输入和B首标
→ C完成数据反向验证，D完成离线准备
→ E完成独立首标和V2综合复验
→ 负责人终裁分歧、冻结哈希并Go/No-Go
```

| 天 | 严格先后顺序 | 主上传 | 负责人 | A | B | C | D | E | 当日门禁 |
|---|---|---|---|---|---|---|---|---|---|
| D1 启动与基线 | ①负责人`【上传】`27-33号 → ②全员`【拉取】` → ③E复验基线并当日反馈 → ④负责人确认开工 | 负责人 | 签发D22-D27、合同和任务书；确认上传锁、文件所有权和W1遗留状态 | 阅读合同、列实现清单，不上传 | 清点revision 1迁移范围，不上传 | 准备数据反向验证矩阵，不上传 | 准备离线交付目录，不上传 | 在`8785c6e`执行V2-1，结果先发负责人，最终随D7记录上传 | V2-1必须通过；失败只修基线，不进入业务实现 |
| D2-D3 A主实现 | ①A基于D1最新提交实现 → ②E本地预检 → ③A吸收阻塞意见 → ④D3末A`【拉取】`并`【上传】` | A | 当日处理公共合同问题单；不得让成员口头补字段 | 一次完成诊断状态、Evidence候选、KnowledgeState、事务仓储和smoke fixture；D3上传 | 在B自有目录本地制作revision 2资产，不上传 | 只读准备，不修改A文件 | 本地写提示词草稿，不上传 | 对A候选执行V2-4/V2-5预检，只反馈不上传 | 诊断首次提交锁定、完成批量提交、无证据`unverified`、失败无半写入 |
| D4 B定稿与只读审计 | ①B`【拉取】`A → ②B完成候选包 → ③A/C/E只读审计 → ④B只修本人资产 | 无 | 受理来源、Schema和迁移问题单 | 审计Profile闭合和诊断消费字段 | 完成revision 2、来源、讲义、诊断、数据、参考结果和案例输入；B首标保留在仓库外私有工作区 | 只读检查数据可运行性和反例覆盖 | 完成离线样本候选 | 预跑V2-2/V2-6/V2-7，只反馈 | 旧`kp-*`零残留；revision、题面/答案、引用和私有域闭合 |
| D5 B资产上传 | ①B吸收D4意见 → ②B全量自检 → ③B`【拉取】`A最新 → ④B`【上传】`资产和案例输入 → ⑤负责人确认提交 | B | 预审来源和资产范围；确认不将draft误设为active | 最终只读核对Schema | 上传B资产、案例输入和作者测试；不得上传B首标 | 等B提交后准备D6运行 | 等待，不上传 | 等B提交后准备D6复验 | V2-2、V2-6前置通过；60案例输入可计算且已登记候选哈希 |
| D6 反向验证与独立标注 | ①C/D/E`【拉取】`B的D5提交 → ②C运行V2-3并`【上传】`报告 → ③D`【拉取】`C并`【上传】`离线准备 → ④B/E各自完成首批20例并分别向负责人登记文件哈希 → ⑤负责人确认两人都已封存 → ⑥B`【拉取】`D并`【上传】`B原始标注 | C后D后B | 接收数据阻塞；核对B/E完成时间和哈希后才允许B标注入库 | 只读复核C报告 | 独立完成并封存B首标；获得负责人许可后单独上传原始标注 | 每份数据3次、已知错误实现全拒绝；上传可复现报告 | 上传离线提示词、脱敏响应和配置说明 | 独立完成E首标并先封存哈希；在封存前不得拉取B标注 | V2-3通过；B/E原始标注均已独立封存，比较前哈希已登记 |
| D7 综合验收 | ①E`【拉取】`全部最新提交 → ②E执行V2-1至V2-8 → ③阻塞者按负责人指定顺序修复 → ④E重新`【拉取】`复验 → ⑤E`【上传】`E标注与验证记录 → ⑥负责人`【拉取】`终裁并签署 | E；阻塞时文件所有者条件上传 | 终裁gold分歧，登记哈希，批准来源/数据/诊断/draft状态，签署Go/No-Go | 仅被点名时修A范围 | 仅被点名时修B范围 | 仅被点名时修C范围 | 仅被点名时修D范围 | 上传独立标注、命令、真实项数和逐项结论 | V2-1至V2-8全绿；否则NO-GO，不提前做W3 |

同一天存在多个主上传时段时，表中箭头就是锁顺序；后一人必须拉取前一人的提交。E只有在自己的原始标注已经封存并向负责人登记哈希后，才能在D7拉取含B原始标注的提交。条件修复永远插在被阻塞验证者重新复验之前。

## 7. 审计关系

| 产物 | 作者 | 必须审计 | 通过标准 |
|---|---|---|---|
| 诊断、学情、事务仓储 | A | E执行，A复核 | V2-4、V2-5；幂等、恢复、原子性和越权边界通过 |
| Profile revision 2 | B | E执行，A复核 | V2-2；严格Schema、引用闭合、revision和坏包拒绝通过 |
| 数据和唯一`clean_df` | B | C执行，E复核记录 | V2-3；每份3次一致，错误实现全部被识别 |
| 案例前置数据 | A+B | E执行，A复核 | V2-6；不得调用PathEngine |
| 私有资产隔离 | A+B+D | E执行，负责人复核 | V2-7；安全DTO、日志和样本零泄漏 |
| 首批20个gold | B、E独立 | 负责人终裁 | V2-8；原始双标保留，分歧有记录和冻结哈希 |
| D离线准备 | D | A权限审计、E脱敏审计 | 参数与D15一致，不接真实模型、不写权威事实 |

上述审计只判断第二周新增产物是否符合D15—D27。A—E第一周交付继续按当时19—26号任务书验收，不因本表新增要求被追溯判为阻塞。

## 8. 负责人批准事项

负责人本周必须书面批准：来源进入active的资格、公开/私有数据和唯一`clean_df`、诊断题与答案、revision 2继续保持`draft`或后续转态资格、60案例输入哈希、首批20个gold分歧与哈希、W2 Go/No-Go。

口头或群消息决定必须当日补问题单和书面结论，否则不生效。组员不得代替负责人批准，也不得把“审计PASS”写成“已active”。

## 9. 统一交付格式

每次申请上传锁必须包含：基线提交号、合同版本`W2-R2`、文件清单、作者测试、全量回归、已知限制、只读审计结论、未提交/未推送声明。上传后必须补充正式提交号和锁释放时间。

过程截图、聊天记录、重复回执和过期整改稿不进入正式仓库。每岗只保留一份最终交接清单；V2结论统一归档到`新版设计文档-重写版/周门禁记录/W2-验证记录.md`。

## 10. 第二周完成定义

- V2-1至V2-8全部通过并记录真实命令、项数、执行人和复核人；
- Profile revision 2可从干净目录加载，revision 1只在Git历史与迁移记录中保留；
- 固定诊断可保存、提交、跳过、恢复和原子完成，失败不产生半写入；
- 数据产生唯一结果，私有资产不泄漏；
- 20个开发案例和60个正式案例输入可计算，首批20个gold双标完成；
- 本周不以PathEngine、真实模型、CIDPP、React或路径合法率作为完成项；
- 负责人在D7基于同一最新提交做最终Go/No-Go。
