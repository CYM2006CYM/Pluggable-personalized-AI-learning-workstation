# 28 第二周公共合同总册

状态：`[负责人正式签发]`。修订号：`W2-R5`，签发日期：2026-07-30；同日勘误补齐显式跳过输入和固定周起点登记；2026-08-02按D33追加`W2-V2-3-ENV-1`审计临时环境补充；2026-08-03追加版本差异处理与本次C收口豁免；2026-08-05追加`W2-D7-CLOSEOUT-1`一次性收口，均不升级修订号。

本文只冻结第二周新增合同。权威顺序固定为：20号负责人决策记录 → 21号跨周公共合同 → 本册 → 06号排期与05/07/11号专项设计 → 27号及岗位任务书。冲突时成员停止相关实现并提交问题单，不得自行选择。

### 0.1 决策生效周与历史边界

- D01—D14用于验收第一周原任务；D15—D33自第二周对应任务包或追加裁决签发后生效。
- 21号当前`W2-C2`作为跨周合同使用，但由D15—D33引入的新增约束不得倒推为第一周岗位欠账。D32主要修改第二周执行节奏、候选冻结和验收状态；2026-07-30同版本勘误只补齐既有`submitDiagnosticAnswer`的输入判别联合，Facade仍为17个方法；D33只补齐第二周V2-3审计复现条件和环境错误归因。
- 第一周代码或资产与D15—D33存在差异时，登记为W2迁移项或06号规定的后续周实现项；不得修改第一周任务书、交接报告或当时验收结论来制造追溯要求。
- 本册只规定第二周应完成的迁移深度；未授权在W2实现的真实模型、React、CIDPP等能力仍按06号后续周排期执行。

### 0.2 不可变周起点与工作HEAD

`W2_START_COMMIT = f343a6c1c630f362f4686e6f6b0f50c6577d5562`是第二周不可变起点。D1身份检查使用“当时`HEAD == W2_START_COMMIT`”；D2-D3开始或继续实现前，A必须等待本次负责人合同勘误进入`main`并拉取最新`origin/main`，其实际工作HEAD只需以该固定周起点为祖先。A申请上传锁时同时报告固定周起点和拉取后的实际HEAD。不得用调查时观察到的`50834a7`或任何后续提交替换`W2_START_COMMIT`。

## 1. W2-A：Pandas诊断资产合同

### 1.1 题面蓝图

文件固定为`assessments/diagnostic/questions.json`。根对象允许下列核心字段和`x-`扩展，其他字段拒绝；`questions[]`条目只允许接口列出的精确字段，不允许未知字段或`x-`扩展：

```ts
interface DiagnosticBlueprintAsset {
  blueprintId: string;
  profileRevision: 2;
  goalIds: string[];
  estimatedMinutes: number;
  minimumCoverage: 1;
  questions: DiagnosticQuestionAsset[];
  scoringVersion: string;
  [extensionName: `x-${string}`]: JsonValue;
}

interface DiagnosticQuestionAsset {
  questionId: string;
  knowledgePointId: string;
  kind: "single_choice" | "judgment";
  difficulty: "S-R" | "S-U" | "M-U" | "M-A" | "C-A";
  prompt: string;
  options?: string[];
  maxScore: 1;
  required: boolean;
  evaluatorRef: string;
  sourceAnchorIds: string[];
}
```

约束：

1. 共8至12题，预计总时长不超过10分钟；六个Pandas核心知识点各至少一题，并至少一题抽样`basic-python`。
2. `single_choice`必须有3至5个非空且互不重复的`options`；`judgment`必须省略`options`。
3. 单选提交值是`options`中的原始字符串；判断提交值是布尔值。
4. `evaluatorRef`固定为`private/answer-key.json#<questionId>`；知识点、目标和来源锚点必须存在于同一revision。
5. 本revision拒绝`code_reading`、`output_prediction`、`multi_choice`和其他题型；未来增加必须修订合同。
6. `blueprintId/questionId/scoringVersion`必须是去首尾空格后仍非空的稳定ASCII标识，格式为`[A-Za-z0-9][A-Za-z0-9._-]*`；`questionId`在蓝图内唯一。
7. `goalIds`必须非空、去重且全部存在；每题`sourceAnchorIds`必须非空、去重且全部存在；每题只能引用一个存在的`knowledgePointId`。
8. `estimatedMinutes`必须是1至10的有限整数；`minimumCoverage`和`maxScore`只能是数值字面量`1`；`questions`必须为8至12项。
9. `prompt`去首尾空格后不得为空；`required`必须是布尔值。`required=true`表示关键覆盖题，但仍允许显式跳过；跳过只登记该知识点证据不足，不改写为答错。
10. 六个Pandas核心知识点各至少出现一题，`basic-python`至少出现一题；同一题不得通过扩展字段声明第二个计分知识点或题目自定义Evidence权重。

### 1.2 私有答案

文件固定为`assessments/diagnostic/private/answer-key.json`，根对象和条目只允许精确字段，不允许扩展：

```ts
interface DiagnosticAnswerKeyAsset {
  blueprintId: string;
  evaluatorVersion: string;
  answers: DiagnosticAnswerKeyEntry[];
}

type DiagnosticAnswerKeyEntry =
  | { questionId: string; kind: "single_choice"; correctAnswer: string }
  | { questionId: string; kind: "judgment"; correctAnswer: boolean };
```

`blueprintId/evaluatorVersion`必须符合稳定ASCII标识格式；`answers[].questionId`唯一。答案必须与题面一一闭合，不得缺少或多出条目；条目`kind`必须与题面一致，单选正确值必须严格等于一个选项。题面、前端、安全DTO、普通日志和模型上下文不得出现本文件内容。

## 2. W2-B：诊断运行与事务合同

### 2.1 保存、提交和锁定

- `saveDiagnosticDraft`只保存背景问卷、当前位置和未正式提交的界面草稿，不判分、不写Evidence候选。
- `submitDiagnosticAnswer`严格消费21号`W2-C2`的判别联合：`action="answer"`必须携带合法`answer`，`action="skip"`禁止携带`answer`。缺失/未知`action`或非法组合返回`diagnostic_answer_invalid`，不得用空值、特殊字符串、布尔哨兵或`null`表示跳过。
- `action="answer"`时由服务端读取私有答案并确定性判分，写不可变`DiagnosticAnswerRecord`和Evidence候选；`action="skip"`时只写不可变跳过记录，返回`skipped`且不创建Evidence候选、不写0分。
- 同一`questionId + requestId`和相同请求体重复到达返回首次结果，不重复写记录或候选；同一`requestId`对应不同`action/answer`返回`idempotency_conflict`。
- 同一题已经`answered`或`skipped`后，使用不同`requestId`再次提交返回`diagnostic_answer_conflict`；首次结果不覆盖。
- 跳过是显式提交，状态为`skipped`，不创建Evidence候选、不写0分，并把该题的知识点加入本次诊断快照的`insufficientKnowledgePointIds`；同一知识点重复跳过时只保留一个ID。
- 被跳过知识点没有其他有效直接Evidence时保持`mastery=null/confidence=0/unverified`；已经存在其他有效直接Evidence时仍按11号公式正常计算，跳过本身不参与分数或置信度计算。
- 诊断Evidence权重只能沿用11号定义的Evidence来源权重；题面、答案和运行记录均不得增加题目自定义权重字段。

### 2.2 完成和批量提交

Pandas诊断只有在8至12题全部处于`answered`或`skipped`后才能完成。`completeDiagnostic`必须：

```text
读取不可变答案与候选
→ 校验blueprint/profile/diagnostic/session版本
→ 一次重算全部KnowledgeState
→ 调用一次LearningSessionRepository.commit
→ 使用evidenceCandidates批量公开正式Evidence
→ 原子替换latest.json并保存latestDiagnostic/LearnerDiagnostic快照
→ 异步请求五维初始画像
```

有一条以上答题Evidence时使用非空`evidenceCandidates`，全部候选共享一个新`evidenceVersion`；全部题均跳过时省略Evidence批量且`evidenceVersion`保持原值。两种完成均必须携带`diagnosticCandidate`，只递增一次`sessionVersion`，并通过`latestDiagnostic/committedDiagnosticId`返回已提交快照身份。任一候选无效、版本冲突或存储失败时整批拒绝，正式Evidence、版本、KnowledgeState和诊断快照均保持原值。五维画像失败不回滚已经成功的确定性完成事务。

## 3. W2-C：学情计算和仓储骨架

- Evidence类型、来源权重、过滤、`mastery/confidence`公式、状态阈值和`skipEligible`唯一依据是11号第6至8节；诊断题不得另设自定义权重字段。
- A必须把11号五个手工示例原样落成测试；禁止添加“mastery单调不减”断言，新增较低证据可以合法降低加权平均值。
- 无有效直接证据时`mastery=null`、`confidence=0`、状态`unverified`；跳过不得改变该结果。
- W2完成`LearningSessionRepository`的文件持久化与事务骨架：`create/getSnapshot/commit/recover`四类能力、临时候选、原子`latest.json`、版本比较、失败隔离和恢复。
- W2不实现PathEngine，不调用`buildPath`，也不统计路径合法率。知识点无环校验继续复用W1 Profile解析器，不另写第二套拓扑合同。

## 4. W2-D：Pandas Profile revision 2迁移

### 4.1 revision规则

当前`f6c8396`提交中的W1 draft是revision 1。本周在同一`pandas-cleaning-v2-draft`目录修改唯一完整`profile.json`；下列内容只是必须变化的字段片段，不是补丁文件、第二份清单或可单独加载的JSON：

```json
{
  "status": "draft",
  "version": "0.2.0-draft",
  "revision": 2,
  "revisionOf": 1,
  "x-candidateApproval": "pending_owner_decision"
}
```

revision 1从未active且无绑定会话，由Git历史和`quality/revision-1-to-2.md`保留，不复制第二个运行目录。

### 4.2 正式知识点ID

| ID | 类型 | importance | 迁移来源 |
|---|---|---:|---|
| `basic-python` | 辅助补救 | 0 | 新增 |
| `pandas.clean.read-csv` | 核心 | 0.05 | 从`kp-structure`拆分 |
| `pandas.clean.inspect-dataframe` | 核心 | 0.05 | 从`kp-structure`拆分 |
| `pandas.clean.missing-values` | 核心 | 0.20 | `kp-missing` |
| `pandas.clean.duplicate-orders` | 核心 | 0.15 | `kp-duplicates` |
| `pandas.clean.type-format` | 核心 | 0.25 | `kp-types` |
| `pandas.clean.validate-result` | 核心 | 0.30 | 合并`kp-invariants`和`kp-engineering` |

六个核心点importance合计1；辅助点不进入核心覆盖分母。B必须同步`goals`、`activities`、`sources`、诊断、兜底题、TaskBundle、章节和质量记录中的全部引用，旧`kp-*`零残留。

### 4.3 路径和内容迁移

- 章节目录、章节ID和节ID迁移到07号第2节的三章六节命名。
- `basic-python`提供一张`cards/basic-python-remediation.md`和一个资料包固定补救活动；它不进入最终目标必做活动。
- `sources/source-registry.json`迁移为`sources/source-map.json`，并更新`profile.json.paths.sources`。
- profile、目标、知识点、活动三类资产必须通过W1-C3闭合校验；任何旧ID、混合revision或悬空引用返回`invalid_profile`。

## 5. W2-E：数据和内容资产

- 数据列、城市映射、日期、金额、状态、缺失、重复选择和`clean_df`严格执行21号D.3/D.4，不重新决策。
- 城市映射仍是上海市/沪→上海、北京市/京→北京、广州市/穗→广州；日期只接受`YYYY-MM-DD`。
- B制作约30行公开CSV、2至3份20至30行私有变体、参考实现、唯一期望结果和至少4份错误实现；C对每份数据连续运行3次并反向验证错误实现。
- 来源文件固定为`sources/source-map.json`，字段和官方裁决按21号D.1；A作为内容第二评审，负责人批准来源进入active资格。

## 6. W2-F：案例输入和首批gold

### 6.1 案例输入

`evaluation/`固定指公共仓库根目录，与`pi-study-helper/`并列；不得改成`pi-study-helper/evaluation/`。文件固定为`evaluation/personas/development-20.jsonl`和`evaluation/personas/final-60.jsonl`。每行字段：

```ts
interface EvaluationPersonaCase {
  caseId: string; // dev-001或final-001
  personaType: "cs_student" | "self_learner" | "practice_oriented";
  background: DiagnosticDraftField[];
  goalId: string;
  diagnosticAnswers: Array<
    | { questionId: string; action: "answer"; answer: string | boolean }
    | { questionId: string; action: "skip" }
  >;
  availableMinutes: number;
  notes?: string;
}
```

`final-60`三类各20例；案例只能引用revision 2资产。文件中禁止出现系统路径、系统推荐结果或gold字段。

### 6.2 双标文件

#### 6.2.1 D4 gold输入冻结包

B必须基于A的D3正式提交生成下列仓库外候选文件；ZIP和旁路文件用于审计传递，不进入Git：

```text
B-W2-D4-freeze-candidate-<W2_START_SHORT>.zip
B-W2-D4-freeze-candidate-<W2_START_SHORT>.zip.sha256
B-W2-D4-freeze-candidate-<W2_START_SHORT>.manifest.json
```

manifest固定为`w2-freeze-manifest-v1`，至少包含以下字段；不得增加会改变哈希口径但未写入`manifestVersion`的隐式规则：

```ts
interface W2FreezeManifest {
  manifestVersion: "w2-freeze-manifest-v1";
  hashAlgorithm: "SHA-256";
  aCommit: string;
  profileVersion: string;
  profileRevision: 2;
  scoringVersion: string;
  evaluatorVersion: string;
  entries: Array<{
    path: string;
    hashMode: "normalized-text" | "raw-binary";
    byteLength: number;
    sha256: string;
  }>;
  assetTreeSha256: string;
  final60Sha256: string;
  diagnosticKnowledgeStateSummarySha256: string;
}
```

哈希规范固定如下：

1. `path`从公共仓库根目录起算，Unicode使用NFC，分隔符统一为`/`；禁止盘符、前导`/`、空段、`.`和`..`，并拒绝仅大小写不同的重复路径。`entries`按规范化路径UTF-8字节序升序排列。
2. `.json/.jsonl/.md/.csv/.txt/.py/.js/.cjs/.mjs/.ts/.tsx/.yaml/.yml`固定使用`normalized-text`：输入必须可解码为UTF-8且无BOM，将CRLF和CR统一为LF，不增加或删除文件末尾换行；其他文件使用`raw-binary`并按原始字节计算。
3. `byteLength`和逐文件`sha256`都针对上述规范化后的字节；SHA-256统一写64位小写十六进制。
4. 资产树输入按`entries`顺序逐项拼接`UTF8(path) + NUL + ASCII(hashMode) + NUL + ASCII(sha256) + NUL + ASCII(byteLength) + LF`，对完整拼接字节计算`assetTreeSha256`。空格、系统路径、ZIP时间戳和枚举顺序不得参与。
5. `final60Sha256`必须等于`evaluation/personas/final-60.jsonl`条目的逐文件SHA-256。manifest内容哈希使用RFC 8785 JCS规范化序列化计算，不受展示缩进或换行影响；manifest内不得保存自身哈希。

负责人同时在本地维护`evaluation/golden/gold-input-freeze.json`，并在D7与正式gold和最终签署一并入库。冻结记录必须包含A正式提交号、Profile revision、`scoringVersion/evaluatorVersion`、规范化相对路径、逐文件SHA-256、资产树SHA-256、`final-60.jsonl` SHA-256、manifest的JCS SHA-256、D4候选V2-3报告的规范化文本SHA-256、诊断/KnowledgeState前置结果摘要SHA-256、冻结时间、执行人和复核人。ZIP SHA-256只证明传输包完整，不能替代内部内容哈希。`gold-input-freeze.json`不得包含自身哈希；其JCS SHA-256由负责人写入D7的W2门禁记录。

D4的gold链必须由A完成Schema与引用审计、E完成V2-2/V2-6及候选阶段V2-7预检、C在冻结候选上完整执行V2-3，负责人书面确认后B/E才可开始独立标注。D4 18:00前未完成上述门禁，当日不得启动标注。D材料链与gold链并行：D取得A正式提交并通过A/E对D材料的审计后即可上传，不等待C的V2-3或负责人gold冻结；gold链失败只阻止标注，不否定已经通过自身审计的D交付。

> **`W2-GOLD-AUTH-CORR-1`（2026-08-03）**：保留上述默认截止规则，同时勘误本批次执行事实。全部技术门禁在D4晚间完成后，负责人实际书面批准自`gold-input-freeze.json.frozenAt = 2026-08-01T19:37:30.5739587+08:00`立即开始B/E首批20例独立标注；原`DEFERRED_UNTIL_D5/D5生效`文字无效。勘误只改授权元数据，67项冻结内容、资产树、`final-60`、manifest JCS、诊断摘要和D4 V2-3报告哈希均不变。冻结记录JCS仅因该元数据勘误从`7d80d2fe78299326e624c10cf75301191119fe061a48ab6f1d2a5d7a84191672`更新为`6e99fc788037c7f9bda72f5d7eeffcc51b1c0a35ee216e635447543882bf2504`，新值由B/E交接声明和D7门禁从外部引用，不写入冻结文件自身。默认合同仍要求交接声明给出晚于生效点的实际开始时间；本批E精确开始时间的证据例外只按后续`W2-D7-CLOSEOUT-1`处理。

> **`W2-D7-CLOSEOUT-1`时间与B声明登记（2026-08-05）**：B既有独立性声明已经存在，引用旧JCS不使声明或`69bfd3a4086a7a6c9c46134d69d0cdda3eab558ac13a0a45115476c6aa33feab`标注失效，负责人在资格记录中登记上述授权元数据替换即可，不要求B重做。本批E精确开始时间不可恢复，负责人接受“授权`19:37:30`→最早可审计内容`21:33:42`→完成`21:34:26`”时间链，精确开始时间不作为资格阻塞；不得伪造时间，覆盖、Schema、冻结绑定、哈希和独立性仍按下述四项硬条件检查。

冻结清单内文件发生任何变化时，B必须重新生成manifest和资产树哈希，A/C/E复验受影响项，C重新确认V2-3，负责人重新冻结。重新冻结前标注必须停止；受变化影响的既有标注必须作废并重新开始。D5 B提交必须与冻结候选逐文件一致；C在Git提交上核对规范化路径、逐文件哈希、资产树哈希和`final-60`哈希，完全一致时才可用最小复现把D4完整V2-3绑定到正式提交。

#### 6.2.2 原始标注封存与终裁

B和E在看任何系统路径或正式输出之前，分别写入：

```text
evaluation/golden/annotations/b-first-20.jsonl
evaluation/golden/annotations/e-first-20.jsonl
```

每行固定字段：`caseId/annotatorRole/nodeConstraints/requiredRemediationKnowledgePointIds/forbiddenActions/notes`。`nodeConstraints`每项含`knowledgePointId/required/allowedDifficulties/allowedScaffoldLevels/skippable`；`forbiddenActions`只允许`skip_unverified_prerequisite/omit_required_node/violate_prerequisite_order/exceed_time_budget`。

两人必须在互不可见的状态下独立完成，并在D6中午前分别向负责人登记完成时间和SHA-256。负责人只在两份文件封存后执行盲标资格检查，硬条件固定为：

1. B/E文件各有且仅有final-001至final-020共20个唯一`caseId`，字段、枚举和引用均符合本节Schema；
2. 两份文件绑定同一份`gold-input-freeze.json`及同一个`final-60Sha256`，登记的完成时间不晚于D6中午截止点；
3. 负责人复算的文件SHA-256与两人封存值一致，封存后文件未覆盖；
4. B/E均有可审计声明和过程记录证明封存前未查看对方标注、系统路径或正式系统输出，且未发生其他破坏独立性的交换。

四项全部通过后，V2-8才可记为`PENDING_OWNER_ADJUDICATION`，B才可上传原始标注，E才可拉取B提交、比较或协商。任一标注未完成、未封存、Schema/覆盖不合格、哈希变化或独立性不成立时，V2-8必须记为`BLOCKED`；B/E原始标注均不得进入Git，E不得拉取或查看B文件，也不得把当次运行称为最终完整V2。E仍应完成不依赖双标的V2-1至V2-7并记录真实结果，问题转D7处理，负责人不得签署W2 GO。若D7仍能在不破坏独立性的前提下补齐，须重新执行资格检查；独立性已经破坏时受影响标注作废，只有重新盲标并通过检查后才能继续，否则本周NO-GO。

资格检查通过后的默认规则是分歧先协商一次，仍不一致由负责人终裁。`W2-D7-CLOSEOUT-1`仅对本批final-001至final-020取消协商：E生成机械差异清单，负责人直接逐案终裁；原始双标文件及其封存哈希不可覆盖，后续修正只能新增记录。第三周剩余40例继续执行默认协商规则。

负责人终裁后生成并登记SHA-256：

```text
evaluation/golden/difficulty-gold.jsonl
evaluation/golden/path-constraints.jsonl
evaluation/golden/adjudication-log.jsonl
```

`adjudication-log.jsonl`必须能追溯`caseId`、B/E原始意见、协商结果或合法跳过标识、负责人终裁和理由；本批协商状态固定为`SKIPPED_BY_W2-D7-CLOSEOUT-1`。正式gold只能由负责人从已封存原始标注生成，不得由B或E自行覆盖。W2只完成final-001至final-020，剩余40例在W3完成。

### 6.3 Claim拆分与指标脚本骨架

E创建`evaluation/claims/claim-split-template.md`，最低字段固定为`claimId`、`artifactId`、`knowledgePointId`、`text`、`sourceAnchorIds`和`profileRevision`。本周只冻结模板，不对系统输出形成正式claim或效果结论。

E创建`pi-study-helper/scripts/evaluation-metrics.mjs`骨架，至少明确各指标的分子、分母、排除项、输入文件和可复算输出位置。本周只验证结构和空输入行为，不读取正式系统输出、不写正式指标值，也不得把脚本骨架的存在记为指标PASS。

## 7. W2-G：离线模型准备

D只制作动态题、Generator/Hunter/Defender/Judge提示词草稿及脱敏录制响应。参数必须逐项写明D15的模型、温度、60秒超时、2次结构重试和10万token会话预算；不使用“保守参数”等模糊词。W2不接真实模型主链、不实现CIDPP服务、不启用D16关闭能力。

## 8. W2-H：D15—D20新增决策承接合同

| 决策 | 第二周立即生效的义务 | 后续周已冻结责任 |
|---|---|---|
| D15 | D形成OpenAI兼容配置、参数、预算、重试、fallback和脱敏录制响应；A审计权威事实边界，E审计泄漏 | W4真实模型接入时严格消费本合同，W4后不得临时切换模型 |
| D16 | A—E均不得启用外部评测器、完整AI代码题、GoA/OAEO、SSE或批量离线生成；P0继续使用`textarea` | Monaco只有通过W5提前门禁才可由E接入 |
| D17 | W2不实现CIDPP；D只保留单Agent、最多一次优化和超时降级约束，E保留未来门禁检查项 | W4周日18:00按V4-1至V4-7自动裁决运行时CIDPP开关 |
| D18 | 白名单是授权边界，不代表依赖已经安装；任何岗位不得修改依赖、`allowScripts`或运行`npm audit fix` | E在W3/W5按门禁安装获准依赖，Monaco仍是条件项 |
| D19 | A担任内容第二评审；B和E独立完成首批gold；负责人终裁分歧 | B负责PPT，D负责演示视频，E负责试用者招募，按06号对应周交付 |
| D20 | 所有合同、来源、依赖、模型和降级缺口必须提交正式问题单；口头或群消息批准当日补单，否则不生效 | 后续各周持续适用 |

第一周A—E产物不按本节追溯审计。本节发现的实现差异只能进入第二周迁移清单或后续周待办。

D15参数直接冻结为：环境变量`OPENAI_MODEL/OPENAI_BASE_URL/OPENAI_API_KEY`，模型未设置时默认`deepseek-chat`；生成、解释和问答类温度`0.7`，Hunter、Defender、Judge、CapabilityScorer和CIDPP评价类温度`0.2`；单次调用超时60秒；结构错误在初次调用后最多重试2次；单会话累计预算10万token，超限使用固定fallback。正式调用必须有Base URL和API Key。模型ID进入缓存键、轨迹、录制响应和评测配置，密钥只留在宿主环境。

D16的Monaco门禁直接冻结为：W5周五18:00前预跑V5-1至V5-8，全部通过且E书面确认有余力，才允许周六接入；周日重跑完整V5，失败即恢复`textarea`并关闭Monaco。未触发条件时不得安装`monaco-editor`或`@monaco-editor/react`。

D18白名单直接冻结为：运行依赖`react/react-dom/react-router-dom/zustand`；开发与测试依赖`vite/@vitejs/plugin-react/typescript/vitest/@types/react/@types/react-dom/jsdom/@testing-library/react/@testing-library/user-event/@testing-library/jest-dom`。禁止UI组件库和图表库，其他依赖必须先提交问题单。

`ModelExecutionResult`当前没有真实token用量字段。D本周不得自行扩展公共DTO；应提交问题单，由负责人在真实模型接入前裁决10万token会话预算按实际用量还是请求预算扣减。

## 9. W2验证编号

| # | 测试项 | 执行与复核 | 硬性通过标准 |
|---|---|---|---|
| V2-1 | 新版基线复验 | E执行，负责人复核 | 固定`W2_START_COMMIT = f343a6c1c630f362f4686e6f6b0f50c6577d5562`。D1身份检查：E确认当时HEAD与其一致、`8785c6e`是其祖先，运行全部基线命令并保存记录；D6最终回归：确认固定周起点是当时最新HEAD的祖先且D1记录仍完整有效，在D6最新HEAD重跑类型检查、全量测试、扩展冒烟和`verify`，全部通过并记录真实项数 |
| V2-2 | Profile与坏包拒绝 | E执行，A复核 | 正常包从干净目录加载；路径穿越、缺失、悬空引用、重复清单和错误revision全部拒绝 |
| V2-3 | 数据唯一性 | C执行，非B成员复核 | 先按`W2-V2-3-ENV-1`确认`pandas==3.0.5`；随后对公开CSV和每份私有变体连续运行3次，`clean_df`逐单元格、dtype、列序和行序一致，且已知错误实现全部被识别；环境不匹配只记`environment_mismatch/BLOCKED`。本批E工具变更由负责人按`W2-D7-CLOSEOUT-1`比较旧新逻辑并复核真实六输入26/26测试后代替C出具不降标确认 |
| V2-4 | 诊断与学情 | E执行，A复核 | 全对、全错、跳过、退出恢复和重复提交均符合合同；无证据为`unverified`；KnowledgeState输入可稳定复算 |
| V2-5 | 仓储事务骨架 | E执行，A复核 | 四类接口存在；只有`commit`可公开正式状态；版本冲突和失败不留下半写入 |
| V2-6 | 案例前置数据 | E执行，A复核 | 20个开发案例和60个正式案例的字段、诊断结果与KnowledgeState前置数据均可计算；本项不得调用`buildPath`或统计路径合法率 |
| V2-7 | 资产隔离 | E执行，负责人复核 | 本批扫描safe DTO、其确定性普通日志测试视图、D录制响应、其确定性安全导出测试视图、C的`v2-3-final-report.md`和D的`d4-validation-report.md`，覆盖五类表面；逐输入诊断答案、私有CSV、隐藏测试、参考实现、API密钥和宿主绝对路径canary零命中。E验证记录是结果载体，不递归扫描自身 |
| V2-8 | 输入与首批gold冻结 | B和E执行，负责人复核/终裁 | 60个案例输入和gold输入资产树已登记哈希；B/E各20例的覆盖、Schema、绑定、封存哈希和独立性全部通过时为`PENDING_OWNER_ADJUDICATION`。本批E精确开始时间按补充裁决豁免，E生成机械差异清单，负责人直接终裁并生成三份正式gold、登记哈希后转为`PASS` |

### 9.1 `W2-V2-3-ENV-1`审计临时环境补充

> **2026-08-03负责人执行补充**：本补充覆盖下列原第4、5项中要求C新增版本不匹配负向作者测试、并由C再次在3.0.5环境完整运行V2-3的本次收口要求；其余入口阻断、依赖文件、证据归属和安全边界继续有效。

1. `W2_V2_3_AUDIT_PANDAS_VERSION = 3.0.5`是D4—D6 V2-3的唯一硬版本值。该值来自C候选证据和负责人独立虚拟环境复验；Python、平台和具体补丁版本只登记实际值，不作为本补充的硬门槛。
2. C的正式入口必须先读取并记录Python、Pandas和平台，再严格判断`pandas.__version__ == "3.0.5"`。不匹配时不得加载参考实现、测试、CSV或opaque fingerprint，不得继续资产结论。
3. 不匹配输出至少包含`status: "BLOCKED"`、`v23Status: "BLOCKED"`、实际版本、期望版本、`environment_mismatch`分类和非空blocker，并以非零退出码结束。该结果不得写成`b_asset_defect`、`validator_defect`、学习者错误或负Evidence。
4. C在授权目录内增加环境预检、报告和交接说明，并新增唯一审计依赖文件`pi-study-helper/scripts/w2-data-validation/requirements-w2-v23.txt`；该文件的有效依赖行必须精确为`pandas==3.0.5`。不得修改应用`package.json/package-lock.json`、SDK、B资产或Profile的`environment-lock.json`。
5. 以后任何岗位发现实际运行环境与合同指定版本不一致时，必须先停止对资产或实现作结论，在仓库外创建全新的独立虚拟环境，按合同或对应`requirements`安装目标版本，再执行测试；记录实际Python、关键依赖、平台、命令和退出码。禁止通过修改应用依赖、锁文件或Profile环境锁来迁就本机环境。
6. 本次C收口由负责人完成两项独立验证：`pandas 2.3.3`下入口以退出码2返回`BLOCKED/BLOCKED`，唯一分类为`environment_mismatch`、blocker非空且未读取B资产；`Python 3.13.14 + pandas 3.0.5`下完整V2-3为PASS。据此免除C新增版本不匹配负向作者测试，并免除C重复运行3.0.5完整V2-3。
7. C最终报告和交接清单必须如实标注第6项为负责人独立证据，不得写成C执行；正式Git内容哈希绑定、环境预检、审计专用requirements及原有真实运行证据仍须保留。非匹配环境不得用于评价B资产。
8. C工具和报告不属于B冻结资产树。只要67项、资产树SHA-256和`final-60` SHA-256未变，本补充不触发候选重建、gold重新冻结或标注作废；B冻结内容变化时仍严格执行第7节重新冻结规则。
9. 本补充不把Profile由`draft`改为`active`，不填写最终`environment-lock.json`，也不代替第3至5周对Node.js/Python/Pyodide正式执行环境、能力和资源上限的原型冻结。

E必须在D6基于当时最新提交执行所有不依赖双标的V2-1至V2-7并写入`新版设计文档-重写版/周门禁记录/W2-验证记录.md`正文。V2-1按“`W2_START_COMMIT`是最新HEAD祖先并在最新HEAD重跑”的D6语义执行，不再要求HEAD等于起始提交；V2-1至V2-7只能记为`PASS`或`BLOCKED`。盲标资格检查通过后，E才能拉取B标注、完成V2-8可执行部分并将其记为`PENDING_OWNER_ADJUDICATION`；检查失败时V2-8必须记为`BLOCKED`，且不得把V2-1至V2-7的独立运行描述为最终完整V2。D7只允许阻塞文件所有者修复、E复验受影响项、负责人终裁gold并追加V2-8最终结论和Go/No-Go签署；不得把V2-1至V2-7的首次执行推迟到D7。

E必须在D3登记V2综合脚本、V2-7扫描器、claim模板和指标脚本骨架的文件清单及SHA-256，作为工具基线。D4以后只有出现可复现的脚本误报、漏报、崩溃或环境兼容缺陷时才允许修改E工具；每次修改必须先报告负责人，登记缺陷证据、修复前后SHA-256、受影响V2项和复验命令，并由对应产物所有者确认没有降低原验收标准。禁止删除失败用例、降低断言、扩大排除范围或修改预期结果来换取PASS。D4-D5确认的工具缺陷可在登记后修复并重跑受影响项；D6首次完整复验中才发现的工具缺陷必须把对应项记为`BLOCKED`，转D7由E条件修复并复验。

V2-7不得扫描或删除公共仓库中按合同允许存在的`private/hidden/reference-solutions`资产，否则会产生必然误报。扫描器只消费运行时输出和为测试构造的安全视图；报告只记录命中类别、输出位置和计数，不得回显敏感正文。任一canary命中即失败，不能通过删除失败记录、忽略目录或改成模糊人工判断放行。

### 8.2 `W2-D7-CLOSEOUT-1`证据与执行补充

1. E在仓库外从`pi-study-helper/fixtures/safe-views/start-session-safe-response.json`确定性构造`ordinary-log.jsonl`和`safe-export.json`。前者固定为`{"event":"start_session_safe_response","payload":<原safe DTO>}`单行JSONL，后者固定为`{"exportVersion":"w2-safe-export-v1","items":[<原safe DTO>]}`；不得手工增删DTO字段，不形成生产接口。
2. V2-7输入manifest必须列出六个输入的表面、规范化位置和SHA-256；C/D报告只读取正式Git内容，E不得修改。canary、原两输入结果、新五类结果、manifest和sidecar均仓库外封存；Git验证记录只登记哈希、完整命令、退出码、逐输入六类计数和限制。
3. 原两输入canary与结果原件的目标SHA-256分别为`59018cc6734d09eddbb0271bac0ab9e9de9f2823b77f4eafc9554d58735f38e1`和`3ee729099433c8c396f538e0d3f14a638c1adbd8c88ff924431b01e67bf2a1fd`。原件遗失时重建结果必须标记`RECONSTRUCTED`并登记新哈希和原因；新六输入完整结果必须使用新哈希，不得伪装为旧两输入结果。
4. 负责人已完成不降标确认：综合脚本由`de57ab5c...783a`变为`77bc838c...c226`，作者测试由`a5985f32...b8a0`变为`e35b4816...730b`；V2-3六输入、67项、manifest JCS、D33环境证据、三次稳定运行和错误实现拒绝标准均未降低，V2-3相关差异只取消C历史报告重复负责人专用措辞的要求，不改变机器判据；真实六输入作者测试26/26通过。登记`OWNER_NO_LOWERING_CONFIRMATION / V2-3=PASS`，不再等待C确认或重复执行完整V2-3。
5. 负责人签署双封存资格后V2-8进入`PENDING_OWNER_ADJUDICATION`。E只生成逐案机械差异清单；负责人直接终裁并生成三份正式gold。E不得修改原始标注、生成正式gold或把待终裁状态提前写成PASS。

## 10. 修订记录

| 修订号 | 日期 | 内容 |
|---|---|---|
| `W2-R1` | 2026-07-27 | 签发D22—D27，冻结诊断Schema、首次提交锁定、完成时批量Evidence、全部题处理条件、Profile revision 2迁移和完整7天排期 |
| `W2-R2` | 2026-07-28 | 修正决策生效周和历史任务边界；将D15—D20从第一周岗位追溯要求迁入第二周，并登记A—E承接职责 |
| `W2-R3` | 2026-07-28 | 补齐诊断跳过的不足知识点语义、Evidence权重来源、gold封存与正式文件、claim/指标脚本骨架，并冻结V2-1至V2-8责任和D6/D7验收顺序 |
| `W2-R4` | 2026-07-28 | 落实D28—D31：统一完整Evidence/KnowledgeState，补齐诊断快照事务和严格Schema，修正W2起始提交、候选审计、D4—D6负载及V2-7扫描口径 |
| `W2-R5` | 2026-07-30；同日勘误 | 落实D32：D2并行准备、D3 A上传、D4的gold链与D材料上传链并行、D5 B→C、D6条件B→E；新增可复算manifest/资产树哈希算法和E工具受控修复通道，明确V2-1两阶段语义、V2-8状态、候选变化失效规则及Profile全周保持draft；勘误同步`action`的`answer/skip`判别联合和固定`W2_START_COMMIT` |
| `W2-R5 / W2-V2-3-ENV-1` | 2026-08-02 | D33追加第二周V2-3临时审计环境：固定`pandas==3.0.5`，环境不匹配在资产判断前以`environment_mismatch/BLOCKED`结束；不升级W2-R5，不修改B冻结资产或最终Profile环境锁 |
| `W2-R5 / W2-V2-3-ENV-1`执行补充 | 2026-08-03 | 冻结版本差异时先建仓库外独立虚拟环境再测试；登记负责人两套独立证据，并免除本次C新增版本负向作者测试及重复运行3.0.5完整V2-3 |
| `W2-R5 / W2-GOLD-AUTH-CORR-1`执行勘误 | 2026-08-03 | 将本批次首批20例标注授权从误写的D5生效校正为负责人冻结时间`2026-08-01T19:37:30.5739587+08:00`立即生效；冻结输入和D6资格门禁不变 |
| `W2-R5 / W2-D7-CLOSEOUT-1`执行补充 | 2026-08-05 | 沿用B声明并登记JCS勘误；负责人替代C确认E工具不降标；E仓库外构造两类安全测试视图并补齐五类V2-7；本批E精确开始时间不阻塞，首20例由负责人跳过协商直接终裁；不改变原始标注、冻结输入或W3规则 |
