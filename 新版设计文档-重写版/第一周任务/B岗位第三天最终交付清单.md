# B岗位第三天最终交付清单

## 1. 交付结论

| 项目 | 结果 |
|---|---|
| 公共合同版本 | `W1-C5` |
| 审计基线 | `2feea21a4c68831c7fff0a722856d21ceb57aaea` |
| Profile状态 | `draft` |
| 候选资产 | 58项 |
| B作者测试 | 1个文件 |
| 资产树SHA-256 | `cf8a13ae90f123f3a9e39b188a65c144271fe1b3dec9d0099bcb23bd143b27b4` |
| 最终状态 | `READY_FOR_OWNER_UPLOAD_LOCK` |

本候选包仅包含B岗位的58项Pandas Profile v2候选资产、1个作者测试文件和本清单。Profile仍为`draft`，不得在本次上传中改为`active`。

## 2. 负责人独立复验

负责人以公共仓库基线和本候选资产完成了独立复验：

| 检查项 | 结果 |
|---|---|
| 58项资产树SHA-256复算 | PASS，与登记值一致 |
| `npm.cmd run typecheck` | PASS |
| W1-C5专项测试 | PASS，2个文件、30/30项 |
| `npm.cmd test` | PASS，26个文件、164/164项 |
| Python候选Harness | PASS，22条执行记录；参考实现通过，starter与known-wrong实现被拒绝 |
| `npm.cmd pack --dry-run --json` | PASS，58项Profile资产全部进入本地包 |

负责人在清洗完成后再次执行了上述验证，并以外置SHA-256文件登记最终ZIP摘要。

## 3. A/C/E审计说明

B在第三天正式上传锁申请中声明：A、C、E只读审计均为`PASS / 不阻塞`。负责人决定接受该正式声明，不再要求B保留三份最终签字回复。

清洗前的旧附件没有与该最终声明一一对应的最终签字文档，其中只包含已过期的中间结论和整改过程材料。负责人没有据此虚构独立审计文件；本清单如实记录结论来源为“B正式申请声明，经负责人认可”。

## 4. 尚未通过的后续门禁

以下事项不属于本次上传锁批准，仍须在后续门禁中完成：

- 正式来源及许可证批准；
- Python、Pandas及相关环境版本冻结；
- Profile从`draft`进入`active`的负责人批准；
- C岗位运行时进程隔离、fixture最小授权和`test_asset_invalid`归因验证；
- 后续端到端与安全门禁。

## 5. 上传边界

本候选包未修改且上传时不得修改：

- 19至26号公共合同和任务文档；
- `package.json`、`package-lock.json`和TypeScript配置；
- Node/Python正式评测器；
- 公共Facade、仓储接口及错误码；
- A、C、D、E岗位文件；
- v1业务源码和回归资产。

B获得上传锁后，须先拉取最新`main`并确认远端基线，检查同路径冲突，复跑必要验证，只暂存本清单列明的60个文件。提交、推送并报告提交号后立即释放上传锁。
