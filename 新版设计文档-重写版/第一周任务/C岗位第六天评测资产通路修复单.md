# C 岗位第六天评测资产通路修复单

- 前置：B 已上传资产隔离修复。
- 顺序：**第 3 步；仅在拉取 B 后开始，完成并上传后 D 才可开始。**
- 验收依据：仅第一周 D01—D14；D15—D20 不追溯适用。

## 任务

验证并在必要时最小修复 C 的评测资产授权通路：公开请求 / 响应和公开 fixture 均不得携带答案、隐藏断言、参考解、完整 Rubric 或宿主路径；评测端口仍可从 B 提供的受控私有位置按授权读取所需材料。

必须保持：

1. 五阶段评测顺序、既有错误归因、幂等请求和中断恢复语义不变。
2. `evaluator_error` 不产生负 Evidence，私有资产不进入 ActivityResult 或 Facade 安全 DTO。
3. 不复制 A 的公共类型，不修改 B 的专业内容，不新增错误码、评分规则或环境上限。

## 交付与验收

- 交付：评测端口的资产读取边界及最小测试证据。
- 最小验证：`evaluation-protocol.test.ts`、`code-evaluation-port.test.ts`、相关 Profile / 资产测试以及 `npm.cmd pack --dry-run`。
- 通过标准：所有公开输出只含安全投影；打包产物不含受限资产；正常、幂等、错误和恢复路径继续通过。

上传后通知 D 拉取；E 仅进行独立复验。
