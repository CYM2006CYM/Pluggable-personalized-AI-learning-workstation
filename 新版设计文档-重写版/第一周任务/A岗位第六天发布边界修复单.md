# A 岗位第六天发布边界修复单

- 前置：E 第六天审计阻塞问题。
- 顺序：**第 1 步，A 完成并上传后 B 才可开始。**
- 验收依据：仅第一周 D01—D14 与 W1-C2 至 W1-C6；D15—D20 不追溯适用。

## 任务

确认并书面冻结“可分发公共表面”与“运行时受控私有资产”的边界，保证修复不会改变已冻结的公共 DTO、17 个 Facade 方法、Profile Schema、评分、错误码或 Evidence 权限。

需要完成：

1. 确认 npm 包中允许保留的公共 Profile 文件，以及必须排除的 `private`、`reference-solutions`、完整 Rubric、隐藏断言和私有数据路径。
2. 确认 C 的评测端口只能从受控运行时位置读取授权资产，不能把这些资产加回公共 DTO、公共 fixture 或发布包。
3. 若现有包文件清单无法表达该边界，提出最小的书面合同 / 配置裁决；不得自行新增业务字段或第二套接口。
4. 向 B 交接可执行的允许清单与拒绝清单。

## 不负责

不改 B 的资产正文、不改 C 的评测实现、不改 D 的 Agent 实现，不将 D15—D20 的新约束回溯写成第一周缺陷。

## 交付与验收

- 交付：边界说明、允许 / 拒绝路径清单及 B 的消费说明。
- 最小验证：`npm.cmd run typecheck`；对现有公共类型和 Facade 契约测试执行回归。
- 通过标准：B 无需猜测哪些资产可发布；修复不会改变任何冻结公共签名。

上传后通知 B 拉取；E 只读复核，不代改 A 文件。
