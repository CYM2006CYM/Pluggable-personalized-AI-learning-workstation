# 20 第一周开发前负责人决策冻结记录

状态：`[D01—D44已生效；第三周D0已签发；W3-C2/W3-R2已同步]`。负责人于2026-07-20至2026-08-03完成D01—D33及第二周勘误；2026-08-04新增D34—D43，形成第三周`W3-C1/W3-R1`；2026-08-06确认W2正式GO提交`f190326a4a906b46e4001484ffa30a7839b82ed2`满足门禁，将其回填为`W3_START_COMMIT`并签发D0，同日按单日闭环原则将调度升级为`W3-R2`；随后新增D44取消第三周剩余40例B/E协商，业务合同升级为`W3-C2`。D34—D44的内容不再由组员自行解释。本文是各周总任务书和公共合同的上位决策记录。

> **2026-08-01同版本同步**：仅将现行严格诊断Schema入口明确为28号`W2-R5`中继承自`W2-R4`的规则，并为D31旧时序增加“已由D32覆盖”的历史标识；不新增D33，不改变D32、`W2-C2`或`W2-R5`的任何业务语义。

SDK属于项目组成部分，当前工程基线固定为Loop Graph SDK `0.2.0@401d3e9bfa49e630196caefbabd732a3209b17a0`。仓库源码目录、`pi-study-helper`依赖和锁文件必须对应同一提交；普通成员不得自行修改SDK源码、版本、哈希或依赖来源。升级由负责人批准，并须同步应用适配、合同和完整回归。SDK哈希不写入Profile、会话或HTTP业务数据。

## 1. 冻结结果总表

| 编号 | 已冻结决策 | P0影响 | 权威合同 |
|---|---|---|---|
| D01 | 正式采用“知识点掌握底座 + Pandas五维画像”双层模型 | 五维在诊断完成后生成，学习单元后异步更新；掌握度仍由程序决定 | 21、11、13 |
| D02 | 官方事实 + 教程解释 | 技术冲突官方优先；来源登记后才可进入 active Profile | 24、02、07 |
| D03 | 固定七列和清洗规则 | 唯一 `clean_df`、城市映射、重复选择和容差按 24 执行 | 24、07 |
| D04 | `profile.json` 是 Profile v2 唯一清单 | 核心字段严格校验，允许 `x-` 扩展，v1 只读兼容 | 21、24、02 |
| D05 | `LearningRuntimeFacade` 唯一入口 | 固定17个方法；Web、HTTP、Pi/TUI不得另建入口 | 22、16 |
| D06 | `ModelExecutionPort` 单次调用，`ReviewOrchestrator` 串行编排 | Generator→Hunter→条件Defender→Judge；失败固定降级 | 23、03 |
| D07 | Python正式评测五阶段协议 | Node权威、Pyodide仅预览；双进程、结构化结果和错误优先级固定 | 25、12、15 |
| D08 | 源码分层和第一周最小文件集合固定 | 不得重复创建 Facade、仓储、模型端口或评测协议 | 21、22、23、25 |
| D09 | 普通 `textarea` 为编辑器 P0 | 草稿、预览、提交、反馈必须有；Monaco不属于P0，只能按D16条件门禁接入 | 12、04 |
| D10 | Pandas代码补全和独立实操均为 `profile_fixed` | 完整AI代码题仅为赛后候选；D16关闭六周实施，不建P0服务、缓存和运行接口 | 23、12、14 |
| D11 | `LearningSessionRepository` 是正式 Evidence 唯一提交者 | 活动仓储只能保存草稿、轨迹和候选；幂等提交一次 | 21、22、12 |
| D12 | 学习测试和正式评测隐藏集物理隔离 | 固定路径和访问边界，不由实现者自行选择 | 24、25、14 |
| D13 | 完整AI代码题生成只保留赛后候选设计 | D16关闭六周实施；未来未通过安全、可复现、可信Oracle、审核和兜底门禁不得启用 | 23、12、14 |
| D14 | 负责人统一拍板，组员按合同实现 | 缺口只能问题单；验收不临时改规则 | 26、19、18 |
| D15 | OpenAI兼容模型接口和默认运行参数 | 默认`deepseek-chat`；环境变量替换端点、模型和密钥；固定温度、超时、重试与会话预算 | 21、03、06、25、26 |
| D16 | 六周关闭能力总裁决 | 外部评测器、完整AI代码题、GoA/OAEO、SSE和批量离线生成关闭；Monaco仅保留W5条件门禁 | 21、01、04、06、10、12、14、15 |
| D17 | 运行时CIDPP自动裁决 | W4周日18:00按V4-1至V4-7自动关闭或保留，不进入周门禁 | 21、03、06、25 |
| D18 | 前端第三方依赖白名单 | 只允许冻结的React/Vite运行、构建和测试依赖；Monaco包受D16门禁约束 | 21、04、26 |
| D19 | 人工标注仲裁和组内盲评安排 | gold分批双标、负责人终裁、生产者回避；人员职责按岗位冻结 | 21、05、06、18、23、26 |
| D20 | 负责人批准机制 | 随时受理；口头或群消息批准须当日补正式问题单，否则不生效 | 21、19、18、22—26 |
| D21 | SDK统一工程基线 | SDK源码、应用依赖和锁文件统一为`0.2.0@401d3e9bfa49e630196caefbabd732a3209b17a0` | 21、09、25、根README |
| D22 | Pandas诊断题严格Schema | 沿用11号字段；P0仅制作和执行单选、判断，答案资产物理隔离 | 28、11、07 |
| D23 | 诊断题首次提交锁定 | 同请求幂等；同题使用新请求再次提交返回`diagnostic_answer_conflict` | 21、28、11 |
| D24 | 诊断Evidence完成时批量提交 | 逐题只保存答案和Evidence候选；`completeDiagnostic`一次原子提交正式Evidence与KnowledgeState | 21、28、11 |
| D25 | Pandas诊断全部题已处理后完成 | `submitDiagnosticAnswer`以`action`严格区分`answer`与`skip`；跳过不计分、不产Evidence，无其他有效直接证据时对应知识点为`unverified` | 21、28、11、07 |
| D26 | Pandas Profile revision 2迁移 | 从W1 draft revision 1迁移到07号正式知识点ID并新增`basic-python`，同步全部引用 | 28、07、02 |
| D27 | 第二周使用完整7天 | D1—D5实现和资产，D6完成gold与复验，D7负责人签署；不提前启动W3 | 27、06、29—33 |
| D28 | Evidence与KnowledgeState采用11号完整模型 | 删除21号旧精简类型；nullable mastery、五状态、来源/形式/影响和可复算元数据进入唯一公共合同 | 21、11、28、29 |
| D29 | 诊断快照进入会话原子事务 | `completeDiagnostic`通过一次commit共同公开Evidence、KnowledgeState、LearnerDiagnostic和版本 | 21、11、28、29 |
| D30 | Pandas诊断资产严格字段闭合 | 唯一ID、数值范围、未知字段、引用、答案一一对应和`required`语义全部冻结 | 28、11、29、30 |
| D31 | 第二周起始提交、候选审计与D4—D6节奏纠错 | V2-1运行负责人任务包提交；D4仓库外候选包；D5冻结案例哈希后双标；E准备工作前移 | 06、27—33 |
| D32 | 第二周D2—D6负载再平衡与验收状态 | D2并行准备、D3 A上传、D4并行推进gold冻结链与D材料上传链、D5 B→C、D6通过盲标资格检查后才B→E；规范化内容哈希而非ZIP哈希绑定候选；V2-1分D1/D6两阶段，V2-8按`BLOCKED→PENDING_OWNER_ADJUDICATION→PASS`条件流转 | 06、27—33、28 |
| D33 | 第二周V2-3审计临时环境基线 | `W2-V2-3-ENV-1`固定`pandas==3.0.5`；版本不匹配必须在资产判断前返回`environment_mismatch/BLOCKED`，不得误报B资产缺陷；该基线不等于最终产品环境锁 | 06、27、28、31、33 |

## 2. 已生效的版本与状态规则

- `profileRevision`：资料包修订，正式资产变化递增；进行中会话继续绑定旧修订。
- `evidenceVersion`：正式 Evidence 事务版本；只能由会话事务提交。
- `capabilityProfileRevision`：五维画像独立版本，关联证据版本、资料包修订、模型和提示版本。
- `sessionVersion`：会话写入版本；过期写请求返回 `session_version_conflict`。
- 五维画像无直接证据时为 `unverified`；AI评分失败保留旧快照，不阻断学习。
- CIDPP只评价教学内容五维（清晰性、完整性、深度、实用性、针对性），不得进入学习者画像。

## 3. 尚可实测但不可自行决定的项目

Python、Pandas、Pyodide、Node.js版本，代码评测墙钟/输出/代码/数据大小上限，Windows进程树终止能力，内存和网络限制，以及模型实际延迟、实际用量和缓存命中率，原则上只冻结测量方法和责任。最终数值须由原型证据写入 `environment-lock.json` 和变更记录，组员不得猜测或伪造。D33仅以两套独立复验结果冻结第二周V2-3的临时审计基线，不提前冻结最终产品环境。D15已经冻结模型调用上限；D16已经关闭六周内外部评测器替换，不再把它列为待实测决定。

### D33 第二周V2-3审计临时环境基线

> **2026-08-03负责人执行补充**：本补充不新增决策编号，不改变`W2-V2-3-ENV-1`的硬版本值；它覆盖本节原第5项中“由C新增版本不匹配负向作者测试”和“由C再次在3.0.5环境完整运行V2-3”的本次收口要求。

1. 裁决标识固定为`W2-V2-3-ENV-1`，唯一硬版本值为`W2_V2_3_AUDIT_PANDAS_VERSION = 3.0.5`。
2. 该值只适用于第二周D4—D6的V2-3候选验证、正式Git绑定和独立复验。Python补丁版本与平台必须写入机器证据，但本裁决不把它们冻结为最终环境门槛。
3. C的V2-3入口必须先做环境预检。`pandas.__version__ != "3.0.5"`时，在读取、执行或评价B资产前结束，输出`status: "BLOCKED"`、`v23Status: "BLOCKED"`和`environment_mismatch`分类，并使用非零退出码；不得输出`b_asset_defect`，不得生成负Evidence。
4. C获准仅在`pi-study-helper/scripts/w2-data-validation/`内修改验证入口、作者测试、最终报告和交接清单，并新增审计专用`pi-study-helper/scripts/w2-data-validation/requirements-w2-v23.txt`，其有效依赖行固定为`pandas==3.0.5`。该授权不包括修改`package.json`、`package-lock.json`、SDK、B资产或Profile的`environment-lock.json`。
5. 此后任何岗位发现实际运行环境与合同指定版本不一致时，先停止对资产或实现作结论，在仓库外创建全新的独立虚拟环境，按合同或对应`requirements`安装目标版本，再执行测试。证据必须记录Python、关键依赖、平台、命令和退出码；不得修改应用依赖、锁文件或Profile环境锁来迁就本机环境。
6. 本次C收口的负责人独立证据固定为：在`pandas 2.3.3`环境中，入口以退出码2返回`BLOCKED/BLOCKED`，唯一分类为`environment_mismatch`、blocker非空且B资产读取次数为0；在`Python 3.13.14 + pandas 3.0.5`独立虚拟环境中，完整V2-3为PASS。基于上述证据，负责人免除C新增版本不匹配负向作者测试，并免除C重复运行3.0.5完整V2-3。
7. C必须在最终报告和交接清单中把第6项明确标为“负责人独立复验”，不得冒充C执行；C仍须保留环境预检、审计专用requirements、正式Git内容哈希绑定、真实命令和证据归属。该豁免不降低错误归因、资产零读取和报告真实性要求。
8. 若B冻结清单内67项、资产树和`final-60`内容均未变化，本次只修C工具及报告，不触发B资产重建、gold输入重新冻结或既有标注作废。若C发现B内容实际变化，仍按D32重新冻结规则处理。
9. 第3至5周选定正式Node.js/Python/Pyodide执行方案后，C与负责人仍须用原型证据填写最终`environment-lock.json`、`environmentHash`和能力标志，并在最终环境中重建或验证指纹；不得把本裁决直接复制为成品环境锁。

## 4. D15—D20执行细则

### D15 模型接口与默认配置

- 模型端口使用OpenAI兼容接口。宿主环境变量固定为`OPENAI_MODEL`、`OPENAI_BASE_URL`和`OPENAI_API_KEY`；`OPENAI_MODEL`未设置时默认`deepseek-chat`，真实调用必须提供Base URL和API Key。
- Generator、诊断解释、学习建议和上下文问答温度为`0.7`；Hunter、Defender、Judge、CapabilityScorer和CIDPP评价温度为`0.2`。
- 单次调用超时60秒；结构校验失败在初始调用后最多重试2次；单会话累计token预算上限为100000，超限直接使用固定fallback。
- W4冻结后不得临时切换默认模型。模型ID必须进入缓存键、裁剪轨迹、录制响应元数据和盲评运行配置；密钥只存在于宿主安全环境。

### D16 六周关闭能力和Monaco门禁

- 外部代码评测器、完整AI代码题生成、GoA/OAEO、SSE推送和批量离线生成在六周内全部关闭，仅保留赛后设计或调研记录。
- `textarea`始终是P0。Monaco仅在W5周五18:00前预跑V5-1至V5-8全部通过且E书面确认有余力时，允许周六进行纯前端替换；周日必须重跑完整V5。任一复验失败立即恢复`textarea`并关闭Monaco，不得改动后端、DTO或评测链。

### D17 运行时CIDPP自动裁决

W4周日18:00前，若V4-1至V4-7任一未通过，或运行时CIDPP尚未完成，运行时CIDPP自动关闭，只保留构建期内容质量摘要，无需再次开会。全部通过且已经接入时，运行时CIDPP仍只允许单一评价Agent、最多一次优化、超时即降级，失败不阻断学习。

### D18 前端依赖白名单

- 运行依赖：`react`、`react-dom`、`react-router-dom`、`zustand`。
- 开发与测试依赖：`vite`、`@vitejs/plugin-react`、`typescript`、`vitest`、`@types/react`、`@types/react-dom`、`jsdom`、`@testing-library/react`、`@testing-library/user-event`、`@testing-library/jest-dom`。
- 只有D16门禁触发后才允许`monaco-editor`和`@monaco-editor/react`。不引入UI组件库和图表库；样式使用CSS，路径图和雷达图使用原生SVG。
- 白名单是后续安装授权，不表示依赖当前已经安装。任何清单外运行或开发依赖必须先提交问题单。

### D19 标注、盲评和材料职责

gold采用双人独立标注，分歧先按预冻结口径协商一次，仍不一致由负责人按同一口径终裁并留记录；全部仲裁必须在系统正式输出产生前完成。20组盲评采用组内交叉回避，生产者不得评价自己的内容，配对随机匿名并在全部回收后解盲；组内评审的独立性限制必须写入效果报告和限制报告。

| 职责 | 冻结岗位 |
|---|---|
| gold第二标注人 | E |
| 内容第二评审 | A |
| 盲评轮换安排人 | 负责人 |
| 试用者招募负责人 | E |
| 演示视频制作 | D |
| PPT制作 | B |

### D20 批准机制和安装策略

负责人随时受理来源清单、CSV/`clean_df`、诊断题、`draft→active`、降级动用和合同修订等批准事项。正式通道为19号问题单；口头或群消息批准必须在当日补齐问题单和书面结论，否则不生效。

六周内不调整`allowScripts`或现有依赖版本，不运行`npm audit fix`。第一周基线报告记录的安装脚本提示以及1项高危、1项中危间接依赖告警继续进入限制报告，不作为当前业务测试失败，也不得由组员自行修复。

## 5. D22—D32第二周实施细则

### D22—D25 诊断合同

- 诊断蓝图沿用11号的`blueprintId/profileRevision/goalIds/estimatedMinutes/minimumCoverage/questions/scoringVersion`及`DiagnosticQuestion`字段名，不再使用`stem`、`primaryKnowledgePointId`或`true_false`等第二套名称。
- Pandas P0资产只出现`single_choice`和`judgment`；单选答案是题目选项中的唯一字符串，判断答案是布尔值。代码阅读和输出预测保留在领域设计中，但本周不制作、不实现评分分支。
- 同一`questionId + requestId`重复请求返回首次结果；同一诊断题已经判定或跳过后，使用不同`requestId`再次提交返回`diagnostic_answer_conflict`，不覆盖、不新增候选。
- `submitDiagnosticAnswer`固定使用`action: "answer" | "skip"`判别联合：回答分支必须携带合法答案并保存不可变答案记录和Evidence候选；跳过分支禁止携带答案，只保存不可变跳过记录，不创建Evidence候选、不写0分。非法组合返回`diagnostic_answer_invalid`；同一幂等键对应不同`action/answer`返回`idempotency_conflict`。两种分支均不公开正式Evidence。
- Pandas蓝图固定`minimumCoverage=1`，每题必须处于`answered`或`skipped`；`completeDiagnostic`通过`LearningSessionRepository.commit`一次性提交全部正式Evidence、KnowledgeState、会话版本和诊断快照。不得用空字符串、特殊字符串、布尔哨兵或`null`表示跳过，不新增Facade方法，也不保留缺少`action`的v2兼容输入。
- `skipped`不生成Evidence、不写0分，并将该题的知识点登记到待检查的`insufficientKnowledgePointIds`；聚合后该知识点没有其他有效直接Evidence时才保持`unverified`，已有其他有效直接Evidence时按正常公式计算。完成事务失败时不得留下部分Evidence或递增一半的版本。

### D26 Profile revision 2迁移

- 当前Git提交`f6c8396`中的`pandas-cleaning-v2-draft`是未启用的revision 1；W2在同一draft工作目录形成`revision: 2`、`revisionOf: 1`和`version: "0.2.0-draft"`。revision 1由Git历史和迁移记录保留，不额外复制运行目录。
- 正式ID固定为`basic-python`、`pandas.clean.read-csv`、`pandas.clean.inspect-dataframe`、`pandas.clean.missing-values`、`pandas.clean.duplicate-orders`、`pandas.clean.type-format`、`pandas.clean.validate-result`。
- B必须同步goals、activities、sources、诊断、兜底题、TaskBundle和章节引用；`source-registry.json`迁移为`source-map.json`并更新`profile.json`。任何旧ID、悬空引用或混合revision都阻止启用。
- revision 1从未active且没有绑定会话，因此本轮不提供双版本运行兼容；若审计发现已有会话证据，立即停止迁移并提交问题单。

### D27 第二周节奏

第二周完整使用7天。D1—D5完成基线、领域实现、资料包和反向审计；D6完成首批20个gold双标、独立复验和阻塞修复；D7只做最终复验、负责人批准和Go/No-Go。无论D5是否全绿，都不得在D6提前启动第三周功能。

### D28—D30 公共类型、事务和诊断Schema纠错

- 11号第6—8节的完整`Evidence`、`KnowledgeStatus`和`KnowledgeState`是唯一算法及公共类型来源。21号旧`EvidenceResult`、四状态`KnowledgeStateValue`和非空`mastery`合同删除，不保留并行兼容层。
- `LearnerDiagnostic`统一使用`insufficientKnowledgePointIds`。`SessionCommitCandidate`必须能携带诊断快照候选，提交后快照可由`SessionSnapshot`恢复；不得把诊断快照写到事务外。
- 有一条以上诊断Evidence时，整批Evidence共享一次递增后的`evidenceVersion`；全部题均跳过时不生成Evidence且`evidenceVersion`保持不变。两种情况均只递增一次`sessionVersion`并原子保存KnowledgeState和诊断快照。
- 诊断蓝图、题目和答案的唯一ID、字段白名单、数值范围、引用闭合及一一对应规则按28号`W2-R5`中继承自`W2-R4`的严格诊断Schema执行。`required=true`表示关键覆盖题，但仍允许显式跳过；跳过只形成证据不足标记。

### D31 W2起始提交和D4—D6执行纠错（历史规则；执行时序已由D32覆盖）

以下条款保留用于审计D31当时的裁决内容。其中涉及D4—D6的日期、上传顺序、冻结时点和V2状态的规则均已由后续D32覆盖，不得脱离D32作为现行执行依据。

- 第二周不可变周起点固定登记为`W2_START_COMMIT = f343a6c1c630f362f4686e6f6b0f50c6577d5562`。D1身份检查时E必须验证当时`HEAD == W2_START_COMMIT`，并验证`8785c6e`是其祖先；V2-1不得检出旧提交后运行。
- `W2_START_COMMIT`只表示不可变周起点，不等于D2-D3持续开发的工作HEAD。负责人本次合同勘误进入`main`后，A必须在开始或继续D2-D3实现前拉取最新`origin/main`；A的实际工作HEAD只需以固定周起点为祖先。A申请D3上传锁时必须同时报告固定`W2_START_COMMIT`和拉取后的实际HEAD，不得把调查时观察到的`50834a7`或其他后续提交改写成周起点。
- D4由B生成仓库外候选ZIP、旁路SHA-256和文件清单供A/C/E在独立临时目录审计；候选包和过程回执不进入Git。
- D5按B正式上传、C反向验证、负责人冻结`final-60`哈希、D上传离线材料执行。B/E只可在负责人冻结案例哈希后开始独立gold。
- E的V2脚本、claim模板、指标骨架和V2-7扫描准备前移到D1—D5；D6完成双标封存、B标注上传和首次完整V2复验，D7只处理阻塞、受影响项复验和负责人签署。

### D32 D2—D6负载、候选冻结和V2-8状态

- D32主要覆盖D31的第二周执行时序；2026-07-30同版本勘误只补齐D25既有显式跳过语义的公共输入表达，不改变17个Facade方法、评分、事务或gold仲裁语义。21号仍为`W2-C2`，第二周执行合同仍为28号`W2-R5`。
- 正常上传顺序固定为：D2无上传；D3由A上传；D4由D上传；D5按B→C上传；D6只有双封存和盲标资格检查通过时才按B→E上传，检查失败时禁止原始标注入Git；D7只有阻塞文件所有者、E受影响项复验和负责人最终上传。
- D4在A的D3正式提交之后分为两条并行链：gold链由B生成仓库外冻结候选，A/C/E完成审计、C完整执行V2-3后由负责人冻结gold输入资产树和`final-60`哈希，B/E才可开始独立标注；D材料链由A/E完成审计后允许D上传，不等待C的V2-3或负责人gold冻结。D4 18:00前gold链未完成审计、V2-3和冻结，当日不得启动标注，但不阻塞已经通过自身审计的D材料上传。
- **`W2-GOLD-AUTH-CORR-1`一次性执行勘误**：上述18:00规则仍是默认合同。本批次所有技术门禁在D4晚间完成，负责人实际书面批准自`2026-08-01T19:37:30.5739587+08:00`立即开始B/E首批20例独立标注；原“D5生效”记录属于授权时间误写。B/E只有在交接声明中登记实际开始时间晚于该生效点，并继续满足互不可见、冻结输入未变和D6中午前封存时，标注才具备资格检查条件。该勘误不批准原始标注提前入Git，不改变D6的双封存、哈希和独立性检查。
- manifest和冻结记录必须按28号`W2-R5`的规范化算法绑定A提交号、Profile/scoring版本、规范化相对路径、逐文件SHA-256、资产树SHA-256、`final-60` SHA-256、D4候选V2-3报告SHA-256及诊断/KnowledgeState前置摘要SHA-256。ZIP哈希只证明传输包，不能替代内部内容哈希；冻结记录自身哈希登记在D7门禁记录中，不写入自身。
- 候选冻结前后若冻结清单内文件变化，B必须重建manifest和资产树哈希，A/C/E复验受影响项，C重新确认V2-3，负责人重新冻结；相关标注在重新冻结前不得继续，已经受影响的标注必须作废重做。
- D5 B提交必须与D4冻结候选逐文件一致。C在Git提交上核对规范化路径、逐文件哈希、资产树哈希和`final-60`哈希；完全一致时以最小复现把D4完整V2-3绑定到正式提交，不一致时不得使用旧报告。
- V2-1分为两个阶段：D1要求`HEAD == W2_START_COMMIT`并保存基线记录；D6要求`W2_START_COMMIT`是最新HEAD祖先、D1记录仍有效，并在最新HEAD重跑全部基线命令。不得在D6继续要求HEAD等于起始提交。
- D6中午前B/E分别完成并封存原始标注、登记SHA-256。只有两份均覆盖final-001至final-020、Schema有效、绑定同一冻结输入、哈希未变，且两名标注者未互相查看标注、未接触系统路径或正式输出时，盲标资格检查才通过，V2-8记为`PENDING_OWNER_ADJUDICATION`。任一标注未完成、未封存、哈希变化或独立性不成立时，V2-8必须记为`BLOCKED`；B/E均不得上传原始标注，E不得拉取、查看B标注或启动最终完整V2，负责人不得签署W2 GO。问题转D7处理；负责人完成有效修复、分歧终裁、生成三份正式gold并登记哈希后，V2-8才可转为`PASS`。
- E在D3登记验证工具基线哈希。D4以后只允许修复有复现证据的工具缺陷，必须登记原因、修复前后SHA-256和受影响V2项并重跑受影响项；不得降低断言、放宽门禁或修改预期结果来换取PASS。D6首次完整复验中才发现的工具缺陷将对应项记为`BLOCKED`，转D7由E条件修复并由负责人复核。
- 本周所有负责人批准只表示候选可继续验证。Pandas Profile revision 2全周保持`draft`，不得把来源、数据、诊断、哈希或候选审计通过写成Profile已经`active`。

### `W2-D7-CLOSEOUT-1` 第二周E交付一次性收口补充

本补充于2026-08-05签发，只处理本批第二周D7收口，不新增D编号、不升级`W2-C2/W2-R5`，也不修改D34—D43第三周规则。

- B既有独立性声明已取得并继续有效。声明引用的`7d80d2fe78299326e624c10cf75301191119fe061a48ab6f1d2a5d7a84191672`是授权勘误前冻结记录JCS；`W2-GOLD-AUTH-CORR-1`产生的`6e99fc788037c7f9bda72f5d7eeffcc51b1c0a35ee216e635447543882bf2504`只替换授权元数据。67项冻结内容、`final-60`、B原始标注及`69bfd3a4086a7a6c9c46134d69d0cdda3eab558ac13a0a45115476c6aa33feab`不变，不要求B重做声明或重新封存。
- E必须提交实际使用的V2-7 canary和原两输入机器结果原件；目标SHA-256分别为`59018cc6734d09eddbb0271bac0ab9e9de9f2823b77f4eafc9554d58735f38e1`和`3ee729099433c8c396f538e0d3f14a638c1adbd8c88ff924431b01e67bf2a1fd`。原件遗失时只能标记为重建运行并登记新哈希和原因，不得冒充原件。canary、输入manifest、机器结果和sidecar仓库外封存，Git只登记哈希、命令、逐输入计数和结论。
- 本批V2-3新E工具不降标确认由负责人替代C完成。负责人已比较综合脚本`de57ab5cb8bb4290459c108c4aff9be9537e23bded213ca72484adc0f800783a→77bc838ce328cf53e72f6cfc0b0128473a20627b4f8c7c9da71909be388fc226`及作者测试`a5985f3234b5a323891be985a6ec2af7c99294ce45b13ec5e304075e5c7bb8a0→e35b48160ed5e81d75b2563cfe02a8a3d6489501e8a2b15ce88dd2930d65730b`：六项真实输入、67项、manifest JCS、D33环境证据、三次稳定运行和错误实现拒绝要求均未删除或放宽；V2-3相关差异只取消C历史报告重复负责人专用措辞的要求，不改变机器判据。真实六输入作者测试`26/26 PASS`，现登记`OWNER_NO_LOWERING_CONFIRMATION / V2-3=PASS`。本补充免除C再次书面确认和重复运行完整V2-3，不覆盖C既有正式证据。
- E获一次性权限在仓库外基于真实safe DTO确定性构造普通日志和安全导出测试视图，不得修改A/C/D产物、生产接口或正式仓库文件。完整V2-7输入固定为safe DTO、普通日志测试视图、D录制响应、安全导出测试视图、C的`v2-3-final-report.md`和D的`d4-validation-report.md`；E的W2验证记录是结果载体，不递归扫描自身。五类表面逐输入六类canary均为0后才可PASS。
- 本批E标注精确开始时间不可恢复，不作为盲标资格阻塞。负责人只接受并登记真实时间链：授权`2026-08-01T19:37:30.5739587+08:00`、最早可审计内容时间`2026-08-01 21:33:42 +08:00`、完成时间`2026-08-01 21:34:26 +08:00`；E不得伪造精确开始时间。覆盖、Schema、冻结绑定、封存哈希和独立性仍必须逐项PASS。
- 资格通过后，本批首20例取消B/E协商，由E生成不改变原始标注的机械差异清单，负责人直接逐案终裁。`adjudication-log.jsonl`的协商状态登记为`SKIPPED_BY_W2-D7-CLOSEOUT-1`；三份正式gold仍只能由负责人生成并登记哈希。本补充签发时第三周剩余40例仍执行`W3-C1/W3-R2`；该第三周规则后来由D44/`W3-C2`单独覆盖，不倒改本批第二周事实。

#### E岗位唯一整改入口

E必须在拉取包含`e7136b320f2ce28edc0185dcf52c9d0e67288f7c`及后续执行单的最新`origin/main`后，严格执行 [E岗位W2-D7-CLOSEOUT-1一次性整改执行单](../第二周任务/E岗位W2-D7-CLOSEOUT-1一次性整改执行单.md)。该执行单只展开本节既有裁决，不新增决策编号：V2-3直接消费`OWNER_NO_LOWERING_CONFIRMATION/PASS`；ordinary log和safe export由E仓库外构造；C/D报告从正式Git读取；V2-7固定六输入；负责人签署双封存后V2-8为`PENDING_OWNER_ADJUDICATION`；E再提交20例机械差异、更新验证记录和交接、七件工具逐文件哈希及最新HEAD的`verify`结果。任何继续等待C确认或等待上述V2-7输入“被提供”的记录均视为未消费现行合同。

## 6. D34—D44第三周实施裁决

| 编号 | 冻结问题 | 负责人裁决 | 主要同步文档 |
|---|---|---|---|
| D34 | 周期与上传节奏 | D0单独签发；D1—D5按完整工作日；无小时级节点；每天最多两次Git上传；能单日独立闭环的任务不得拆日；正常D4收口，D5只处理真实阻塞，必要时启用不开发新功能的条件D6 | 06、34—40 |
| D35 | 第三周起点和生效 | `W3_START_COMMIT`必须等于W2最终GO提交完整哈希；未回填及未签发D0时任务包不生效；未签署W3 GO不得启动W4 | 00、06、34、35 |
| D36 | Profile revision 2激活 | W2 GO后，等待B正式TaskBundle和C正式环境锁均入库；D3由负责人核对后批准，A通过Profile仓储事务从draft激活；禁止手工移动或改写active目录；新会话只消费实际active revision | 09、21、34—36 |
| D37 | PathEngine | 唯一版本`path-engine-v1`；使用11号稳定拓扑排序、预算压缩、`skipEligible`、`path_infeasible`和后缀重算规则 | 06、11、35、36 |
| D38 | W3正式任务范围 | 只实现`act-inspect-dataframe`代码补全和`act-practical`最终实操，不扩大任务数 | 06、12、35、37、38 |
| D39 | 评测结果分层 | 21号精简`ActivityResult`是唯一公共结果；五阶段、完整Rubric、输出片段和阻断细节只属于C内部协议；`CodeEvaluationPort`只返回公共结果 | 09、12、21、35、38 |
| D40 | Node环境裁决 | W3允许`measured_node_submit`；具体Node/Python/Pandas版本和限制由C在D1完成原型证据，D2负责人批准；Pyodide待W5 | 06、09、12、35、38 |
| D41 | 剩余40例与正式gold | 原始标注固定为`b-final-021-060.jsonl`和`e-final-021-060.jsonl`；不得覆盖W2文件；正式gold按`final-001`至`final-060`追加，前20例内容不变并登记新全量哈希 | 05、06、35、37、40 |
| D42 | W3前端范围与版本 | 只做开始、诊断、路径、学习、活动、总结六页mock；不接真实HTTP、Pyodide或审核页；依赖使用冻结白名单固定版本 | 04、06、09、21、35、40 |
| D43 | E安装权限 | 仅E可安装React/DOM 18.3.1、Router 6.30.1、Zustand 5.0.14、Vite 7.1.7、plugin-react 5.0.4、jsdom 26.1.0，并保留TS 5.9.3/Vitest 4.1.10；不得升级SDK、其他依赖或`allowScripts`，不得运行`npm audit fix` | 04、09、21、35、40 |
| D44 | 第三周剩余40例gold仲裁 | 保留B/E独立双标、互不可见和分别封存；双封存资格PASS后由负责人生成机械差异清单并直接逐案终裁，B/E不查看对方原始标注或机械差异清单、不参加协商、不修改封存原件；正式gold仍只能由负责人生成，本批`negotiationStatus`固定为`SKIPPED_BY_D44` | 05、06、17、18、21、28、34、35、37、40 |

### D34 上传和条件D6细则

`W3-R2`正常上传顺序固定为：D1 `A→B`；D2 `负责人→C`；D3 `A→D`；D4 `E→负责人`；D5仅条件上传且最多两次。A的D1提交不执行Profile激活；D3必须绑定B正式TaskBundle和C正式环境锁后再由A执行事务激活。B的D1上传还必须等待B/E双封存以及负责人Schema、覆盖、输入哈希和独立性资格检查全部PASS，仅有E封存声明不足以获锁。D2由负责人生成机械差异清单，D3由负责人直接终裁；B/E不参与协商。E在D3只准备V3工具、输入清单和测试用例，D4才可基于A/C/D正式提交及负责人只读gold候选执行完整V3。每次上传前持锁者必须拉取最新main、报告起点与实际HEAD、运行岗位门禁并释放上传锁。岗位候选已完成但因上传名额排队时必须保持哈希不变，不得把等待写成新增开发。D4有上游硬阻塞时，D5按“一个阻塞文件所有者→E”执行，负责人签署转入D6；仅E记录需修正时D5可按`E→负责人`收口。多个独立修复需求或D6仍有BLOCKED时直接NO-GO。

### D35 生效登记

```text
W3_START_COMMIT = f190326a4a906b46e4001484ffa30a7839b82ed2
W3_CONTRACT = W3-C2
W3_SCHEDULE = W3-R2
W3_D0_STATUS = EFFECTIVE
W3_D0_SIGNED_AT = 2026-08-06
```

被回填的`W3_START_COMMIT`已包含W2的E最终验证正文、三份首批正式gold、哈希和负责人GO签署。D0由负责人签发，岗位成员不得改写该周起点；后续第三周提交必须以该提交为祖先。

## 7. 生效与引用顺序

本记录 → 21 号公共合同总册 → 五份岗位任务书 → 00—18 号设计文档 → 组员实现与测试。任何未与本记录一致的旧段落均以本记录和公共合同总册为准，相关内容必须在同步时删除或标注为历史说明。
