# 53 岗位A第五周任务书

> 所有权：W5公共DTO、应用层安全投影、TUI共享会话和深链
>
> 正常上传：D1、D2、D4，分别解除C、E和最终复验阻塞

## 0. 开工输入

确认`HEAD`包含`W5_START_COMMIT=4e316822d343d90bdf295f37b7aaaa0131890501`及负责人D0提交；必读20号D61—D72、52号第2、6节、12号第12—17节、04号TUI/恢复设计及W4 A/C/E交接。

## 1. D1：公共合同与安全投影

- 在既有contracts中落实`PublicExecutionBundle`和公开运行请求/响应；不新增第二套Facade。
- 应用服务从服务端准备状态确定性投影公开数据、公开测试和摘要；拒绝私有测试、Rubric、参考实现和宿主路径。
- 校验session、activity、revision、environment、过期时间和bundleHash；错误沿用公共错误体系。
- 向C交付可消费入口、类型导出、固定测试实现和负向用例。

出口：公共类型/投影定向测试、泄漏反例、逐文件哈希和`handoff-w5-a-d1.md`；获锁后上传。

## 2. D2：TUI共享会话与深链

- D2为实现和接口交接日；在B正式seal、C正式环境证据和E最终页面到位前，不得把本日结果写成跨端正式闭环。
- Pi/TUI只通过既有Facade读取`getNextStep`和恢复视图，不直接读仓储。
- pending活动保存安全ID和服务端版本；Web不可用不标完成、不改题型。
- 深链只含`sessionId/nodeId/activityId`，失效时引导从Web开始页恢复，不自动新建会话。
- Web/TUI陈旧写入按CAS冲突处理；同一幂等键不重复提交。

出口：TUI深链、pending、失效/冲突/恢复测试及`handoff-w5-a-d2.md`；向E交付安全路由示例。

## 3. D4：跨端正式复验

拉取B正式环境锁和seal、C正式服务、E正式Web候选。在干净数据目录证明TUI进入同一session、Web继续、正式提交后TUI读取同一Attempt/Evidence/下一步；反向再跑一条。只修A所有权缺陷，不代改页面或环境锁。

对B提供的三类展示案例输入，A必须使用正式PathEngine运行并保存实际路径/状态输出及输入哈希，供E独立核对；A不得手工制造路径差异。

出口：V5-1/V5-3/V5-4中A责任证据、输入提交/seal绑定和`handoff-w5-a-d4.md`。

## 4. 禁止与停止

允许修改contracts、A应用服务、TUI适配和对应测试。禁止修改C执行器、B Profile内容、D材料、`src/web/`、SDK、依赖、gold或环境测量值。公开投影若无法在不泄漏私有资产的情况下生成，立即停止并报告，不得用删测试或硬编码空包绕过。

申请每次上传锁前运行A定向、受影响回归、全量、typecheck、docs、verify和`git diff --check`，报告三个日次各自精确文件清单。
