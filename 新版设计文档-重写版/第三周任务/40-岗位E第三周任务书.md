# 40 岗位E第三周任务书

状态：`[D0已于2026-08-06生效，按D44/W3-C2及34号W3-R2调度执行]`。E负责剩余40例第二标注、React六页mock、独立V3复验和验证正文；不负责机械差异或gold仲裁，不得替其他岗位修改实现。

## 1. 独立性和安装授权

E在自身40例封存前不得查看B标注、系统对正式60例的路径或输出。E是唯一获准安装W3前端依赖的岗位，只能使用React/DOM `18.3.1`、Router `6.30.1`、Zustand `5.0.14`、Vite `7.1.7`、plugin-react `5.0.4`、jsdom `26.1.0`，保留TS `5.9.3`和Vitest `4.1.10`。禁止SDK/其他依赖升级、`allowScripts`修改和`npm audit fix`。

## 2. 逐日任务

| 工作日 | 任务 | 出口 |
|---|---|---|
| D1 | 一个完整工作日内独立完成`final-021—060`、40/40校验和封存；在封存前不查看B | `e-final-021-060.jsonl`封存SHA；不上传 |
| D2 | 不拉取或读取B原始标注，不查看负责人机械差异清单，不参加协商；一次完成React工程、路由、六页mock、DTO、正常/空/错/冲突/恢复状态和前端测试 | 完整页面候选和前端作者测试；无gold协商交付，不上传 |
| D3 | 准备V3-1至V3-8工具、输入清单和测试用例；A/D上传后只登记待测提交，负责人只读gold候选形成后只登记其哈希；不得提前声明完整V3结论 | 哈希固定的验证工具基线和完整输入清单，不产生最终PASS/BLOCKED正文 |
| D4 | 拉取A/C/D全部正式提交并取得负责人只读gold候选；独立执行完整V3-1至V3-8和全量门禁，完成验证正文后获锁上传E标注、React mock、验证正文和交接 | E正式提交和可供负责人签署的最终记录 |
| D5 | 仅处理D4可复现阻塞并复验受影响项 | 无阻塞则W3已结束，不上传 |

## 3. 六页和DTO标准

开始、诊断、路径、学习、活动、总结均可路由到达，固定尺寸控件不随状态跳动。必须覆盖正常、空、错误、`session_version_conflict`和恢复；活动页包含草稿、运行、正式提交和安全反馈mock。前端只展示DTO，不计算路径、mastery、Rubric、分数或通过结论；不得包含真实HTTP、Pyodide、审核页、隐藏测试或完整Profile。

## 4. V3独立复验

E必须独立执行35号V3-1至V3-8，不采信A/B/C/D报告中的PASS文字。每项记录被测提交、环境、原始命令、退出码、测试项数和证据SHA-256；失败记BLOCKED并点名文件所有者，不降低断言或改预期。

重点包括：20开发案例各10次路径一致且合法；两个任务错误矩阵和各3次一致；扫描C无正式事实写入；提交事务每阶段故障无半写；评测器故障无负Evidence；60例gold先由负责人直接终裁并冻结；六页DTO一致及泄漏扫描。E只验证负责人正式gold候选及其哈希，不读取B原始标注或参与仲裁。

## 5. 正式交付

- `e-final-021-060.jsonl`及封存/输入绑定声明；
- React/Vite六页mock、固定依赖锁、组件/状态/DTO测试；
- V3-1至V3-8中文验证正文、机器证据索引和哈希；
- typecheck、全量测试、文档、extension smoke、release、`verify`和`git diff --check`真实结果；
- E交接清单明确PASS/BLOCKED、未决项和负责人可签署条件。

E不得修改A/B/C/D文件来换取PASS，不得生成正式gold或签署Go/No-Go。D4上传后由负责人复核、终裁和最终提交。
