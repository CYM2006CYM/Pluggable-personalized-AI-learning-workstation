# 40 岗位E第三周任务书

状态：`[D0已于2026-08-06生效，按34号调度执行]`。E负责剩余40例第二标注、React六页mock、独立V3复验和验证正文；不得替其他岗位修改实现。

## 1. 独立性和安装授权

E在自身40例封存前不得查看B标注、系统对正式60例的路径或输出。E是唯一获准安装W3前端依赖的岗位，只能使用React/DOM `18.3.1`、Router `6.30.1`、Zustand `5.0.14`、Vite `7.1.7`、plugin-react `5.0.4`、jsdom `26.1.0`，保留TS `5.9.3`和Vitest `4.1.10`。禁止SDK/其他依赖升级、`allowScripts`修改和`npm audit fix`。

## 2. 逐日任务

| 工作日 | 任务 | 出口 |
|---|---|---|
| D1 | 独立标注`final-021—040`；创建React工程、路由、mock DTO和状态机 | 本地结果，不上传 |
| D2 | 完成`final-041—060`、40/40校验和封存；继续六页骨架 | `e-final-021-060.jsonl`封存SHA；不查看B |
| D3 | 资格检查通过后拉取B；参与一次分歧协商；继续DTO/页面和D材料只读检查 | 协商记录、页面候选，不上传 |
| D4 | 独立准备V3-1至V3-8工具和用例；继续页面空/错/冲突/恢复状态 | 验证工具基线和本地候选，不上传 |
| D5 | 拉取A最终提交；运行完整V3和全量门禁；完成六页mock、E标注、验证正文；获锁上传 | E正式提交和可供负责人签署的最终记录 |

## 3. 六页和DTO标准

开始、诊断、路径、学习、活动、总结均可路由到达，固定尺寸控件不随状态跳动。必须覆盖正常、空、错误、`session_version_conflict`和恢复；活动页包含草稿、运行、正式提交和安全反馈mock。前端只展示DTO，不计算路径、mastery、Rubric、分数或通过结论；不得包含真实HTTP、Pyodide、审核页、隐藏测试或完整Profile。

## 4. V3独立复验

E必须独立执行35号V3-1至V3-8，不采信A/B/C/D报告中的PASS文字。每项记录被测提交、环境、原始命令、退出码、测试项数和证据SHA-256；失败记BLOCKED并点名文件所有者，不降低断言或改预期。

重点包括：20开发案例各10次路径一致且合法；两个任务错误矩阵和各3次一致；扫描C无正式事实写入；提交事务每阶段故障无半写；评测器故障无负Evidence；60例gold先冻结；六页DTO一致及泄漏扫描。

## 5. 正式交付

- `e-final-021-060.jsonl`及封存/输入绑定声明；
- React/Vite六页mock、固定依赖锁、组件/状态/DTO测试；
- V3-1至V3-8中文验证正文、机器证据索引和哈希；
- typecheck、全量测试、文档、extension smoke、release、`verify`和`git diff --check`真实结果；
- E交接清单明确PASS/BLOCKED、未决项和负责人可签署条件。

E不得修改A/B/C/D文件来换取PASS，不得生成正式gold或签署Go/No-Go。D5上传后由负责人复核、终裁和最终提交。
