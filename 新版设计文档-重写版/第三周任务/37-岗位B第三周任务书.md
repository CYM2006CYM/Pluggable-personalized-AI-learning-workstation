# 37 岗位B第三周任务书

状态：`[D0已于2026-08-06生效；D45/W3-C3已于2026-08-07生效；按34号W3-R2调度执行]`。D45不改变B的任务范围。B负责剩余40例第一标注和两个固定TaskBundle，不负责路径、执行环境、机械差异或正式gold。

## 1. 输入和独立性

B只基于W2冻结的`final-60.jsonl`、诊断/KnowledgeState前置事实和35号标注Schema处理`final-021`至`final-060`。在B/E分别封存前，B不得查看E原始标注、系统对正式案例的路径或正式输出。聊天摘要、文件名和别人声称的结论不能替代独立性。

## 2. 逐日任务

| 工作日 | 任务 | 出口 |
|---|---|---|
| D1 | 一个完整工作日内独立完成`final-021—060`、校验40/40并封存SHA-256；闭合两个TaskBundle及关联资产；确认E已独立封存，并取得负责人对Schema、覆盖、输入哈希和独立性的资格检查PASS后，才可获锁上传 | `b-final-021-060.jsonl`；两个闭合TaskBundle；B提交；任一资格项未通过则不上传 |
| D2 | 不查看E原始标注或负责人机械差异清单，不参加协商；仅在负责人点名时解释本人已封存字段，且不得修改原件 | 无gold协商交付；事实澄清如有则书面留痕 |
| D3—D5 | 只响应负责人点名的B资产缺陷或V3可复现阻塞；协助说明业务Rubric | 无缺陷不上传 |

## 3. TaskBundle硬要求

任务固定为`act-inspect-dataframe`和`act-practical`，来源均为`profile_fixed`。每项必须包含活动定义、入口/输出契约、fixture索引、公开/隐藏测试、参考实现、至少一个已知错误实现、六维或资产已冻结的确定性Rubric、阻断规则、安全反馈、环境引用和可复算`assetBundleHash`。B不得按C实现方便修改业务含义，也不得自行填写环境限制。

## 4. gold边界

- B只上传自己的`b-final-021-060.jsonl`，不得覆盖`b-first-20.jsonl`；
- 不得读取、复制或重排E原始标注；
- 不得读取负责人机械差异清单，不得与E协商；不得生成或修改正式三份gold，全部分歧由负责人直接终裁；
- 发现冻结输入变化立即停止，报告负责人重新冻结，受影响标注作废。

## 5. 正式交付

- 40/40第一标注、Schema/覆盖检查、输入绑定哈希、封存时间和SHA-256；
- 两个TaskBundle、逐文件哈希/资产树摘要、内容复算命令和B作者测试；
- D1交接清单明确提交号、Profile保持可审计来源及C消费入口。B业务资产不依赖A路径实现，不再设置人为的A提交绑定。

禁止修改PathEngine、Node/Python环境、C适配器、A事务、D材料、React、SDK、依赖和正式gold。
