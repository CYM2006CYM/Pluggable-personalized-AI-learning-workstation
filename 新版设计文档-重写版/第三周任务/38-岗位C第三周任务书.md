# 38 岗位C第三周任务书

状态：`[D0已于2026-08-06生效，按34号W3-R2调度执行]`。C负责Node父进程、Python子进程、Rubric汇总、错误归因和环境证据，公共输出止于`ActivityResult`。

## 1. 逐日任务

| 工作日 | 任务 | 出口 |
|---|---|---|
| D1 | 一个完整工作日内建立父子进程原型，并完成Node/Python/Pandas、临时目录、环境白名单、shell关闭、输出裁剪、超时、进程树终止、公开/隐藏进程隔离、错误分类和限制测量 | 负责人D2可裁决的完整环境报告、原始命令、退出码和证据；不上传 |
| D2 | 拉取B正式TaskBundle和负责人环境裁决；完成两个任务适配器、五阶段内部协议、公共结果映射及正确/错误/故障矩阵；获锁上传 | `measured_node_submit`、V3-3/V3-4/V3-6作者证据及C正式提交 |
| D3—D5 | 只响应A/E复现的C所有权阻塞 | 无阻塞不上传 |

## 2. 环境证据

不得猜测版本或能力。报告必须记录Node、Python、Pandas、平台、命令、退出码、墙钟/输出限制、临时目录清理、进程隔离和进程树终止。未证明网络隔离或可靠内存限制时明确为false/省略。实际环境不同先在仓库外独立虚拟环境按合同版本复验，不修改应用依赖或B资产迁就本机。

## 3. 公共边界

1. `CodeEvaluationPort.run()`只返回21号公共`ActivityResult`。
2. prepare/user_code/public_tests/hidden_tests/summarize、完整Rubric和输出片段只留在C内部协议。
3. C源码不得导入或写入正式Attempt、Evidence、KnowledgeState、PathRepository或Session commit。
4. 正确、部分正确、语法错、运行错、测试失败、超时、超输出和禁用导入必须稳定分类。
5. 依赖、环境、测试资产、协议、harness或父进程故障归评测器，不生成学习者分数事实。
6. 隐藏测试、参考实现、私有CSV和宿主路径不得出现在公共结果、普通日志、Agent或浏览器。

## 4. 正式交付

- 两个任务各连续3次逐字段一致的V3-3作者证据；
- V3-4源码权限扫描和V3-6故障矩阵；
- 环境锁、原型证据、复现命令、作者测试和C交接清单；
- D2提交必须绑定B D1正式提交及其`assetBundleHash`和负责人环境裁决，报告实际HEAD和文件清单。

禁止修改B业务资产、A事务、React、Agent材料、正式gold、SDK或未授权依赖。
