# 39 岗位D第三周任务书

状态：`[D0已于2026-08-06生效；D47/W3-C5已于2026-08-09生效；按34号W3-R2调度执行]`。D负责离线模型材料和权限证据，本周不接实时模型。

## 1. 逐日任务

| 工作日 | 任务 | 出口 |
|---|---|---|
| D1 | 基于W2配置和录制响应，一次完成动态单选/判断schema、固定fallback、超时/无效输出和越权请求样本；联调ModelExecutionPort/Orchestrator边界；完成脱敏、确定性fallback、权限拒绝、作者测试和交接；封存待上传候选 | 哈希固定的完整D候选、作者测试和扫描结果；不因等待上传锁继续扩项 |
| D2 | 保持D1候选不变，只响应负责人对候选的只读问题 | 无问题不修改 |
| D3 | 拉取最新main，核对候选哈希后获锁上传 | D正式提交 |
| D4—D5 | 只响应D所有权的可复现阻塞 | 无阻塞不上传 |

## 2. 必须覆盖

- 动态题只使用Pandas P0 `single_choice`和`judgment`冻结schema；
- 录制响应正常、超时、无效输出、provider错误和固定fallback；
- Agent试图修改mastery、KnowledgeState、路径、Rubric、ActivityResult或gold时确定拒绝；
- 上下文不含隐藏测试、参考实现、私有CSV、原始标注、密钥和宿主路径；
- SDK `GraphRunResult`仍只在ModelExecutionPort适配器边界映射，不扩散到应用层；
- 录制响应通过只证明离线合同，不声明实时模型可用。

D47明确第三周不使用真实Key。候选成功轨迹可由负责人在仓库外用模型生成并完成审计冻结；D只消费或维护无密钥的固定轨迹，并通过假LLM按同一`ModelExecutionPort`和Schema回放。固定集合须覆盖成功、无效JSON/Schema、超时/provider错误、越权拒绝和确定性fallback；报告登记`LIVE_NOT_RUN`，实际fallback登记`MOCK_FALLBACK_USED`，不得冒充在线模型PASS。该补充不扩大D的D1封存范围，也不要求D在D3继续开发。

## 3. 正式交付

录制响应、提示词/配置版本、动态题schema fixture、fallback和越权拒绝测试、脱敏扫描、离线复现命令及D交接清单。全部内容在D1完成并封存；D3仅因每日上传额度排队入库，只上传授权的模型适配、配置、fixture和测试，不上传密钥、审计ZIP或其他岗位文件。

禁止修改确定性评分、PathEngine、Evidence、Node评测、B资产、React、依赖、SDK哈希和gold。
