# 39 岗位D第三周任务书

状态：`[待D0生效后执行]`。D负责离线模型材料和权限证据，本周不接实时模型。

## 1. 逐日任务

| 工作日 | 任务 | 出口 |
|---|---|---|
| D1 | 基于W2配置和录制响应准备动态单选/判断schema、固定fallback、超时/无效输出和越权请求样本 | 本地fixture和测试计划 |
| D2 | 离线联调ModelExecutionPort/Orchestrator边界；验证脱敏、确定性fallback和权限拒绝 | 作者测试和扫描结果，不上传 |
| D3 | 拉取负责人合同提交；完成材料、交接和A/E只读审计整改；获锁上传 | D正式提交 |
| D4—D5 | 只响应D所有权的可复现阻塞 | 无阻塞不上传 |

## 2. 必须覆盖

- 动态题只使用Pandas P0 `single_choice`和`judgment`冻结schema；
- 录制响应正常、超时、无效输出、provider错误和固定fallback；
- Agent试图修改mastery、KnowledgeState、路径、Rubric、ActivityResult或gold时确定拒绝；
- 上下文不含隐藏测试、参考实现、私有CSV、原始标注、密钥和宿主路径；
- SDK `GraphRunResult`仍只在ModelExecutionPort适配器边界映射，不扩散到应用层；
- 录制响应通过只证明离线合同，不声明实时模型可用。

## 3. 正式交付

录制响应、提示词/配置版本、动态题schema fixture、fallback和越权拒绝测试、脱敏扫描、离线复现命令及D交接清单。D3只上传授权的模型适配、配置、fixture和测试，不上传密钥、审计ZIP或其他岗位文件。

禁止修改确定性评分、PathEngine、Evidence、Node评测、B资产、React、依赖、SDK哈希和gold。
