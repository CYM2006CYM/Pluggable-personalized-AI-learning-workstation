# 34 第三周总任务布置与权限边界

状态：`[负责人已于2026-08-06完成D0签发；D46/W3-C4及W3-R2已生效]`。调度修订号：`W3-R2`，公共合同：`W3-C4`。D46只修正前端mock分层和E-D2验收口径，不改变本文排期、上传顺序或岗位所有权。本文负责第三周五个完整开发日的顺序、上传锁、所有权和Go/No-Go；字段和行为以[35号公共合同](./35-第三周公共合同总册.md)为准。

## 1. 生效前提

```text
W3_START_COMMIT = f190326a4a906b46e4001484ffa30a7839b82ed2
W3_D0_STATUS = EFFECTIVE
W3_D0_SIGNED_AT = 2026-08-06
```

负责人已核对以下D0签发条件；第1—4项在签发前满足，第5项以本D0提交推送成功并释放上传锁为完成标志：

1. W2 V2-1至V2-8均为PASS；
2. E的W2最终验证正文、三份首批正式gold、哈希和负责人GO签署已进入同一main提交；
3. 上述提交完整哈希已回填到20、34、35号；
4. `HEAD == origin/main == W3_START_COMMIT`，工作区既有未跟踪材料已登记并保留；
5. D0合同提交完成并释放上传锁。

历史审计提交`6a76d96813516251950de443920102ca48a3b83d`不满足这些条件，未被用于周起点。D0不占五个开发日；D1从任务包生效后的下一个完整工作日开始。

## 2. 本周目标和边界

跑通无需实时模型和真实HTTP的确定性闭环：

```text
诊断 → PathEngine → 活动 → Node正式评测
→ Attempt/Evidence原子提交 → KnowledgeState
→ 路径未完成后缀更新
```

并行完成剩余40例gold双标、两个固定TaskBundle、离线Agent材料和React六页mock骨架。本周不实现真实HTTP、Pyodide、审核页、实时模型、更多代码任务、生产级沙箱或W4功能。

## 3. 调度原则

第三周执行[负责人任务布置原则](../团队协作参考/负责人任务布置原则-单日闭环与真实依赖.md)：一名成员能够在一个完整工作日内独立闭环、且没有真实外部输入依赖的工作，不得为了填满周计划而拆成两天。阶段划分只允许来自正式上游提交、盲标隔离、负责人裁决或独立复验等真实依赖；等待上传锁不等于继续开发。

## 4. 总执行表

| 工作日 | 负责人 | A | B | C | D | E | Git上传 |
|---|---|---|---|---|---|---|---|
| D1 | 双封存后完成Schema、覆盖、输入哈希和独立性资格检查；明确Profile激活等待B/C正式输入 | 完成PathEngine、路径Facade/仓储及V3-1/V3-2并上传；不提前激活Profile | 完成021—060全部40例、封存并闭合两个TaskBundle；仅在E已封存且负责人资格检查PASS后上传 | 一次完成环境、资源、隔离、终止和故障原型证据 | 一次完成离线schema、fallback、脱敏和越权拒绝材料并本地封存 | 完成021—060全部40例并封存，不查看B | `A→B` |
| D2 | 根据C证据冻结Node环境并上传；生成gold机械差异清单，不向B/E开放 | 基于C滚动候选准备正式提交事务，不上传 | 不查看E标注或差异清单，不参加协商；仅响应本人封存字段的事实澄清 | 拉取B和环境裁决，完成适配器、Rubric、故障矩阵及V3-3/V3-4/V3-6并上传 | 只等待上传锁，不继续扩项 | 完成React六页mock、DTO、状态和前端测试；不查看B标注或差异清单，不参加协商 | `负责人→C` |
| D3 | 核对B/C正式输入后批准Profile激活；依据双标原件和机械差异直接终裁，形成只读60例正式候选 | 拉取B/C，通过仓储事务激活Profile v2；完成Attempt/Evidence原子事务、KnowledgeState、路径后缀、幂等和恢复并上传 | 只响应负责人点名的内容缺陷 | 只响应A可复现的接口阻塞 | 上传D1已冻结的离线材料 | 只准备V3工具、输入清单和测试用例；不得在A/D上传和gold候选形成前声明完整V3结论 | `A→D` |
| D4 | 复核E结果，提交60例gold、哈希、验证记录和W3签署 | 仅响应真实阻塞 | 仅响应真实阻塞 | 仅响应真实阻塞 | 仅响应真实阻塞 | 拉取最终提交，完成V3-1至V3-8、全量门禁和验证正文并上传 | `E→负责人` |
| D5 | 仅处理D4遗留的真实阻塞；无阻塞则确认W3已于D4结束 | 仅按可复现问题整改 | 仅按可复现问题整改 | 仅按可复现问题整改 | 仅按可复现问题整改 | 仅复验受影响项 | 条件上传，最多2次 |

每天使用完整工作日，不设置12:00、18:00或其他小时级开始/截止点。每天最多两次Git上传，仓库外审计包和聊天交付不计Git上传，但不能替代合同要求的正式文件。

## 5. 依赖顺序

主实现链：

```text
A路径第一阶段；B两个TaskBundle
→ 负责人环境裁决 → C评测适配器
→ 负责人批准Profile激活 → A激活v2并完成正式提交事务
→ D正式离线材料
→ E独立复验 → 负责人签署
```

gold链：

```text
B/E在D1分别完成40例独立标注 → 分别封存 → 资格检查
→ D2负责人生成机械差异清单 → D3负责人直接终裁并形成只读候选
→ D4正式60例gold
```

B/E不得查看对方原始标注或机械差异清单，不参加协商，不得修改封存原件；本批`adjudication-log.jsonl`的`negotiationStatus`固定为`SKIPPED_BY_D44`。

任何岗位不得因自己的并行任务完成而越过主链。B上传原始标注前必须确认E已封存，并取得负责人盲标资格检查PASS；任一资格项失败时B不得上传。A的D1 PathEngine提交不依赖B/C，也不得提前激活Profile；C的D2正式提交必须绑定B的D1提交和负责人环境裁决；A可以按C滚动候选准备，但D3必须拉取B/C正式提交并在负责人批准后激活同一revision，再完成正式事务上传；E可以先完成mock和验证工具，但完整V3正文只能在D4基于A/C/D正式提交和负责人只读gold候选生成。

## 6. 上传锁和拉取规则

正常上传顺序固定为：

```text
D0 负责人
D1 A → B
D2 负责人 → C
D3 A → D
D4 E → 负责人
D5 条件上传，最多2次
```

每名持锁者必须：拉取最新`origin/main`；确认`W3_START_COMMIT`为祖先；报告实际HEAD、拟暂存清单和工作区状态；只暂存授权文件；运行岗位测试及`git diff --check`；提交后报告提交号、测试和哈希；推送后释放锁。禁止强推、覆盖、删除他人文件、清理既有未跟踪材料或把审计ZIP加入Git。

岗位工作在较早日期已经完成、但因当日两个上传名额已占用而延后上传时，必须保持候选哈希不变；这属于上传锁排队，不得把等待日写成新增开发日。

## 7. D5和条件D6

D4正常完成`E→负责人`后，W3即结束，D5不再人为安排开发。若D4存在阻塞：仅需E修正记录时，D5可按`E→负责人`收口；必须先修上游实现时，D5按“一个阻塞文件所有者→E”执行，负责人最终签署顺延至条件D6。

条件D6不开发新功能。D5已经完成“一个阻塞文件所有者→E”后，D6只允许负责人提交E提供且SHA-256不变的受影响项复验记录和最终签署。若D5需要多个独立文件所有者上传，或D6仍有任一V3项BLOCKED，负责人签署W3 NO-GO。

## 8. 文件所有权

| 所有者 | 允许修改 | 禁止修改 |
|---|---|---|
| A | 路径领域规则、Facade实现、会话/活动仓储事务及A测试 | Python进程、B资产、前端、Agent材料、正式gold |
| B | Pandas两个TaskBundle及关联固定资产、B原始标注、B测试 | 路径算法、环境限制、正式gold、E标注 |
| C | Node/Python评测基础设施、内部协议、环境证据、C测试 | 正式Attempt/Evidence、B业务资产、前端、gold |
| D | 模型配置、提示词、录制响应、离线联调和越权证据 | 实时模型、mastery、路径、评测分数、gold |
| E | React mock、前端依赖锁、E测试、E原始标注、V3验证正文 | A/B/C/D实现、正式gold、隐藏资产公开面 |
| 负责人 | 合同、环境裁决、Profile激活批准、终裁gold、门禁和签署 | 代替标注者修改原始盲标 |

## 9. 周门禁

V3-1至V3-8的硬标准见35号。最后统一运行：

```powershell
npm.cmd run typecheck
npm.cmd test
npm.cmd run check:docs
npm.cmd run smoke:extension
npm.cmd run check:release
npm.cmd run verify
git diff --check
```

全部PASS、完整60例gold已冻结、Profile通过事务激活且`HEAD == origin/main`后，负责人才能签署W3 GO。未签署GO不得启动W4功能。
