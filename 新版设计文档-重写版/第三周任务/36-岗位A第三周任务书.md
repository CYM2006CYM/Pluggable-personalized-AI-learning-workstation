# 36 岗位A第三周任务书

状态：`[D0已于2026-08-06生效；D46/W3-C4已于2026-08-08生效；按34号W3-R2调度执行]`。依据20号D34—D46、34号`W3-R2`和35号`W3-C4`；D46不改变A的任务范围。A负责确定性路径、Facade接线和正式事实事务，不负责Python执行器或Pandas内容。

## 1. 开工输入

- 已回填的`W3_START_COMMIT`和D0提交；
- D3负责人对Pandas Profile v2的书面激活批准，以及包含B正式TaskBundle和C正式环境锁的revision 2候选；
- 11号`path-engine-v1`及21号17方法Facade、Evidence、KnowledgeState和仓储合同；
- D1使用W2 GO已经冻结的revision 2候选完成路径和V3双口径测试；D3才消费B正式TaskBundle、负责人批准的C环境锁及C正式`ActivityResult`适配器。

任一输入缺失时，只能做不依赖它的本地准备，不得伪造fixture或越权修改其他岗位文件。

## 2. 逐日任务

| 工作日 | 任务 | 出口 |
|---|---|---|
| D1 | 一次完成先修闭包、`skipEligible`、验证/回补、稳定拓扑排序、难度/脚手架、预算压缩、`path_infeasible`、build/confirm/getNextStep/replan/recover、路径快照、CAS和D45双口径20开发案例测试；获锁上传；本日不提前激活Profile | V3-1 180分钟投影和V3-2原始预算/边界fixture作者证据；A第一阶段提交和交接清单 |
| D2 | 基于C滚动候选一次准备ActivityRepository、正式提交事务和故障矩阵，不上传 | 可在C正式提交后快速复核的本地候选 |
| D3 | 拉取B/C正式提交；负责人批准后通过仓储事务激活Profile v2；完成Attempt/Evidence、KnowledgeState、路径后缀、checkpoint、幂等和故障恢复；获锁上传 | active revision绑定B/C正式输入；V3-4/V3-5/V3-6作者证据；A最终提交 |
| D4—D5 | 只响应E可复现的A所有权阻塞，不主动扩展 | 无阻塞则不上传；有阻塞按34号条件顺序 |

## 3. 必须满足

1. 不修改第二周冻结的`development-20.jsonl`、生成脚本或B资产；源文件按`normalized-text`绑定SHA-256 `54c0f5f30bc0b9a104ac2e9e38e6ca3d6f33c5cbe3ade17c62be1c69be1b8473`。
2. V3-1按`w3-v3-feasible-180-v1`只投影预算为180分钟，必须candidate=20/20、每例10次逐字段一致且实际路径合法率100%；V3-2恢复原始预算，并覆盖“全部掌握”“缺失先修”两个A自有fixture。正式60例gold冻结前不得运行正式案例输出。
3. 只有A事务仓储可公开正式Attempt/Evidence/KnowledgeState/路径和会话版本。
4. 同一`requestId+attemptId`重复提交不重复计分；幂等身份内容不同返回冲突。
5. 每个提交阶段故障注入均无半写入；恢复后不重复提交。
6. `evaluator_error`、环境、依赖、资产或协议故障不生成负Evidence，不触发重算。
7. `mastered`但`skipEligible=false`的先修仍为必做且必须阻塞后置节点；重算只改变未完成后缀，已完成、进行中和锁定节点不移动。
8. 初始路径经candidate确认后active；自动重规划成功时原子发布新active并归档旧superseded，失败继续使用旧active，candidate不得使`getNextStep`失去正式路径。
9. 正式resolver只读active/archived；draft只能经测试专用loader读取。Profile激活只走仓储事务，A不得修改B的候选资产内容。
10. 同层排序严格使用11号五项比较，`chapterId`不参与排序；W3 chapter模式只校验合法chapterId并沿用goal目标及完整先修闭包。
11. 难度必须来自真实Profile活动；必做/最终活动使用声明难度，非必做活动按KnowledgeState区间、距区间距离和Profile声明顺序选择；不得复制或生成B活动。
12. `changeReasons`继续使用既有`PathReasonCode[]`并按实际差异生成，不新增公共枚举，不把四类触发统一写为`evidence_insufficient`。

## 4. 允许和禁止

允许修改A拥有的领域路径、Facade实现、会话/路径/活动仓储与对应测试。禁止实现Python父子进程、修改C内部Rubric协议、B Profile/TaskBundle、D材料、React、依赖、SDK、gold和负责人合同。

## 5. 正式交付

- D1：PathEngine、Facade路径/恢复接线、作者测试、20例180分钟投影V3-1证据、20例原始预算及两个边界fixture的V3-2证据和交接清单；证据登记源文件hashMode/hash、投影规则、逐案两种预算、输入/输出哈希和结果类型；不包含Profile激活；
- D3：Profile事务激活、正式提交事务、幂等/故障恢复测试、ActivityResult到Evidence映射、KnowledgeState/路径后缀更新和最终交接；
- 报告`W3_START_COMMIT`、拉取后实际HEAD、测试命令/项数、`git diff --check`和拟提交文件清单。

A不得把仓库外整库副本、ZIP、缓存或其他岗位文件上传Git。
