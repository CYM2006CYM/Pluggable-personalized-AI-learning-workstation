# 35 第三周公共合同总册

状态：`[负责人已于2026-08-06完成D0签发；D44/W3-C2及W3-R2已生效]`。合同修订号：`W3-C2`，调度修订号：`W3-R2`。20号D34—D44高于本文；21号公共类型仍是源码接口唯一来源。本文只冻结第三周跨岗位实现深度，不复制第二套类型。

## 1. 身份与生效

```text
W3_START_COMMIT = f190326a4a906b46e4001484ffa30a7839b82ed2
```

该值是不可变周起点，不得用D0提交、后续HEAD、单岗位提交或过程报告代替。D0现已生效，成员只可按34号调度、本文合同和各岗位任务书实施。

## 2. Profile激活合同

1. W2 GO是revision 2可申请激活的前置，不等于文件已经active。
2. B正式TaskBundle和C正式环境锁均入库且内容不再变化后，负责人书面批准，A再通过Profile仓储事务完成候选重验、旧active归档、新active发布和失败回滚；禁止手工修改active目录。
3. Git中的revision 2候选资产是可审计来源；运行时active由仓储生命周期管理。
4. `startSession`读取实际active revision并绑定到会话；客户端不能指定revision，进行中会话不跟随active变化。

## 3. PathEngine合同

- `engineVersion = "path-engine-v1"`。
- 输入只包含冻结Profile快照、目标、KnowledgeState/evidenceVersion、入口模式、时间预算和用户合法约束。
- 严格执行先修闭包、`skipEligible`、验证/回补节点、稳定拓扑排序、固定同层比较、预算压缩和最终合法性校验。
- 无合法路径返回`path_infeasible`及结构化补救建议，不能删核心先修伪造成功。
- 相同规范化输入快照产生逐字段相同输出；禁止随机数、当前时间和Agent措辞进入排序。
- 正式Evidence导致状态阈值、`skipEligible`或`error_remediation`变化时，只重算未完成后缀；已完成、进行中和用户锁定节点不移动。

## 4. TaskBundle和评测范围

W3只冻结两个`profile_fixed`任务：

| activityId | 类型 | 用途 |
|---|---|---|
| `act-inspect-dataframe` | `code_completion` | 代码补全正式提交 |
| `act-practical` | `coding_practical` | 最终实操正式提交 |

每个TaskBundle必须闭合题面、入口/输出契约、fixture、公开/隐藏测试、参考实现、已知错误实现、固定安全反馈、Rubric、环境引用和`assetBundleHash`。B定义业务资产；C只能消费并校验，不得重写权重、阻断规则或通过线。

## 5. 公共结果和C内部协议

21号合同A的精简`ActivityResult`是唯一跨边界结果，源码只从领域类型导入。`CodeEvaluationPort`固定为：

```ts
interface CodeEvaluationPort {
  prepare(input: PrepareEvaluationInput): Promise<PreparedEvaluation>;
  run(input: RunEvaluationInput, signal: AbortSignal): Promise<ActivityResult>;
}
```

C内部可以保留prepare、user_code、public_tests、hidden_tests、summarize五阶段、完整Rubric明细、阻断断言和裁剪前运行材料，但必须在端口内确定性映射为公共结果。C源码不得依赖Attempt、Evidence、KnowledgeState或PathRepository，不得创建正式事实。隐藏测试、完整Rubric、参考实现、私有CSV和宿主路径不得进入公共结果、普通日志、Agent上下文或浏览器。

## 6. Node正式提交环境

W3环境状态为：

```ts
type W3NodeEnvironmentStatus =
  | "draft_pending_C_prototype"
  | "measured_node_submit";
```

C在D1一个完整工作日内测量Node、Python、Pandas、平台、执行器版本、依赖白名单、墙钟、stdout/stderr、源码/数据大小、临时目录清理、公开/隐藏进程隔离和进程树终止。未证明的内存或网络能力必须为false或省略。D2负责人只根据可复现证据批准具体值；获批后C才能写入`measured_node_submit`环境锁。Pyodide、浏览器预览和`measured_dual_backend`留到W5。

## 7. 正式提交事务

```text
openActivity创建attemptId
→ saveActivityDraft
→ prepareActivityRun
→ CodeEvaluationPort返回公共ActivityResult
→ A映射Attempt/Evidence候选
→ UnitOfWork原子commit
→ KnowledgeState
→ 路径未完成后缀和checkpoint
```

- 同一`requestId + attemptId`及相同内容重复提交返回首次结果，不重复计分。
- 同一幂等身份但内容、版本或绑定不同，返回`idempotency_conflict`。
- 修改代码重新正式提交必须创建新attemptId。
- 每个提交阶段故障均不得留下半写入或重复Attempt/Evidence。
- 只有确定通过、部分正确和学习者错误可生成正式Evidence；依赖、环境、测试资产或协议故障不生成负Evidence、不降低mastery、不触发路径重算。

## 8. 剩余40例和正式gold

| 文件 | 所有者 | 范围 |
|---|---|---|
| `evaluation/golden/annotations/b-final-021-060.jsonl` | B | `final-021`至`final-060`第一标注 |
| `evaluation/golden/annotations/e-final-021-060.jsonl` | E | `final-021`至`final-060`第二标注 |

两人基于同一冻结输入独立标注，各自封存前互不可见，也不得查看系统对正式60例的路径或输出。双封存后负责人做Schema、覆盖、输入哈希和独立性资格检查并生成机械差异清单；B/E不得查看对方原始标注或机械差异清单，不参加协商，不得修改封存原件。负责人按预冻结口径直接逐案终裁；需要澄清时，B/E只能解释本人已封存字段且不得改变原件。

负责人按`final-001`至`final-060`稳定顺序更新`difficulty-gold.jsonl`、`path-constraints.jsonl`和`adjudication-log.jsonl`。`final-021`至`final-060`的`negotiationStatus`统一为`SKIPPED_BY_D44`。W2前20例正式内容不得改变；三份文件登记完整60例SHA-256。正式gold冻结前，系统禁止运行任何正式60案例路径或输出；`development-20.jsonl`不受此限制。

## 9. React mock和DTO

W3只实现开始、诊断、路径、学习、活动、总结六页。每页必须可路由到达，并覆盖正常、空、错误、`session_version_conflict`和恢复状态。mock对象必须逐字段满足21号安全DTO；前端只能显示服务端给出的状态、reasonCode、分数和反馈，不计算mastery、路径、Rubric或是否通过。

固定依赖为React/DOM `18.3.1`、Router `6.30.1`、Zustand `5.0.14`、Vite `7.1.7`、plugin-react `5.0.4`、jsdom `26.1.0`、TypeScript `5.9.3`和Vitest `4.1.10`。只有E可安装精确白名单版本并修改`package.json/package-lock.json`。禁止真实HTTP、Pyodide、审核页、SDK升级、其他依赖升级、`allowScripts`修改和`npm audit fix`。

## 10. 离线Agent合同

D只使用录制响应联调动态单选/判断schema、固定fallback和越权拒绝。Agent不得修改mastery、KnowledgeState、路径、ActivityResult、Rubric或正式gold，不读取隐藏测试、参考实现和原始标注。本周不接实时模型，不把录制响应成功写成线上模型能力。

## 11. V3门禁

| 门禁 | PASS标准 |
|---|---|
| V3-1 | 20个开发案例各运行10次，快照完全一致，路径合法率100% |
| V3-2 | 时间不足、全部掌握、缺失先修均产生确定结果；非法路径为0 |
| V3-3 | 正确、部分正确、语法错、运行错、测试失败、超时、超输出、禁用导入分类正确；两个任务各连续3次一致 |
| V3-4 | C源码和输出止于公共`ActivityResult`；正式事实只由A事务仓储公开 |
| V3-5 | 重复`requestId+attemptId`不重复计分；各提交阶段故障无半写入 |
| V3-6 | 依赖、环境、测试资产或协议故障不生成负Evidence |
| V3-7 | 60/60独立双标、负责人直接终裁和完整哈希在任何正式60案例系统运行前冻结；B/E不协商 |
| V3-8 | 六页面可达，mock与DTO一致，具备空、错、冲突和恢复状态，前端无业务判定 |

V3项只允许`PASS`或`BLOCKED`。E记录执行提交、命令、项数、环境和证据哈希；负责人复核并签署。任何BLOCKED均不得用解释性文字改写为PASS。

## 12. 修订记录

| 修订 | 日期 | 内容 |
|---|---|---|
| `W3-C1/W3-R1` | 2026-08-04预冻结 | 冻结五日调度、路径、两个任务、公共结果分层、Node环境、事务、剩余40例gold和React mock；待W2 GO哈希回填及D0签发后生效 |
| `W3-C1/W3-R1` D0签发 | 2026-08-06 | 回填`W3_START_COMMIT=f190326a4a906b46e4001484ffa30a7839b82ed2`并正式生效；不修改预冻结的五日任务内容和上传顺序 |
| `W3-C1/W3-R2` | 2026-08-06 | 业务类型、算法和V3门禁不变；按单日闭环与真实依赖原则压缩调度，正常主链D4完成，D5改为条件缓冲 |
| `W3-C2/W3-R2` | 2026-08-06 | D44取消剩余40例B/E协商；保留独立双标和双封存，由负责人生成机械差异并直接终裁；上传顺序和其余V3门禁不变 |
